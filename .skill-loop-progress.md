# Skill Loop Progress
# Skill: project-startup
# Target: /Users/josh/Developer/omp-orchestrator
# Total Passes: 10
# Started: 2026-09-02T02:20Z

## Status: IN PROGRESS — Pass 2 of 10

## Why this order

Pass 1 is CI, not plan-shape, because a dark CI signal makes every later pass unverifiable.

**Measured 2026-09-02 — and the figure I first wrote here was wrong.** `gate.yml` had run on every
push and failed **67 times, 67 of 67**, all since `2026-09-01T05:59:47Z` (`gh run list --limit 200`).
My original line said "six consecutive red runs" because I ran `gh run list --limit 6` and reported
the limit as the total — the same badly-shaped-probe class as `head -10` on a crontab.

Root cause, corrected by CITruth and it matters: **not** "asupersync needs nightly" but
**asupersync's DEFAULT FEATURES need nightly** — `default = ["proc-macros", "nightly-outcome-try"]`
in its own manifest. The distinction makes `default-features = false` a real alternative, which is
why `5c7ff11`'s message argues against it (11 manifests, one under concurrent authorship, and it
would silently drop `Try` for `Outcome` across every kernel call site) rather than ignoring it.

The local `PRE_PUSH_GATE_OK` green-lit all eight commits regardless. A loop that cannot verify its
own passes is ceremony, so the signal gets restored first.

Missions 2–10 then follow the skill's own Steps 1–10 applied to this repo.

## Missions
1. **CI truth**: asupersync needs nightly; CI runs stable. Restore a real gate signal, and make
   the local pre-push gate stop certifying what CI rejects.
2. **Plan shape**: `docs/PLAN.md` is 1,080 KB and 26% of our docs exceed 50 KB against the
   corpus's 1%. Split the monolith into per-undertaking `docs/plans/plan_to_<verb>.md`.
3. **DPB bar as a gate**: five of seven substance markers sit at 0–4%. Wire
   `references/document-pass-bar.md`'s runner as an enforced ratchet, known-good leg included.
4. **Contract corpus**: scaffold the nine Phase 1 kernel contracts (`subprocess`, `cancellation`,
   `ack_spine`'s three authorities, `receiver_receipt`, `finding`, `scratch_home`,
   `dispatch_claim`, `kernel_only`) to the bar.
5. **error_codes/**: the single biggest omission — corpus has 45, we have 0. One document per
   error family across 51 crates, starting from an exit-code registry.
6. **Program epic**: file bead #1 as a program epic naming the whole arc in its title plus its own
   closure condition, after `frankenfs` and `eidetic_engine_cli`.
7. **Bead hygiene**: close the measured gaps — `close_reason` 47% vs corpus 79%, `dependencies`
   31% vs 53%, and the epic-blocks-own-leaf strangle class.
8. **Measurement stack**: `conformance/`, `fuzz/`, `benches/` are 0/0/0 here against 32/34/28
   repos. Create them and NAME the external oracle.
9. **Requirement registry**: replace `00-brief.md` §2's prose status table — which drifted in both
   directions — with an artifact-path-bound registry plus a both-directions gate.
10. **Corpus mining + ledger**: mine a repo we have NOT read (rotate off asupersync), answer the
    open questions in the improvement ledger, and append the mandatory row.

## Completed Passes

### Pass 1 — CI truth — 2026-09-02T03:35Z — PRODUCTIVE
- Agent: `CITruth`
- Commits: `5c7ff11` (pin the toolchain), `fa7d9a4` (pre-push-gate stops certifying what CI cannot
  reproduce, 5 legs), `4b398e4` (`fetch-depth: 0` on all ten checkouts)
- **Acceptance MET, verified by me independently**: `gh run view <id> --log-failed | grep -c E0554`
  returns **0** on both new runs. Jobs went 0 success -> **5 success / 5 failure**, and
  `porting-gate` from dying at the asupersync compile to `✓ 25m28s`. The five failures are REAL
  verdicts about our code: `kernel-bypass-gate` (81 bypasses / 123 files), `omp-inventory-map`,
  `installer` (E0433), `no-shell-gate` (`numbers`), `commit-build-fence` (a non-verdict, beaded).
- **The third commit exceeded the pass and was right to.** Run 1 showed `findings_ledger` red in CI
  and green locally, failing its OWN anti-vacuity control — it runs `git show --name-only 07de72b`
  and `checkout@v4` defaults to depth 1. A verdict about the checkout, not the code: the same
  false-signal class as the 67 reds. Run 2 confirms it gone, replaced by a real `numbers` failure.
  Scope creep, accepted, recorded here rather than laundered.
- Gate's new output claims exactly two things and says so: that somebody recorded a green workspace
  after the newest tracked source changed, and that they used the compiler CI installs. It states
  it did NOT run the suite, did NOT contact GitHub, and does not know whether CI's job list matches
  the local gates.
- Beads filed: 5, each with a NO-CLAIM — `six-reds-deec6e8-tub` (P2, with `--no-verify` and
  "receipt deliberately NOT written" on the record), `receipt-writer-identity-gum` (P1),
  `fence-ci-non-verdict-16l` (P1), `installer-dep-uncommitted-txl` (P1).
- **It corrected its own P0 claim down to P1.** Its "a stale binary is still writing receipts"
  report was wrong: `~/.local/bin/pre-push-gate` does not exist, all three on-disk copies sha to
  post-upgrade builds, and the `1788315601` write was 20:20:01 — three and a half minutes BEFORE
  its 20:23:51 reinstall. It read a one-minute ordering backwards, retitled the bead, dropped the
  severity, and put the correction in a comment. What survives is a latent hole: the receipt
  carries no writer identity.
- Two defects it deliberately did NOT fix, both correct calls: `commit-build-fence`'s argv (would
  swap one non-verdict for another while looking like progress) and `installer`'s Cargo.toml line
  (another pane's uncommitted state — committing it would sweep them).
- Verdict: **PRODUCTIVE**. The loop can now verify its own passes.

### Note for every later pass — the packet template was wrong
`CARGO_TARGET_DIR=${TMPDIR}/...` is REFUSED by `crates/omp-orchestrator/build.rs:80` with
`CARGO_TARGET_POLICY_REFUSED code=unowned-target registered_root=/Volumes/BuildFH/lanes`. The house
form is a lane under `FRANKEN_CARGO_TARGET_ROOT`. Further: even a per-agent lane name is advisory —
a wrapper rewrites it (`[CARGO_LANE_COLLAPSED] requested=/Volumes/BuildFH/lanes/citruth-receipt
lane=session-omp-orchestrator`). Still off the shared repo target, so the contention guarantee
holds, but do not promise a private lane name.
