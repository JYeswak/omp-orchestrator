# Skill Loop Progress
# Skill: process-audit composite — /rigor-atlas + /brennerbot-with-ntm + /vibing-with-ntm + /choose-the-best-skills-for-me-to-run-in-this-project
# Target: the LIVE planning / dispatching / management process of this repo (the 2026-09-02 session as subject), graded against docs/plan/00..12 and AGENTS.md
# Total Passes: 10
# Started: 2026-09-02T21:40:00Z

## Status: IN PROGRESS — Pass 9 of 10
## Frame (Josh, 2026-09-02): "make sure that we are measuring this against more than just dispatch or not"
The measurement frame is §11.2's matrix — S1..S9 stages × {template, dispatch, reap, logging, build grading} = 45 cells, 3 visible `Y` at plan time — plus §12's seven-field dispatch contract, §05's A1–A11 negative patterns, §06's four legs × six properties, §09's M1–M7 + PV1–PV8, and R24's corpus lens (F1 no perf requirement, F2 no fuzz, F3 harnesses discussed ~80× built 0).

## Output contract for EVERY pass (Rule Zero)
A pass produces PRODUCT: beads filed via `br create` (WHAT/WHY/ACCEPTANCE, `--actor <PassName>`), each with line-numbered evidence and a falsifier — never a new document. Findings that are already tracked are cited by bead id, not re-filed. A pass that finds nothing must name 3+ specific checks it ran and what they returned. Subagents are read-only toward source and the running fleet: no dispatch, no pane sends, no edits to crates/, no `cargo test --workspace` (red from a peer's uncommitted `fleet-truth` edit; scope to `-p`).

## Missions
1. Matrix re-measured live (§11.2 / brennerbot): every one of the 45 S×property cells is a hypothesis with a falsifier; grade what TODAY's session exercised, cell by cell, and file a bead per cell that failed live.
2. Dispatch contract vs actual packets (§12 seven fields / vibing-with-ntm dispatch): audit the packets actually sent today (/tmp/d-*.md, ack-*.md, p*.md, and the loop's own) against Trigger/Packet/Amazing/Adequate/Negative/Skills/Done-signal; file the missing-field classes.
3. Honest-credit audit (vibing-with-ntm Second Rule): sample the 97 closes and 43 delivered-awaiting-grade; how many closed on re-executed evidence vs self-report; commits-as-KPI, refusals-as-delivery, process artifacts as progress. File the credit debts.
4. Liveness truth stack (vibing-with-ntm One Rule): pane truth / robot state / work state / artifact evidence — enumerate every instrument error today (≥17 known) as a taxonomy; which are structural (need a gate) vs habit; file the structural ones.
5. Rigor-atlas prescriptions (process-kernel / control-kernel / scheduler-kernel / monitoring): name the falsifiable invariant per stage; map today's defects to prescriptions (tri-state-indeterminate-never-allow, typed-no-data-metric-taxonomy, incarnation-lease-fencing, serialize-the-check-then-act-boundary, no-number-without-a-falsifier, receipts-as-typed-objects); file each absent prescription with the mirror exemplar path:line.
6. Gates re-measured (§06 four legs × six properties): plan said 2/8 all-four, 0/8 all-six; re-measure NOW after kwo9/leht/iis6; which gates fired today and which stayed vacuously green; file the delta.
7. Measurement stack vs the corpus (R24 F1–F3, §02): no performance requirement, no fuzz, harnesses built 0, 4.46% direct-probe coverage; state what must hold true across the surfaces and how it is measured; file the missing requirement classes with corpus citations.
8. The unanimous structural gaps, live (§12 duel): S9 human-decision ledger, fleet/namespace identity, leases with expiry — today's identity/liveness/reservation failures ARE these gaps firing; measure how many human decisions today reached docs/decisions.jsonl, how many identity ambiguities, how many lease expiries bit; file what is not already tracked.
9. Milestones and plan-validation (§09 M1–M7, PV1–PV8): did today's autonomous dispatch constitute M3/M4/M5 progress by OBSERVABLE, not claim; independent readback per bead; zero-human closes; grade each milestone against evidence and file the gap.
10. Skill juncture (choose-the-best-skills One Rule + §12 Appendix A): at THIS juncture — autonomous dispatch just resumed, 43-grade backlog, identity crisis — which ≤3 skill campaigns, evidence-backed; which skills should have run today and did not; file the process gaps a skill would have caught.

## Completed Passes

### Pass 1 — Matrix re-measured live — 2026-09-02T22:05Z
- Files changed: .beads/issues.jsonl (4 beads), this file
- Changes: 45-cell matrix re-measured for today with a command per cell. Visible Y went 3 → 7 (S4 dispatch, S4 logging, S5 template, S5 dispatch, S5 logging-heartbeat, S6 logging, S6 build-grading). REFUTED the plan: `bv --robot-triage` IS invoked (refill-idle-panes main.rs:227, cron 8,28,48, ledger 15:29). Three of the seven Y carry a refuter that a second/broken/hand-only mechanism sits beside them.
- Beads: `ve4w` P0 two live dispatch authorities, no shared claim fence — cron refill sent oj6.3 into control-plane %1389 (Target=control-plane) while the supervisor sent it to %1414; 82× fc-22l to pane 1. `8zx1` P0 S6 grading has no dispatcher/packet/consumer — 4 grading + 43 delivered wait on 2 hand packets/day. `yfp2` P1 supervisor's Agent Mail identity is ambient launchd AGENT_NAME=WildStone (a ~/Developer/fsw agent) — 21/23 dispatch-result mails refused. `nsx1` P1 S9 has a reader and no writer — 0 decision rows today vs 188 owner=josh refusals and an SLB approval that blocked the reaper install all afternoon.
- Cited as tracked: gfm6, t784 (partially stale — `grading` status now exists), eg0m, iis6, 2kx2, 93lo, vh1g, j2z9, t00+8nuh, ipg.19, 6hrv, p3f4, kxe.4, 42na.
- Handoffs: M2 ← 0/16 hand packets and 0/1 supervisor packet carry the ntm template's required fields. M3 ← t00 worker comment 19:37Z claims the plist no longer contains OMP_REAP_SWEEP; live plist (mtime 14:32) contains it — self-report contradicted by installed state; 13/37 closes josh-only-authored. M6 ← census flipped REACHABLE/UNWIRED between ticks once. M8 ← nsx1 + %1414 dual identity + yfp2.
- Commit: ae6bbd8
- Verdict: PRODUCTIVE

### Pass 2 — Dispatch contract vs actual packets — 2026-09-02T22:30Z
- Files changed: .beads/issues.jsonl (2 beads), this file
- Changes: 26 hand packets + 8 supervisor packets audited × the seven fields + AUTH/ACK/STOP. Population: Trigger hand 21/26 supervisor 0/8; Done-signal-with-exit 0/34; Stop 0/34; template use 0/34 (positive control on the template → 4); Skills 2/26; Adequate 8/26. Supervisor packets carry the bead body VERBATIM (8/8, only members satisfying §12:318) but drop the typed `acceptance_criteria` field (BrShowRow has no such field; bead 815 carries 41 acceptance lines ONLY there). Hand packets restate the bead from memory 26/26; one (d-8e1g) added acceptance items the bead lacks. 32/32 ACKs carry a sender-dictated pane id; 1/32 is receiver-derived.
- NEW FAILURE FOUND: the orchestrator's 15:21 `$TMUX_PANE` fix (p2/p3/p4.md) dropped the parser's ` -- ` separator → 5 ACKs unparseable, incl. live supervisor dispatch ipg.19 (ACKed 33s after send, still RETRY_BLOCKED at +7min). The fix for dictation introduced a parse defect. Also: ipg.4 closed DONE with 0 ACK and 7 RETRY_BLOCKED — a close the loop believes was never delivered (→ M3).
- Beads: `a92y` P0 send_and_verify renders 4/16 template fields and drops typed acceptance; acceptance 4 makes hand dispatch call the renderer so both paths emit ONE packet. `elq6` P0 ACK instruction hand-typed in two media and drifted from the parser twice; falsifier: any post-fix packet whose ACK contains a literal %N, a sender-chosen --actor, or a prefix the parser does not match.
- Cited: 93lo (supervisor still emits no ACK line, 8/8 on three builds), 8zx1, ve4w (hand packets are a THIRD dispatch path: 26 today vs 8 loop), yfp2, t784, gfm6, mad1/nfgc (FROM line), 3kpc, iis6, 2kx2.
- Verified correct: supervisor body verbatim 8/8 (no acceptance substance lost today despite the dropped field); FROM 25/26; claim+readback stated in every post-12:37 hand packet; no size cap tripped ≤6017B; grader assignment recorded on beads; all 7 dictated pane ids were the CORRECT pane (defect is evidentiary, not a mis-send).
- Commit: 76a0d17
- Verdict: PRODUCTIVE

### Pass 3 — Honest-credit audit — 2026-09-02T23:05Z
- Files changed: .beads/issues.jsonl (3 beads), docs/decisions.jsonl (HD-0008), this file
- Changes: population 40 today-closes (pass 1's 37 predated three closes), 105 all-time; mcq2's 18 non-conforming reproduced exactly. All 40 graded on a/b/c/d with re-runs (`cargo test --offline -p …`, 12 suites re-executed). COUNTERFEIT CLASSES: implementer self-close 9 (8 of the 9 closes since autonomous dispatch resumed at 18:18Z); grader unattributable 23/40 (comments authored `josh` only); ledger-says-undelivered-bead-says-done 5 (232, kxe.5, ipg.4, ipg.18, j5r — each with DISPATCH_FAILED + AWAIT_HUMAN + 5–8 RETRY_BLOCKED + mail refused); refusal-as-delivery 1 (ipg.18: wired_lanes 19/5 RED at HEAD by the close's own admission); commit-count-as-KPI 0 (positive control). Delivered-awaiting-grade: 50 citing a real sha (pass 1: 43); 22 claim done with no "not graded" disclaimer; 2 (1p0u, ewnr) carry a PASS grade and were never closed — the grader graded and did not close.
- Beads: `qt9r` P0 implementer self-close class — mechanism: refuse close when actor==assignee or last-DONE author; record closed_by. `gcyf` P1 unattributed grader class — --actor required from panes, GRADER_PROSE_MISMATCH refusal, backfill. `vggs` P1 close↔dispatch reconciliation — CLOSE_RECONCILED/UNRECONCILED rows, AWAIT_HUMAN retirement.
- REOPEN/REGRADE: mi1o (WONTFIX by its own assignee, zero comments/commands/shas) → reopen. ipg.18 → regrade: a permanently-RED cargo target cited as satisfying acceptance 1. 232/hsv/iad/i0kp/ipg.4/kxe.5/j5r → assign graders, not reopen — every re-runnable claim matched.
- Verified sound end-to-end (a,b,c,d): leht (census_membership 11/0), mj8w (installed build_id ancestor chain), oe2 (refill 35+6/0; oracle_routing 7/2 where both fails are post-close tree changes), h27o (pane-dispatch-ready 51/0), 193/hsv (findings_ledger 17/0).
- POST-CLOSE DRIFT, not false closes: 4wmo's installed pre-commit hook sha (11:45) ≠ pre-commit-gate.rs (13:31); `hook_freshness` panics STALE live. 58u2 measured 61 packages, now 65.
- Instrument notes: `br list -a --json` default page (242) hides closed_at/close_reason — grade from .beads/issues.jsonl; two suites went RED once / GREEN on rerun under parallel cargo load on the dirty tree — single-run reds are unproven; `~/.cargo/bin/cargo` is now rch-hooked.
- Human decision surfaced: HD-0008 (183 unpushed commits, public remote) → Josh: push. Recorded in docs/decisions.jsonl — first S9 row today.
- Commit: 3b34887
- Verdict: PRODUCTIVE

### Pass 4 — Liveness truth stack (One Rule) — 2026-09-02T23:50Z
- Files changed: .beads/issues.jsonl (3 beads), this file
- Changes: 100 distinct instrument-error events on 2026-09-02 (71 bead comments matched; 22 numbered instances; 5 panes' scrollback; 12 AGENTS.md commits), ≥40 the orchestrator's own. 16 classes. STRUCTURAL with ≥2 instances and no existing mechanism: C1 grep-matches-prose (16) → `xrtc`; C2 missing-representation (11) → `r73l`; C3 wrong-input verdicts (16: cargo-vs-commit, hook-vs-empty-index, wrong process) → `xv30`. Already-filed mechanisms cited for: roster-returns-empty (7 → 2kx2), dictated-echo (4 → elq6/gfm6), single-capture (4 → iis6/riqd/2tny), stale-figure (6 → opsa/d1n), any-over-disjunction (5 → 8jli), loud-in-principle-silent-in-fact (5 → nsx1/4wmo). HABIT: truncated instrument (4), keyword-grep-over-help (1+2 caught by readback), $?-after-pipe (3), timezone (6 local-as-Z + a 26-comment drift series), diagnosis-without-definition (5 → 3r1r/xv30).
- Two numbering sequences for one class ("seventh" by AmberGate at 21:44Z vs "eighth"/"ninth" by the orchestrator) exist only in scrollback — gjyi's failure mode, measured. Orchestrator's `2>/dev/null` idiom: 70× in 51 command blocks, the enabling condition for reading empty as permission.
- Positive controls (all four truth sources agreed before action): h71g dispatch; the identity-cleanup veto (%1367/%1389 live in control-plane); eg0m after 2kx2 (roster empty vs authoritative snapshot 2 locks); 8nuh refutation (binary-is-not-the-variable, 9 vs 10 env vars); 6fot FAIL (strings over the Mach-O hook, positive control 8/2/1); 1p0u PASS (tree pinned first). Pins fired correctly 3/3 — the pin family is sound; the one text-keyed pin (C1 "twelve") is the exception.
- Commit: b286ee5
- Verdict: PRODUCTIVE

### Pass 5 — Rigor-atlas prescriptions — 2026-09-03T00:05Z
- Files changed: crates/loop-switch/src/lib.rs, crates/loop-switch/tests/non_file_at_path.rs, .beads/issues.jsonl (6 beads), this file
- Changes: loop classified process-kernel (KINDS.md:1071 — reusable pane ids, non-transactional delivery, no substrate atomicity), secondary scheduler/control/monitoring; do_not_claim: liveness from a safety check, continuity from a typestate, two-person degrading to one, exactly-once delivery, wall-clock fairness. Six-stage invariant table: S4 PARTIAL (bead-availability typed, but loop-queue-filter clock `unwrap_or(0.0)`), S5 PARTIAL (claim permit + pane fence + marker exist; no incarnation, no idempotency key, both live dispatchers bypass the fence), S6 ABSENT (string classifier, no actor), S7 PARTIAL, S8 EXISTS at install / ABSENT at run time, S9 ABSENT (reader only; nsx1). 13 prescriptions mapped, every KNOW with a mirror path:line. Corpus-ranked prescriptions no bead named → filed ywd5 (incarnation-lease-fencing, P0), 7kxf (saga-outcome-unknown-state, P0), igwd (two-person-rule-transition-table, P0), j2te (sampled-reexecution-trust-ladder), wbo2 (layered-enforcement-of-one-rule). Five prescriptions already satisfied here with path:line.
- PRODUCT CHANGE (Rule Zero): the forbidden-claim grep hit `loop-switch/src/lib.rs:14,16` — header said the switch "cannot fail INTO the off state" and unreadable means RUNNING; the code beside it returned Off on any read error, and `Path::exists()` is true for a directory. Reproduced: `mkdir` at the switch path → fleet OFF with no human intent. Fixed `b1906c7` (`is_file()` gate, header rewritten to match; known-bad RED before / GREEN after, known-good file→OFF, mutation bites, byte-identical restore). Positive control for the grep: a planted "guarantees exactly-once" hit at line 3.
- BUILT ≠ WIRED, found while installing the fix: the live supervisor never reads the switch (0 hits in omp-orchestrator/src, no Cargo dep); its only consumers are fast-dispatch/loop-driver, which nothing schedules here, and the installed copies are control-plane 08-26 builds. Filed 31lo P0 with six acceptance legs incl. install. main.rs held by GreenFrog to 00:59Z.
- Instrument note: socraticode timed out twice (index re-embedding 0/539) — outcome UNKNOWN, fell back to structural reads.
- Commit: 50c1288
- Verdict: PRODUCTIVE

### Pass 6 — Skill selection (One Rule) — 2026-09-03T00:40Z
- Files changed: docs/plan/skill-ledger-20260902.md (701 rows, validate-run.py exit 0), .beads/issues.jsonl (4 beads), this file
- Changes: profile Rust 100% / 66 members / 36 bins / no golden-fuzz-property-conformance / CI runs no cargo test; juncture = autonomous dispatch resumed under a supervisor emitting 344 RETRY_BLOCKED / 298 REFUSED / 188 GATE_UNWIRED vs 81 RESULT_RECORDED, 205 commits unpushed behind a correct pre-push refusal. **0 of the 4 skills this loop applied were evidence-selected — chosen by name from 11-lifecycle.md:355-366.** The evidence ranks the same defect family but routes it to agent-mail (37), just-say-no (37), beads-compliance (33), reality-check (32). Contra-indicated: installer-workmanship (deliverable is install.sh; repo forbids .sh). Unranked: multi-agent-swarm-workflow, jeff-swarm-ops. Should-have-run with ≥3 defects behind absence: beads-compliance/validation-discipline (the plan's own S6 skill, 0 invocations) → o7gz P0; oracle-gates/gate-truth (gate.yml runs no cargo test, red 10/10 on a stale sha) → 28dq; trauma-guard/accretive-cron (identical refusal repeated 458×/344×/85× with no recurrence halt) → 9he5; ee (no store here) → v33s.
- Instruments UNKNOWN, verbatim: fh `SEARCH_INDEX_STALE ... exit 3` on target AND positive control (instrument down, harvest did not run today); ee `workspace_store_missing` (no .ee here); cass `stalled, 484 quarantined`; jsm suggest ignores task text; ms suggest cwd-only (positive control via `ms search` hit vibing-with-ntm first).
- PLAN CHANGE for passes 7-10 (evidence over name): 7 = beads-compliance-and-completion-verification (S6; Joshua's "dispatch failure with grading" is this lane), 8 = agent-mail (identity + claim fence, camp-01), 9 = ntm-fleet-monitor + beads-bv (widest juncture anchors), 10 = just-say-no (honesty inventory of the loop itself). Dropped: installer-workmanship, multi-agent-swarm-workflow, jeff-swarm-ops, beads-north-star (folded into 7).
- Root cause delivered this pass on Joshua's ask: zlyy P0 — four kernel defects (send_and_verify Ok/Err collapses a 3-valued verdict → every dispatch reads DISPATCH_FAILED, 86 today; the marker is a clock that never calls read_ack_readback; expiry re-selects `first()` with no cooldown; claim owner is a per-tick pid).
- Commit: 894369a
- Verdict: PRODUCTIVE

### Pass 7 — Close grading by re-execution (the S6 skill, 0 prior invocations) — 2026-09-03T02:20Z
- Files changed: .beads/issues.jsonl (5 reopens, 2 beads, ~12 measurement comments), this file
- Changes: population 49 closes on 09-02/03 (positive control 35 on 09-01). ~45 single-crate re-executions on the lane. **5 FAIL-REEXECUTED → reopened** (588v hook_freshness RED — the installed hook predates c10a96a/4e1df0b; calr empty_staged known-good RED; d1n plan_citations ratchet breached by 54d8574; leht census 7/4 with positive control unreachable; ipg.18 red by the closer's own admission). 4 of the 5 were TRUE at close and regressed the same day through gates nothing on the commit path runs → yqun P0. Test verdicts differ by rch worker (findings_ledger 17/17 vs 14/17; registry not on worker) → jj7v P0. Honesty score: **30/49 gate running code (24 green, 5 red, 1 dirty), 13/49 gate a document, 6/49 gate nothing.** Same-actor closes 17/49; closer=josh 31/49 with 21 prose-only graders (qt9r/gcyf measured). Unverifiable from the bead alone: 4/49 (8%). North-star reading: the gap is acceptance DURABILITY, not presence — a close-time exit-2 tripwire would have caught 0/5 of tonight's reds; a nightly re-run all 5 (o7gz/j2te).
- Instrument error, self-caused and measured: the grader held a sqlite handle on beads.db for ~25 min; `br list` 94,963ms → 64ms after the kill (b5e5).
- Decision recorded on 588v: the live hook stays at the 11:45 build (lacks GATE_SECTION_HELD) rather than HEAD's, because HEAD's GATE 7 compiles a crate locally on every commit; unblocks on g5e0 acceptance 2.
- Also this tick (not the pass): fuzz workspace 4fd6a87/9fbe43c (first run fired in <1s on a harness bug; 19.6M + 44.2M clean runs), fuzz epic k81x + 7 children (ed52 → zlyy), rust-analyzer checkOnSave=false 260b729 (six RA instances were the local-build source), 929j installed → 247s → reverted (g5e0; AmberGate fixed the shim resolution in 292714f).
- Commit: 86fc381
- Verdict: PRODUCTIVE

### Pass 8 — Agent Mail identity + claim fence (pass 6's top campaign) — 2026-09-03T03:05Z
- Files changed: .beads/issues.jsonl (1 bead, 3 comments), this file
- Changes: roster truth — 5/5 agent panes BOUND (identity file after process start, resolve_pane_identity agrees), but binding = `legacy-unverified` 5/5 (bare-name files, no pid/socket facts); **20 registered agents with no live pane**, 2 of them (SilverWolf, BlueLantern) still holding 23 in_progress beads and granting reservations — subagents/relays of pane 1; **13 of 20 assignee strings are not Agent Mail agents** (15 in_progress beads), all four `supervisor:<pid>` dead. Reservation discipline over 147 crate commits in 24h: **76.9% fully covered, 12.2% unreserved, 10.9% partial**; 15 HOLDER_MISMATCH (4 pure aliases); positive control b1906c7 reconstructed exactly. verified_sender **0/5** — the field exists only at send time and is persisted nowhere; the daemon runs the open profile; the supervisor path never signs. Mail health: health_level RED behind 7 green verdicts (commit coalescer p99 31.8s, 1111 blocking-dispatch timeouts); 82 DISPATCH_RESULT_MAIL_DEGRADED in 24h, 76 = `WildStone not found` (yfp2, running supervisor pre-fix); **fd_exhaustion 62 in 24h, launchd soft maxfiles=256, no setrlimit** → smcq P1. Unread: SnowyCanyon 97 (oldest 21h), GreenFrog 152, BlueLantern 189.
- Missing mechanism: `macro_start_session` at kickoff (SKILL.md:22-36) — yfp2 acceptance 1 IS it for the supervisor (commented there, not re-filed); u21m accepted namespace-2 for subagents. Warning recorded: do NOT install the skill's `install_precommit_guard` — it keys on the ambient AGENT_NAME yfp2 proves foreign.
- Surface disagreement re-measured: `am robot reservations` 27 == MCP snapshot 27 after 64cb8db; the 2kx2 empty-roster shape did not reproduce.
- Commit: 29fc567
- Verdict: PRODUCTIVE

---

# LOOP 1 CLOSED — SUPERSEDED 2026-09-05

Loop 1 (process-audit composite) reached **8 of 10 completed passes**, all PRODUCTIVE,
last commit `29fc567` at 2026-09-03T03:05Z. Its `Status` line above still reads
`Pass 9 of 10` and that is now **stale, not in progress**: its subject was the
2026-09-02 session, which ended ~46h before this file was next touched. Passes 9
(milestones/plan-validation) and 10 (just-say-no honesty inventory) were never run.
They are NOT abandoned silently — the two missions are recorded at lines 23-24 and
remain valid work. Closing loop 1 rather than resuming it because its measurement
frame ("grade what TODAY's session exercised") cannot be re-run against a session
that is over; re-running it now would measure a different day and report it under
the old frame, which is the wrong-input verdict class its own Pass 4 filed as `xv30`.

---

# Skill Loop Progress
# Skill: atlas-arc (Planning Compiler and Execution Constitution) — /Users/josh/.claude/skills/atlas-arc/SKILL.md, 833 lines, version 1.0.0
# Target: this repo's LIVE planning process — docs/PLAN.md (8,209 lines), docs/plan/00..12, docs/plan/flow/boxes/S1..S9, docs/planning/{DESIGN_INDEX,ROUND_LOG,CENSUS-EXCLUSIONS,round3..round6d}, .beads/issues.jsonl, docs/decisions.jsonl
# Total Passes: 10
# Started: 2026-09-05T02:20:00Z

## Status: IN PROGRESS — Pass 3 of 10 (passes 1-2 complete; pass 2 awaiting R6 grade by a non-author)

## Resolution of the invocation
Joshua invoked `/atlas-arc`. It **is** installed (`~/.claude/skills/atlas-arc/`,
`~/.agents/skills/atlas-arc/`) and is the skill being applied. Measured caveat that
governs every pass: the install contains **`SKILL.md` + `references/` only — there is
no `scripts/` directory and no `arc.py`**. So `arc.py init`, `lint`, `status`, and
`certify-plan` **CANNOT RUN**, and `BUILD_READY` cannot be tool-certified in this repo.
Per atlas-arc's own rule an unavailable tool is `UNKNOWN`, never a pass. Every pass
below is therefore hand-measurement against the rule text, and no pass may emit a
`BUILD_READY` claim. The sibling `planning-arc` v1.2 DOES ship a working
`scripts/validate_plan.py` (`--selftest` PASS, planted defects detected) and is used
as the executable oracle where a rule overlaps; that is a tool substitution and is
labelled as one wherever it happens.

## Standing constraints on every pass
1. **BUILD FREEZE is active** — `docs/plan/flow/CONTRACT.md:54-58`: no new crate, no
   feature bead, no install, no canonical mapping change until every box has
   `agreement.status=converged` plus Joshua approval. Passes may measure, file beads,
   and edit `docs/planning/**`; they may NOT create crates or install anything.
2. **Rule Zero, inherited from loop 1** — a pass produces PRODUCT: beads via
   `br create` with WHAT/WHY/ACCEPTANCE and a falsifier, each carrying line-numbered
   evidence. Never a new document for its own sake. Already-tracked findings are cited
   by bead id, not re-filed.
3. **R6 applies to the loop itself** — no pass grades its own output. A non-author
   (pane 1 or pane `%9`) re-executes each pass's claims before the pass is committed.
4. **File ownership** — `crates/**` is held by live panes (`%7` on `xl9d`, `%8` on
   `1hdf`). Loop passes own `docs/PLAN.md`, `docs/plan/**`, `docs/planning/**`,
   `.beads/issues.jsonl`, `docs/decisions.jsonl`. A pass needing a crate edit files a
   bead instead.
5. **Host constraint** — `/Volumes/BuildShared` at 95%, 484 MiB free, below the 1.0 GiB
   floor documented at `crates/omp-orchestrator/src/main.rs:2361`; machine
   `tier=saturated`. Single-crate `cargo test -p <crate>` is bounded and permitted;
   `--workspace` and release builds are not.

## Missions — derived from atlas-arc's ten absolute rules, one per pass
1. **R1 breadth-before-depth + truth surfaces**: compute `max(section_maturity) - median(section_maturity)` across all twelve boxes and PLAN.md's sections against the six maturity levels (0 Mapped, 1 Grounded, 2 Contracted, 3 Falsifiable, 4 Compilable, 5 Frozen); every violation needs a `CRITICAL_PATH_EXCEPTION` with reason/affected/expiry/reviewer/risks or is a defect. Also census the required `.atlas-arc/` truth surfaces — manifest, charter, plan index, implementation status, sections, registries — and report which of the four governing surfaces (Charter / target-state / current reality / execution state) are machine-visible versus conflated.
2. **R2 stable IDs before prose growth**: atlas-arc names sixteen ID kinds (`REQ DEC IF DATA STATE AUTH INV CLAIM RISK UNK GATE WS B DEF RB REC`). Census which kinds exist at all, which have canonical owners, and which are restatements pointing at a non-owner. Known starting evidence: `DESIGN_INDEX.md` 51 canonical IDs vs 11 in the constitution vs 7 body-earned, and `CENSUS-EXCLUSIONS.md` rules 1-5 govern the census instrument.
3. **R3 unknowns are typed scheduling objects**: every unknown must carry exactly one of `BLOCKS_PLAN BLOCKS_BUILD ASSUME_REVERSIBLY DEFER_TO_BEAD HUMAN_DECISION OUT_OF_SCOPE`; "open", "TBD", "investigate", "decide later" are invalid alone. Measure against the 88 wave disagreement rows and every `UNK-*`/open-question surface; the earlier 269-unknown typing claim must be re-derived, not inherited.
4. **R4 review emits typed deltas**: a `DEF-*` needs nine fields — severity, class, affected IDs, evidence, failure consequence, minimal repair, owner, status, verification method. Audit `docs/planning/round3..round6d/DEFECTS.toml` for all nine, and audit the "only the integrator edits canonical surfaces" rule against who actually edited what in rounds 3-6.
5. **R5 fresh agents are the build-readiness oracle**: run the six-point packet test for real — a fresh agent given only a bounded packet must state purpose/non-goals, locate owned surfaces, name upstream+downstream contracts, state acceptance and negative tests, name blockers and permitted assumptions, and begin without rereading the plan or asking an avoidable question. Use a real queued bead; the honest failure mode is that it asks an avoidable question.
6. **R6 no self-approval**: audit every close in `.beads/issues.jsonl` for author==closer, unattributed graders, and closes whose evidence was read rather than re-executed. Loop 1 measured 9 implementer self-closes and 23/40 unattributable graders on one day; re-derive across the full tracker and check whether the mechanism beads filed then (`qt9r`, `gcyf`) changed the rate.
7. **R7 claims are earned by evidence classes**: every nontrivial claim needs claim class, validity domain, oracle, negative control or mutant, evidence artifact, do-not-claim boundary, and owning bead+gate. Sample the plan's and the beads' claims; find every place a lower evidence class silently justifies a stronger claim. The measured session precedent: a green suite that a mutation could not turn red.
8. **R8 context is a budgeted resource**: measure against the four targets — router `AGENTS.md` ≤ 12,000 chars, bead packet ≤ 32,000, one contract excerpt ≤ 8,000, total initial worker context ≤ 50,000 — plus the 50/70/85% checkpoint discipline. This repo's `AGENTS.md` and the 6,449-byte dispatch packets are the subjects; report the real numbers and whether compaction recovery needs the whole plan.
9. **R9 graph validity is architecture validity**: the ten pre-build conditions — no cycles, no orphan build-critical beads, every cross-cutting contract owned, every bead has acceptance and tests, every gate has evidence requirements, dependency direction consistent, ready frontier is foundation/probe/vertical-slice work, priority not substituting for dependencies, fan-in/fan-out hotspots reviewed, milestone gates not giant implementation beads. Measure on the live graph: 345 ready, 526 open, 102 in_progress.
10. **R10 closure is reversible**: the seven reopen triggers — contract hash change, upstream decision invalidating assumptions, missing/stale/weaker-than-claimed evidence, absent or no-longer-killing negative test, failed fresh-agent simulation, current reality diverging from the status surface, release gate finding unowned residual scope. Determine which closed beads are reopen-eligible RIGHT NOW and whether any mechanism watches for these triggers at all.

## Completed Passes

### Pass 1 — R1 breadth-before-depth + truth surfaces — 2026-09-05T04:1xZ
- Files changed: `docs/planning/R1-MATURITY-MATRIX.md` (new), `.beads/issues.jsonl` (9 beads), this file
- **R1 RE-DERIVED, NOT INHERITED. VIOLATED, delta unchanged.** Rubric stated before scoring (six levels, with the discriminator per level); all 25 subjects scored with a `file:line` per score — 12 boxes and 13 numbered sections (13, not 12: `ls docs/plan/[0-9][0-9]-*.md | wc -l` → 13, the same off-by-count `boxes/S2.toml:131-133` already corrected once). Boxes: S1=4, S2=3, ten at 2 → **max=4 median=2 delta=2 VIOLATED**. Sections: twelve at 3, `04-diagrams`=2 → max=3 median=3 delta=0. Combined 25: max=4 median=3 delta=1. **R1 names no population, so the verdict is choosable** — primary verdict is the population `HD-0014`/`CONTRACT.md:143-144` actually used (the boxes), which reproduces 4/2/2 exactly.
- MOVEMENTS SINCE 2026-09-03, both re-measured: S2 rose 2→3 (an *observed* refusal with its command at `S2.toml:24` and `:30-33`, not a known-bad named inside a `resolves =` gap — that distinction is the L3 discriminator and it is what holds ten boxes at 2). `S8.toml:12` is repaired and now agrees with `DESIGN_INDEX.md:129` (rollback PROJECTED, not demonstrated) — the precedent conflation named in the mission is genuinely closed.
- **EXCEPTIONS_FOUND=0, EXCEPTIONS_WELLFORMED=0.** `grep -rl CRITICAL_PATH_EXCEPTION` → 3 files, every hit a NEGATIVE (`CONTRACT.md:147`, the `HD-0014` body, this file); positive control `grep -rl "breadth before depth" docs/` hits `CONTRACT.md`. Best candidate `HD-0014` scores **3/5**: reason ✓, affected ✓, reviewer ✓ (`decider":"Joshua"`); expiry ✗ (`review_after=""`, no `condition` key, though `HD-0001..0008` all carry one); risks-introduced ✗ (`HD-0003` has `residual_risk_stated_at_decision_time`; `HD-0014` does not). `CONTRACT.md:147-152` disposes of the ORDER, not the DELTA.
- **NO ORACLE FOR R1 EXISTS ON THIS HOST.** atlas-arc ships no `scripts/arc.py`; `planning-arc/scripts/validate_plan.py` has 0 `maturity` occurrences (positive control 19 `def `); `grep -rl maturity crates/ --include=*.rs` → 0 (positive control `fn main` → 94). `BUILD_READY` stays UNRUN. This is why the delta sat at 2 for ~48h with no surface reporting it.
- **THE REMEDIATION LANDED AND THE DELTA DID NOT MOVE.** `docs/plan/flow/maturity0/S1..S9.toml` — 12 tracked files, 512 lines, commit `c47aec8` at 21:25Z, **9 minutes after `HD-0014`** — 12/12 carry all eight required fields, 157 unknowns each typed into exactly one R3 disposition. So `CONTRACT.md:148-150` is DISCHARGED and still reads as in force. A maturity-0 record is level 0 by construction, so the gate closed without moving the arithmetic. The seven relocations that WOULD move it (`S1.toml:28-33, :102-117, :258-269, :276-280, :293-296, :321-324` → S7/S8) live only in a bead comment on `i0uh`, which is still `in_progress` at `updated_at 2026-09-03T21:30:13Z`; `relocat` across all 791 rows → 2 hits, both proposing beads. Churn since `HD-0014`: `maturity0` +512/-0, `boxes` +1/-1, sections 0 commits, `docs/planning` +1253/-156.
- **TRUTH SURFACES: 0/6 by name, 4/6 by function, `GOVERNING_SURFACES_VISIBLE=1/4`.** `.atlas-arc/` deliberately NOT created (canonical mapping change under the freeze). No carrier at all for `manifest.json` (nothing holds a maturity vector). Registries: only 4 of the 13 named kinds have a carrier (decisions, unknowns, beads, defects); every filesystem hit for claims/gates/risks/receipts/requirements/invariants is inside `./.rch-tmp/` vendor caches, and `boxes/S2.toml:97` independently names `registries/claims.toml` as a GAP. `docs/plan/CURRENT-REALITY.md:14-21` is the ONE artifact making the four-surface distinction explicit — keep that table.
- **8 CONFLATIONS, all cited by line:** (1) `CURRENT-REALITY.md` present-tense at a pin 25 commits old, 5 of 7 execution figures drifted (761→791 rows, 104→121 closed; positive control `grading=11`/`tombstone=22` match exactly), 0 in-tree generator vs 3 files for `decisions.jsonl`; (2) `S1.toml:8,:249,:283-285,:339` assert `HD-0009..0012` undecided while `decisions.jsonl:14-17` carry Joshua's decisions — S1's stated approval blocker is discharged; (3) `S1.toml:337` `refutations = 3` beside `:338` "(b) refutations = 0"; (4) `S2.toml:107-110` "no per-box diagram" refuted by `:124-126` `validate_exit = 0`, propagated into `maturity0/S2.toml:26` as a live unknown (bounded: 1 of 12 each); (5) `ROUND_LOG.md:13` ROUND_5_REQUIRED vs `DESIGN_INDEX.md:3` vs `decisions.jsonl:26` vs on-disk `round{4,6a,6b,6c,6d}`; (6) `DESIGN_INDEX` harvests HD-0001..0017 while the ledger holds 18 — and its last commit `92acadd` is **11 minutes after** the `HD-0018` row existed; (7) `CONTRACT.md` carries three surfaces and three superseding amendments with nothing marking which is live; (8) `00-brief.md` carries Charter + current reality + plan index, 0 stable IDs, and its `R1`/`R13` labels collide with atlas-arc's rule numbers.
- Also measured: **`non-goal` appears 0 times in all 13 sections** (two engines agree) — a Charter requirement (`SKILL.md:50`) and an R5 obligation present only in the derived records. **Bead references: 6 occurrences across 4 of 13 sections, 9 sections at zero** — the single discriminator holding every section at ≤3.
- Beads: `r1-no-checker-d81g` P0 · `r1-relocations-no-integrator-1xx9` P0 · `r1-exception-missing-so9x` P1 · `current-reality-stale-pin-sb8p` P0 · `round-log-four-rounds-stale-atmo` P1 · `design-index-hd-harvest-stale-f02j` P2 · `s1-box-asserts-resolved-blocker-cz67` P1 · `s2-gap-refuted-by-own-file-6knl` P2 · `charter-surface-not-machine-visible-qwyr` P1. Each carries WHAT/WHY/ACCEPTANCE plus a named falsifier with a known-good and an anti-vacuity leg.
- Cited as tracked, not re-filed: `arc-maturity0-intake-i0uh` (delivered+verified, commented), `arc-current-reality-2d1q` (commented, plus a correction to my own count), `cbp4` (commented — its evidence path `unknowns/DEFECTS.jsonl` does not exist; the rows are real at `DEFECTS.toml:16` and `:28`), `arc-s4-apply-6c-dispositions-jy4o`, `arc-typed-unknowns-02ai`, `coverage-durable-join-8x85`, `denominator-invisible-growth-7t33`.
- Instrument errors made and corrected, in the artifact: (a) a mis-columned census reported `non-goal=2` for `00-brief.md`; two engines then agreed on 0, and every published count was re-run with both; (b) a churn probe scoped to `docs/plan/flow/boxes/` concluded the remediation produced nothing — it had, in the sibling `maturity0/` the scoped probe could not see, which is the `xv30` wrong-input class; (c) **`beadrefs=273` at `CONTRACT.md:143` and inside the `HD-0014` body is unreproducible** — `S1.toml` returns 4 (3 unique) / 898 / 58 under three instruments and no command is recorded, against `CONTRACT.md:215-216`.
- **A LATER MEASUREMENT THAN THE GRADER'S, AND IT INDICTS MY OWN INSTRUMENT.** The grader read 6 files where I read 3; re-run after this pass's artifact landed and `grep -rl CRITICAL_PATH_EXCEPTION` returns **10** — 4 `.beads/.br_history/` snapshot generations, `beads.db`, `issues.jsonl`, this file, `docs/decisions.jsonl`, `CONTRACT.md`, and `docs/planning/R1-MATURITY-MATRIX.md`. **Seven of the ten are this pass reporting the absence**, because `so9x`'s description quotes the token; not one of the seven is an exception record. So the census contaminated the instrument that produced it. The honest instrument for any re-run is `docs/plan/**` + `docs/decisions.jsonl` only, excluding `.beads/`, this file, and `docs/planning/`. `EXCEPTIONS_FOUND` counts **records**, never matches. (The grader's `.br_history` note is the first half of this; the observer effect is the second.)
- Commits: `4a4d45b` (artifact + 9 beads) and `36a1a65` (this file), both read back with `git log` and both independently verified by the grader with `git cat-file -e`. The post-grading amendments to this entry and to the artifact land in a third commit on these same two paths; its sha is the newest `docs(planning)` entry in `git log` and is deliberately **not** typed here, because I caught myself about to write a sha I had not yet observed — the exact fault this pass filed against `CONTRACT.md:143`.
- Verdict: PRODUCTIVE
- **R6 DISCHARGED — graded by pane 1 (non-author) 2026-09-05T02:50Z.** All three named
  re-runs re-executed, plus the one the pass flagged for a second pair of eyes.
  - (a) `CRITICAL_PATH_EXCEPTION` — CONFIRMED and stronger than claimed: `docs/plan/flow/CONTRACT.md:147`
    states it outright, *"**No `CRITICAL_PATH_EXCEPTION` is recorded.** One was recommended; the
    ruling makes it unnecessary."* So `EXCEPTIONS_FOUND=0` is a positive statement in the contract,
    not an absence inferred from a grep. Population differed — 6 files, not 3, because 4 are
    `.beads/.br_history/` snapshots of `issues.jsonl` written minutes earlier; the pass's 3 is right
    for the non-snapshot population.
  - (b) bead counts — the STABLE quantities match exactly: `in_progress=101 grading=11 blocked=3
    tombstone=22`. The moving ones drifted (`total 791→802, closed 121→122, open 533→543`), every
    unit of it live work in the intervening ten minutes. The pass's chosen positive control
    (`grading`/`tombstone`) is what makes the drift readable as movement rather than error.
  - (c) `git rev-list --count 05cdd2a..HEAD` → **28**, not 25; the three extra are the pass's own
    `4a4d45b`/`36a1a65` plus `%7`'s `120947d`. Both claimed shas verified with `git cat-file -e`.
    `4a4d45b` = 2 files / 323 insertions (`R1-MATURITY-MATRIX.md` 264 lines + 68 bead lines); 9 beads
    carry the pass's actor.
  - **`beadrefs=273` INDEPENDENTLY CONFIRMED UNREPRODUCIBLE.** `CONTRACT.md:143` reads
    `S1 meas=6 gap=10 contracts=1 diagram=2 beadrefs=273 MATURITY=4`. Measured on the file it
    describes: `docs/plan/flow/boxes/S1.toml` is **449 lines total**; `omp-orchestrator-` occurs **4**
    times (4 unique), `bead` 15 times (4 unique), `beadref` **0** times. No instrument over that file
    yields 273, and 273 exceeds half the file's line count. That figure sets S1 to `MATURITY=4`, which
    is what makes `R1_DELTA=2`, which is what `HD-0014` ruled on — **a load-bearing number in the
    governing contract has no producing command.** Same class as the retired "81 JSON-RPC methods,
    17 used" pair, sitting in the document that enforces the freeze.
  - **Cross-lane convergence the pass could not see:** pane `%7`, working
    `s1-coverage-breaks-advisory-ratchet-xl9d` with no contact, added the allowance row
    *"advisory-unreachable: S1 depth is suspended by **Atlas Arc R1** and the HD-0012 hook decision
    pending Joshua approval; no production caller is honest while…"* — reaching the same conclusion
    from the crate side and citing R1 by name. `census_membership` went `10p/2f` → `13p/0f`.
    Two lanes agreeing on "S1 depth is suspended" outweighs either measurement alone.
  - **Highest-leverage finding, accepted:** `r1-relocations-no-integrator-1xx9` — the seven S1→S7/S8
    relocations that would actually move the delta exist only in a bead comment on `i0uh`
    (`in_progress` since 2026-09-03T21:30Z), with no bead owning integration. Structurally identical
    to `arc-s4-apply-6c-dispositions-jy4o` sitting unassigned for want of a non-author integrator:
    **two independent instances of "the analysis landed and the application has no owner."**
  - Grader's own correction, recorded so no later pass inherits it: pane 1 dispatched a CI lane citing
    "67 of 67 runs failed" as evidence about the present tree. Measured after: **381 unpushed
    commits**, last CI run `4b398e4` at 2026-09-02T03:01:50Z against HEAD at 2026-09-04 — CI has never
    seen 381 commits. True about a three-day-stale tree, used as a claim about now: the cargo-vs-commit
    wrong-input class of `AGENTS.md` rule 8. **Any pass citing CI state must pin the tree first.**

### Pass 2 — R2 stable IDs before prose growth — 2026-09-05T05:0xZ
- Files changed: `docs/planning/R2-ID-CENSUS.md` (new), `docs/planning/CENSUS-EXCLUSIONS.md` (rule 4 amended in place), `.beads/issues.jsonl` (5 beads, 4 comments), this file
- **THE SIXTEEN-KIND CENSUS. Literal-prefix instances: 5 of 16** (`REQ` 13 ids/22 occ, `INV` 13/48, `RISK` 6/10, `GATE` 24/128, `DEF` 14 strings/21 objects). **With this repo's analogues admitted: 14 of 16** — only `RB-*` and `REC-*` are truly absent (`\bRB-\d` and `\bREC-\d` return zero across `docs/**`, `crates/**`, `.beads/issues.jsonl`). **Canonical owner resolvable: 11 of 16.** The three owner-less kinds are the state R2 calls worse than absence: `REQ-001..013`, `INV-001..012`, `RISK-001..006` and every `DEF-*`. Corpus named explicitly (`PLAN.md:1-199` head + the 13 sections, never PLAN.md's body summed with sections); CENSUS-EXCLUSIONS rules 1-5 applied and stated.
- **ANALOGUES ARE WHERE THE CONTENT LIVES, and this is the headline.** `docs/contracts/` carries **238 distinct law ids in 27 families** (`LAW` 45, `DJ` 21, `CAN` 16, `KOP` 16, `SPC` 14, `ADM` 13, `GT` 12, `OC` 12, `OBS` 12, `DCL` 10, `INV-L0-*` 9, `AS` 8, `RR` 7, `VC` 7, …), each family resolving to one owning file. **0 of 238 appear in `DESIGN_INDEX.md`. 0 in `docs/PLAN.md`. 0 in the 13 sections.** 41 appear in `docs/plan/flow/**`, 37 in the tracker, and **10 in `crates/**`** (only `PO-L1..L5` and `RR-L1..L5`). Meanwhile `DESIGN_INDEX.md:69-70` declares all 32 contract files normative and `:275` publishes `IDS_AFTER=51` as if 51 were the population. Judged on the register alone five R2 kinds read ABSENT; judged honestly they exist and are unregistered. → `contract-law-ids-unregistered-a6b7`.
- **EARNED / DECLARED / INDEX-ONLY RE-DERIVED, ALL THREE REPRODUCE EXACTLY**: `DESIGN_INDEX.md` **51**, `docs/PLAN.md` **12** (byte-for-byte `HD-0001`, `HD-9999`, `INV-2026`, `WP-001..009`), **11 live** after rule 2 excludes `HD-9999`, set-difference **40**, PLAN-only exactly `[HD-9999]`. Round 6C **ADOPT 24 / DEMOTE 16 / DELETE 0** reproduces at `round6c/DEFECTS.toml:24-26`, the 24 are `GATE-001..024` at `:40-66`, and `:32` still records `APPLIED=none`. `56cdd28` verified with `git cat-file -e` (+158 lines, `wired_lanes.rs`). Running round 4 `DEF-001`'s own `verification_method` verbatim: **51 of 51 earned, 0 index-only** — round 4's cut worked. But that only asks "does the string appear". **Classified by consumer: `GATE-001..024` CODE 24 / SECTION 0; `HD-0001..0017` CODE 8 / SECTION 1; `WP-001..009` CODE 0 / SECTION 0; `INV-2026` CODE 0 / SECTION 1.** Of the sixteen kinds, **exactly two are referenced by a consumer that executes them** — `GATE-*` via `wired_lanes.rs` and `B-*` via the tracker. Twelve are read only by documents. The 269 typed unknown ids in `DISPOSITIONS.toml` have **zero** consumers outside that file.
- **UNREPRODUCIBLE FIGURES — the instrument was validated by what it CONFIRMED before it refuted anything.** Six figure families at `CONTRACT.md:143-144` reproduce: S1 `meas=6` ✓ `gap=10` ✓ `contracts=1` ✓; S2..S9 `meas=5-8` ✓ exact, `gap=9-19` ✓ exact, `contracts=0` ✓. `diagram=2` reproduces under a named instrument (`[box.diagram]` at `S1.toml:341-344` names two `.mmd`; every other box names one). `GROWTH_PER_COMMIT=19.22` reproduces — `S1-COVERAGE.md:551` carries `(211-38)/9`, cited as the negative control that the corpus *can* show its work. **TWO DO NOT: `beadrefs=273`** (449-line file, `omp-orchestrator-` → 4 occ/4 unique, token `beadref` → **0**; third independent confirmation, tracked `so9x`) **and NEW: `beadrefs=0-3` for S2..S9** — measured range is 1-6 occurrences / 1-3 unique and **no box returns 0**, so the published range's minimum is unreachable by any instrument.
- **THE MECHANISM EXISTS, RUNS, IS IN CI, AND IS RED — and every freeze-governing figure bypassed it.** `NUMBERS.toml` is a 34-row load-bearing-figure registry, **34/34 carrying a runnable `command`**, enforced by `crates/no-shell-gate/tests/numbers.rs`, wired at `.github/workflows/gate.yml:146`. EXECUTED (`RCH_CARGO_WRAPPER_BYPASS=1 cargo test --offline -p no-shell-gate --test numbers`, 7.6s): **8 tests, 7 pass, `no_declared_figure_has_drifted` FAILED — 3 of 34 drifted** (`workspace_crates_on_disk` 65→72, `ipg6_root_symbols` 622→611, `spawning_crates` 47→51). Substring test over the registry for `beadref` / `273` / `MATURITY` / `GROWTH_PER_COMMIT` / `DECLARED_AT_HEAD` / `IDS_AFTER` / `open_disagreements` → **False for all seven**. `NUMBERS.toml:19-21` states the broken rule verbatim: *"A number without a runnable command is not measured, it is remembered."* This is not Pass 1's "no checker exists" — the checker exists and fires; the number that stopped all building was written outside it. → `numbers-registry-misses-freeze-figures-o2f7` P0.
- **DUPLICATE IDS = 15 strings minted twice for different objects. `id = "DEF-001"` is DEFINED SEVEN TIMES** as seven different defects with seven distinct severities (P0/P1/P1/P1/P2/P2/P3) and seven distinct classes: `flow/unknowns/DEFECTS.toml:4`, `round3:35`, `round4:61`, `round6a:40`, `round6b:57`, `round6c:97`, `round6d:45`. `DEF-002` five times. **And the ambiguity is already consumed:** `DISPOSITIONS.toml:3481,3493,3505,3517,3529,3541` make six live references to a bare `DEF-002` with five candidate owners, plus one bare `DEF-004` in `docs/decisions.jsonl`. `DEF-*` is R2's own review-defect kind and R4's only legal review output — the one family that cannot afford ambiguity. → `def-id-namespace-collision-vjfo` P0.
- **RESTATEMENTS POINTING AT A NON-OWNER = 61.** 54 dangling `affected_ids` (`REQ-001..013` 22 occ, `INV-001..012` 22, `RISK-001..006` 10 — **all of them, with zero exceptions, inside `round3/DEFECTS.toml` and `round4/DEFECTS.toml`**; 13+12+6 = 31, exactly the count `DESIGN_INDEX.md:275` records cutting), plus the 6 `DEF-002` and 1 `DEF-004` references. The cut was right and was executed on the register only; the references outlived their referents, so `round3:38`'s P1 `NORMATIVE_CORPUS_OMISSION` now names an unaddressable affected set. → `cut-aliases-left-dangling-references-m3v6` P1.
- **`GATE-*`'s canonical owner is a file that disclaims ownership.** The 24 ids' only definition site is `DESIGN_INDEX.md:162-185`, which describes itself at `:9-12` as "a deterministic alias … does not add product scope", while `crates/no-shell-gate/tests/wired_lanes.rs:63-87` now depends on those ids and the aliased sections carry the string **0 times**. GOOD NEWS, measured with a MISS branch present: **24/24 line-pins still resolve, 0 misses** — the register is correct today and unpinnable, which is exactly what the 24 ADOPT labels would fix by moving the binding from a line number to a string. Measured and commented on `jy4o`, not re-filed.
- **CORRECTION TO A BINDING RULE, amended in place.** `CENSUS-EXCLUSIONS.md:14` (rule 4) is binding on this census and two of its claims do not reproduce. `docs/PLAN.md` is **not purely assembled**: 125 of 6,259 nonblank lines appear in no section, 103 below `:200`, and `PLAN.md:6-8` says so verbatim. Cost: `WP-001..009` and the **only** constitution DAG live at `PLAN.md:87-95` and occur in **zero** section files, so obeying rule 4 by picking the sections reports the `WS-*` kind ABSENT — **my own table read `WS = ABSENT` before this was caught.** Also rule 4's `8,209 vs 10,019` pair compares PLAN.md against a **19-file** glob including six non-sections; the like-for-like pair is **8,209 vs 7,969**. → `census-rule4-hides-wp-register-1px7` P1, and rule 4 rewritten.
- Beads filed: `numbers-registry-misses-freeze-figures-o2f7` P0 · `def-id-namespace-collision-vjfo` P0 · `contract-law-ids-unregistered-a6b7` P1 · `cut-aliases-left-dangling-references-m3v6` P1 · `census-rule4-hides-wp-register-1px7` P1. Each carries WHAT/WHY/ACCEPTANCE, a named falsifier with an anti-vacuity leg, and line-cited evidence.
- Cited as tracked, not re-filed, with a measurement comment on each: `cbp4` (dual-row `HD-*`; **its acceptance-2 command now returns 2 for all six ids, and `HD-0015`/`HD-0018` are two NEW pairs that postdate the filing — 8 duplicate pairs across 26 rows / 18 unique ids**), `so9x` (`beadrefs=273`, third confirmation + the new `0-3` range refutation), `y0dy` (51/12/40 all three reproduce exactly; one layer down is the 238 law ids), `jy4o` (`APPLIED=none` measured with the command). Also cited without comment: `f02j` (`DEC=17`/`IDS_AFTER=51` off by one — still true, `HD-0018` present), `5dia` (rule 4's owner), `cfg5` (the `NUMBERS.toml` drift class), `kldh` (`numbers.rs` host-coupling), `68za`/`033k` (the round-5 cut).
- **Instrument error made and corrected, recorded so no later pass inherits it:** I reported "0 `.github/**` references to `NUMBERS.toml`" and read it as "not in CI". Wrong input — `gate.yml:146` runs `cargo test -p no-shell-gate`, which *includes* `tests/numbers.rs` without naming the file. The gate **is** wired. The `xv30` class, caught by re-reading the workflow instead of trusting a filename grep.
- Handoff, out of R2 scope, not filed: `workspace_crates_on_disk` 65→72 and `git log --diff-filter=A` per crate dates **seven crates to after the BUILD FREEZE** (`lifecycle-event` 09-03T20:23Z, `lifecycle-monitor` 20:47Z, `installer` 21:07Z, `s1-coverage` 21:08Z, `omp-types` 09-04T16:50Z, `bead-availability` 09-04T19:43Z, `ntm-fleet-monitor` 09-05T02:49Z). The freeze's exit condition is unmet: `agreement.status` over the 12 boxes is `draft` ×9 and **absent entirely** on `S6a`/`S6b`/`S6c`. Pane 1 owns the disposition — it is a freeze question, not an R2 question.
- Commit: see `git log --oneline -1` on `docs/planning/**` + this file
- Verdict: PRODUCTIVE (self-assessed scope only; **R6 forbids self-grading — see the R6 OWED line in the pass report**)
