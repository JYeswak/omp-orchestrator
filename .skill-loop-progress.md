# Skill Loop Progress
# Skill: process-audit composite — /rigor-atlas + /brennerbot-with-ntm + /vibing-with-ntm + /choose-the-best-skills-for-me-to-run-in-this-project
# Target: the LIVE planning / dispatching / management process of this repo (the 2026-09-02 session as subject), graded against docs/plan/00..12 and AGENTS.md
# Total Passes: 10
# Started: 2026-09-02T21:40:00Z

## Status: IN PROGRESS — Pass 7 of 10
## Frame (Josh, 2026-09-02): "make sure that we are measuring this against more than just dispatch or not"
The measurement frame is §11.2's matrix — S1..S9 stages × {template, dispatch, reap, logging, build grading} = 45 cells, 3 visible `Y` at plan time — plus §12's seven-field dispatch contract, §05's A1–A11 negative patterns, §06's four legs × six properties, §09's M1–M7 + PV1–PV8, and R24's corpus lens (F1 no perf requirement, F2 no fuzz, F3 harnesses discussed ~80× built 0).

## Output contract for EVERY pass (Rule Zero)
A pass produces PRODUCT: beads filed via `br create` (WHAT/WHY/ACCEPTANCE, `--actor <PassName>`), each with line-numbered evidence and a falsifier — never a new document. Findings that are already tracked are cited by bead id, not re-filed. A pass that finds nothing must name 3+ specific checks it ran and what they returned. Subagents are read-only toward source and the running fleet: no dispatch, no pane sends, no edits to crates/, no `cargo test --workspace` (red from a peer's uncommitted `fleet-truth` edit; scope to `-p`).

## Missions
1. Matrix re-measured live (§11.2 / brennerbot): every one of the 45 S×property cells is a hypothesis with a falsifier; grade what TODAY's session exercised, cell by cell, and file a bead per cell that failed live.
2. Dispatch contract vs actual packets (§12 seven fields / vibing-with-ntm dispatch): audit the packets actually sent today (/tmp/d-*.md, ack-*.md, p*.md, and the loop's own) against Trigger/Packet/Amazing/Adequate/Negative/Skills/Done-signal; file the missing-field classes.
3. Honest-credit audit (vibing-with-ntm Second Rule): sample the 97 closes and 43 delivered-awaiting-grade; how many closed on re-executed evidence vs self-report; commits-as-KPI, refusals-as-delivery, process artifacts as progress. File the credit debts.
4. Liveness truth stack (vibing-with-ntm One Rule): pane truth / robot state / work state / artifact evidence — enumerate every instrument error today (≥17 known) as a taxonomy; which are structural (need a gate) vs habit; file the structural ones.
5. Rigor-atlas prescriptions (process-kernel / control-kernel / scheduler-kernel / monitoring): name the falsifiable invariant per stage; map today's defects to prescriptions (tri-state-indeterminate-never-allow, typed-no-data-metric-taxonomy, incarnation-lease-fencing, serialize-the-check-then-act-boundary, no-number-without-a-falsifier, receipts-as-typed-objects); file each absent prescription with the mirror exemplar path:line.
6. Gates re-measured (§06 four legs × six properties): plan said 2/8 all-four, 0/8 all-six; re-measure NOW after kwo9/leht/iis6; which gates fired today and which stayed vacuously green; file the delta.
7. Measurement stack vs the corpus (R24 F1–F3, §02): no performance requirement, no fuzz, harnesses built 0, 4.46% direct-probe coverage; state what must hold true across the surfaces and how it is measured; file the missing requirement classes with corpus citations.
8. The unanimous structural gaps, live (§12 duel): S9 human-decision ledger, fleet/namespace identity, leases with expiry — today's identity/liveness/reservation failures ARE these gaps firing; measure how many human decisions today reached docs/decisions.jsonl, how many identity ambiguities, how many lease expiries bit; file what is not already tracked.
9. Milestones and plan-validation (§09 M1–M7, PV1–PV8): did today's autonomous dispatch constitute M3/M4/M5 progress by OBSERVABLE, not claim; independent readback per bead; zero-human closes; grade each milestone against evidence and file the gap.
10. Skill juncture (choose-the-best-skills One Rule + §12 Appendix A): at THIS juncture — autonomous dispatch just resumed, 43-grade backlog, identity crisis — which ≤3 skill campaigns, evidence-backed; which skills should have run today and did not; file the process gaps a skill would have caught.

## Completed Passes

### Pass 1 — Matrix re-measured live — 2026-09-02T22:05Z
- Files changed: .beads/issues.jsonl (4 beads), this file
- Changes: 45-cell matrix re-measured for today with a command per cell. Visible Y went 3 → 7 (S4 dispatch, S4 logging, S5 template, S5 dispatch, S5 logging-heartbeat, S6 logging, S6 build-grading). REFUTED the plan: `bv --robot-triage` IS invoked (refill-idle-panes main.rs:227, cron 8,28,48, ledger 15:29). Three of the seven Y carry a refuter that a second/broken/hand-only mechanism sits beside them.
- Beads: `ve4w` P0 two live dispatch authorities, no shared claim fence — cron refill sent oj6.3 into control-plane %1389 (Target=control-plane) while the supervisor sent it to %1414; 82× fc-22l to pane 1. `8zx1` P0 S6 grading has no dispatcher/packet/consumer — 4 grading + 43 delivered wait on 2 hand packets/day. `yfp2` P1 supervisor's Agent Mail identity is ambient launchd AGENT_NAME=WildStone (a ~/Developer/fsw agent) — 21/23 dispatch-result mails refused. `nsx1` P1 S9 has a reader and no writer — 0 decision rows today vs 188 owner=josh refusals and an SLB approval that blocked the reaper install all afternoon.
- Cited as tracked: gfm6, t784 (partially stale — `grading` status now exists), eg0m, iis6, 2kx2, 93lo, vh1g, j2z9, t00+8nuh, ipg.19, 6hrv, p3f4, kxe.4, 42na.
- Handoffs: M2 ← 0/16 hand packets and 0/1 supervisor packet carry the ntm template's required fields. M3 ← t00 worker comment 19:37Z claims the plist no longer contains OMP_REAP_SWEEP; live plist (mtime 14:32) contains it — self-report contradicted by installed state; 13/37 closes josh-only-authored. M6 ← census flipped REACHABLE/UNWIRED between ticks once. M8 ← nsx1 + %1414 dual identity + yfp2.
- Commit: ae6bbd8
- Verdict: PRODUCTIVE

### Pass 2 — Dispatch contract vs actual packets — 2026-09-02T22:30Z
- Files changed: .beads/issues.jsonl (2 beads), this file
- Changes: 26 hand packets + 8 supervisor packets audited × the seven fields + AUTH/ACK/STOP. Population: Trigger hand 21/26 supervisor 0/8; Done-signal-with-exit 0/34; Stop 0/34; template use 0/34 (positive control on the template → 4); Skills 2/26; Adequate 8/26. Supervisor packets carry the bead body VERBATIM (8/8, only members satisfying §12:318) but drop the typed `acceptance_criteria` field (BrShowRow has no such field; bead 815 carries 41 acceptance lines ONLY there). Hand packets restate the bead from memory 26/26; one (d-8e1g) added acceptance items the bead lacks. 32/32 ACKs carry a sender-dictated pane id; 1/32 is receiver-derived.
- NEW FAILURE FOUND: the orchestrator's 15:21 `$TMUX_PANE` fix (p2/p3/p4.md) dropped the parser's ` -- ` separator → 5 ACKs unparseable, incl. live supervisor dispatch ipg.19 (ACKed 33s after send, still RETRY_BLOCKED at +7min). The fix for dictation introduced a parse defect. Also: ipg.4 closed DONE with 0 ACK and 7 RETRY_BLOCKED — a close the loop believes was never delivered (→ M3).
- Beads: `a92y` P0 send_and_verify renders 4/16 template fields and drops typed acceptance; acceptance 4 makes hand dispatch call the renderer so both paths emit ONE packet. `elq6` P0 ACK instruction hand-typed in two media and drifted from the parser twice; falsifier: any post-fix packet whose ACK contains a literal %N, a sender-chosen --actor, or a prefix the parser does not match.
- Cited: 93lo (supervisor still emits no ACK line, 8/8 on three builds), 8zx1, ve4w (hand packets are a THIRD dispatch path: 26 today vs 8 loop), yfp2, t784, gfm6, mad1/nfgc (FROM line), 3kpc, iis6, 2kx2.
- Verified correct: supervisor body verbatim 8/8 (no acceptance substance lost today despite the dropped field); FROM 25/26; claim+readback stated in every post-12:37 hand packet; no size cap tripped ≤6017B; grader assignment recorded on beads; all 7 dictated pane ids were the CORRECT pane (defect is evidentiary, not a mis-send).
- Commit: 76a0d17
- Verdict: PRODUCTIVE

### Pass 3 — Honest-credit audit — 2026-09-02T23:05Z
- Files changed: .beads/issues.jsonl (3 beads), docs/decisions.jsonl (HD-0008), this file
- Changes: population 40 today-closes (pass 1's 37 predated three closes), 105 all-time; mcq2's 18 non-conforming reproduced exactly. All 40 graded on a/b/c/d with re-runs (`cargo test --offline -p …`, 12 suites re-executed). COUNTERFEIT CLASSES: implementer self-close 9 (8 of the 9 closes since autonomous dispatch resumed at 18:18Z); grader unattributable 23/40 (comments authored `josh` only); ledger-says-undelivered-bead-says-done 5 (232, kxe.5, ipg.4, ipg.18, j5r — each with DISPATCH_FAILED + AWAIT_HUMAN + 5–8 RETRY_BLOCKED + mail refused); refusal-as-delivery 1 (ipg.18: wired_lanes 19/5 RED at HEAD by the close's own admission); commit-count-as-KPI 0 (positive control). Delivered-awaiting-grade: 50 citing a real sha (pass 1: 43); 22 claim done with no "not graded" disclaimer; 2 (1p0u, ewnr) carry a PASS grade and were never closed — the grader graded and did not close.
- Beads: `qt9r` P0 implementer self-close class — mechanism: refuse close when actor==assignee or last-DONE author; record closed_by. `gcyf` P1 unattributed grader class — --actor required from panes, GRADER_PROSE_MISMATCH refusal, backfill. `vggs` P1 close↔dispatch reconciliation — CLOSE_RECONCILED/UNRECONCILED rows, AWAIT_HUMAN retirement.
- REOPEN/REGRADE: mi1o (WONTFIX by its own assignee, zero comments/commands/shas) → reopen. ipg.18 → regrade: a permanently-RED cargo target cited as satisfying acceptance 1. 232/hsv/iad/i0kp/ipg.4/kxe.5/j5r → assign graders, not reopen — every re-runnable claim matched.
- Verified sound end-to-end (a,b,c,d): leht (census_membership 11/0), mj8w (installed build_id ancestor chain), oe2 (refill 35+6/0; oracle_routing 7/2 where both fails are post-close tree changes), h27o (pane-dispatch-ready 51/0), 193/hsv (findings_ledger 17/0).
- POST-CLOSE DRIFT, not false closes: 4wmo's installed pre-commit hook sha (11:45) ≠ pre-commit-gate.rs (13:31); `hook_freshness` panics STALE live. 58u2 measured 61 packages, now 65.
- Instrument notes: `br list -a --json` default page (242) hides closed_at/close_reason — grade from .beads/issues.jsonl; two suites went RED once / GREEN on rerun under parallel cargo load on the dirty tree — single-run reds are unproven; `~/.cargo/bin/cargo` is now rch-hooked.
- Human decision surfaced: HD-0008 (183 unpushed commits, public remote) → Josh: push. Recorded in docs/decisions.jsonl — first S9 row today.
- Commit: 3b34887
- Verdict: PRODUCTIVE

### Pass 4 — Liveness truth stack (One Rule) — 2026-09-02T23:50Z
- Files changed: .beads/issues.jsonl (3 beads), this file
- Changes: 100 distinct instrument-error events on 2026-09-02 (71 bead comments matched; 22 numbered instances; 5 panes' scrollback; 12 AGENTS.md commits), ≥40 the orchestrator's own. 16 classes. STRUCTURAL with ≥2 instances and no existing mechanism: C1 grep-matches-prose (16) → `xrtc`; C2 missing-representation (11) → `r73l`; C3 wrong-input verdicts (16: cargo-vs-commit, hook-vs-empty-index, wrong process) → `xv30`. Already-filed mechanisms cited for: roster-returns-empty (7 → 2kx2), dictated-echo (4 → elq6/gfm6), single-capture (4 → iis6/riqd/2tny), stale-figure (6 → opsa/d1n), any-over-disjunction (5 → 8jli), loud-in-principle-silent-in-fact (5 → nsx1/4wmo). HABIT: truncated instrument (4), keyword-grep-over-help (1+2 caught by readback), $?-after-pipe (3), timezone (6 local-as-Z + a 26-comment drift series), diagnosis-without-definition (5 → 3r1r/xv30).
- Two numbering sequences for one class ("seventh" by AmberGate at 21:44Z vs "eighth"/"ninth" by the orchestrator) exist only in scrollback — gjyi's failure mode, measured. Orchestrator's `2>/dev/null` idiom: 70× in 51 command blocks, the enabling condition for reading empty as permission.
- Positive controls (all four truth sources agreed before action): h71g dispatch; the identity-cleanup veto (%1367/%1389 live in control-plane); eg0m after 2kx2 (roster empty vs authoritative snapshot 2 locks); 8nuh refutation (binary-is-not-the-variable, 9 vs 10 env vars); 6fot FAIL (strings over the Mach-O hook, positive control 8/2/1); 1p0u PASS (tree pinned first). Pins fired correctly 3/3 — the pin family is sound; the one text-keyed pin (C1 "twelve") is the exception.
- Commit: b286ee5
- Verdict: PRODUCTIVE

### Pass 5 — Rigor-atlas prescriptions — 2026-09-03T00:05Z
- Files changed: crates/loop-switch/src/lib.rs, crates/loop-switch/tests/non_file_at_path.rs, .beads/issues.jsonl (6 beads), this file
- Changes: loop classified process-kernel (KINDS.md:1071 — reusable pane ids, non-transactional delivery, no substrate atomicity), secondary scheduler/control/monitoring; do_not_claim: liveness from a safety check, continuity from a typestate, two-person degrading to one, exactly-once delivery, wall-clock fairness. Six-stage invariant table: S4 PARTIAL (bead-availability typed, but loop-queue-filter clock `unwrap_or(0.0)`), S5 PARTIAL (claim permit + pane fence + marker exist; no incarnation, no idempotency key, both live dispatchers bypass the fence), S6 ABSENT (string classifier, no actor), S7 PARTIAL, S8 EXISTS at install / ABSENT at run time, S9 ABSENT (reader only; nsx1). 13 prescriptions mapped, every KNOW with a mirror path:line. Corpus-ranked prescriptions no bead named → filed ywd5 (incarnation-lease-fencing, P0), 7kxf (saga-outcome-unknown-state, P0), igwd (two-person-rule-transition-table, P0), j2te (sampled-reexecution-trust-ladder), wbo2 (layered-enforcement-of-one-rule). Five prescriptions already satisfied here with path:line.
- PRODUCT CHANGE (Rule Zero): the forbidden-claim grep hit `loop-switch/src/lib.rs:14,16` — header said the switch "cannot fail INTO the off state" and unreadable means RUNNING; the code beside it returned Off on any read error, and `Path::exists()` is true for a directory. Reproduced: `mkdir` at the switch path → fleet OFF with no human intent. Fixed `b1906c7` (`is_file()` gate, header rewritten to match; known-bad RED before / GREEN after, known-good file→OFF, mutation bites, byte-identical restore). Positive control for the grep: a planted "guarantees exactly-once" hit at line 3.
- BUILT ≠ WIRED, found while installing the fix: the live supervisor never reads the switch (0 hits in omp-orchestrator/src, no Cargo dep); its only consumers are fast-dispatch/loop-driver, which nothing schedules here, and the installed copies are control-plane 08-26 builds. Filed 31lo P0 with six acceptance legs incl. install. main.rs held by GreenFrog to 00:59Z.
- Instrument note: socraticode timed out twice (index re-embedding 0/539) — outcome UNKNOWN, fell back to structural reads.
- Commit: 50c1288
- Verdict: PRODUCTIVE

### Pass 6 — Skill selection (One Rule) — 2026-09-03T00:40Z
- Files changed: docs/plan/skill-ledger-20260902.md (701 rows, validate-run.py exit 0), .beads/issues.jsonl (4 beads), this file
- Changes: profile Rust 100% / 66 members / 36 bins / no golden-fuzz-property-conformance / CI runs no cargo test; juncture = autonomous dispatch resumed under a supervisor emitting 344 RETRY_BLOCKED / 298 REFUSED / 188 GATE_UNWIRED vs 81 RESULT_RECORDED, 205 commits unpushed behind a correct pre-push refusal. **0 of the 4 skills this loop applied were evidence-selected — chosen by name from 11-lifecycle.md:355-366.** The evidence ranks the same defect family but routes it to agent-mail (37), just-say-no (37), beads-compliance (33), reality-check (32). Contra-indicated: installer-workmanship (deliverable is install.sh; repo forbids .sh). Unranked: multi-agent-swarm-workflow, jeff-swarm-ops. Should-have-run with ≥3 defects behind absence: beads-compliance/validation-discipline (the plan's own S6 skill, 0 invocations) → o7gz P0; oracle-gates/gate-truth (gate.yml runs no cargo test, red 10/10 on a stale sha) → 28dq; trauma-guard/accretive-cron (identical refusal repeated 458×/344×/85× with no recurrence halt) → 9he5; ee (no store here) → v33s.
- Instruments UNKNOWN, verbatim: fh `SEARCH_INDEX_STALE ... exit 3` on target AND positive control (instrument down, harvest did not run today); ee `workspace_store_missing` (no .ee here); cass `stalled, 484 quarantined`; jsm suggest ignores task text; ms suggest cwd-only (positive control via `ms search` hit vibing-with-ntm first).
- PLAN CHANGE for passes 7-10 (evidence over name): 7 = beads-compliance-and-completion-verification (S6; Joshua's "dispatch failure with grading" is this lane), 8 = agent-mail (identity + claim fence, camp-01), 9 = ntm-fleet-monitor + beads-bv (widest juncture anchors), 10 = just-say-no (honesty inventory of the loop itself). Dropped: installer-workmanship, multi-agent-swarm-workflow, jeff-swarm-ops, beads-north-star (folded into 7).
- Root cause delivered this pass on Joshua's ask: zlyy P0 — four kernel defects (send_and_verify Ok/Err collapses a 3-valued verdict → every dispatch reads DISPATCH_FAILED, 86 today; the marker is a clock that never calls read_ack_readback; expiry re-selects `first()` with no cooldown; claim owner is a per-tick pid).
- Commit: (below)
- Verdict: PRODUCTIVE
