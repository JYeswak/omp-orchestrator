[package]
name = "ack-stage"
version = "0.1.0"
edition = "2021"
publish = false
description = "Typed follow-up actions for receiver receipts"

[lints.rust]
unsafe_code = "forbid"

[dependencies]
receiver-receipt = { path = "../receiver-receipt" }
serde_json = "1"
tick-monitor = { path = "../tick-monitor" }
