[package]
name = "ack-spine"
version = "0.1.0"
edition = "2021"
publish = false
description = "Separate transport, delivery, and acknowledgement authorities"

[[bin]]
name = "ack-spine"
path = "src/main.rs"

[lib]
name = "ack_spine"
path = "src/lib.rs"

[dependencies]
serde_json = "1"
finding = { path = "../finding" }

[lints.rust]
unsafe_code = "forbid"
