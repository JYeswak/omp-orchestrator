[package]
license.workspace = true
name = "kernel-only-operator-hook"
version = "0.1.0"
edition = "2021"
publish = false
description = "Fail-closed PreToolUse hook that blocks operator kernel bypasses"

[[bin]]
name = "kernel-only-operator-hook"
path = "src/main.rs"

[lib]
name = "kernel_only_operator_hook"
path = "src/lib.rs"

[dependencies]
asupersync = { git = "https://github.com/Dicklesworthstone/asupersync", rev = "fa3c01aec" }
subprocess-contract = { path = "../subprocess-contract" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
[lints.rust]
unsafe_code = "forbid"
