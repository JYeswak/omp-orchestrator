[package]
license.workspace = true
name = "subprocess-contract"
version = "0.1.0"
edition = "2024"
publish = false
description = "Shared cancel-correct process-group and dual-pipe subprocess boundary"

[dependencies]
asupersync = { workspace = true }

[dev-dependencies]
futures-lite = "2"

[lints.rust]
unsafe_code = "forbid"
