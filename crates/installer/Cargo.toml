[package]
name = "installer"
version = "0.1.0"
edition = "2021"
description = "One-touch install: build, verify four-way identity, install to ~/.local/bin, launchd plist"

[[bin]]
name = "installer"
path = "src/main.rs"

[dependencies]
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
