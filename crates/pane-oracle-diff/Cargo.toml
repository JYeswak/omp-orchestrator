[package]
license.workspace = true
name = "pane-oracle-diff"
version = "0.1.0"
edition = "2021"
description = "A5 tribunal: tmux agent-pane census vs ntm projection. Port of bin/pane-oracle-diff.sh."

[[bin]]
name = "pane-oracle-diff"
path = "src/main.rs"

[lib]
name = "pane_oracle_diff"
path = "src/lib.rs"

[dependencies]
oracle-compare = { path = "../oracle-compare" }
regex = "1"
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
