[package]
license.workspace = true
name = "pane-truth"
version = "0.1.0"
edition = "2021"
description = "Ground-truth tmux pane state; the shell remains the differential oracle."

[lib]
name = "pane_truth"
path = "src/lib.rs"

[[bin]]
name = "pane-truth"
path = "src/main.rs"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock"] }
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
