[package]
license.workspace = true
name = "loop-switch"
version = "0.1.0"
edition = "2021"

[dependencies]
serde_json = "1"
chrono = { version = "0.4", features = ["clock"] }

[[bin]]
name = "loop-switch"
path = "src/main.rs"

[lints.rust]
unsafe_code = "forbid"
