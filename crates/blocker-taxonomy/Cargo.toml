[package]
name = "blocker-taxonomy"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Closed enum of bead blocker kinds with report and kernel-clearable redispatch."
repository.workspace = true
authors.workspace = true

[[bin]]
name = "blocker-taxonomy"
path = "src/main.rs"

[lib]
name = "blocker_taxonomy"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
