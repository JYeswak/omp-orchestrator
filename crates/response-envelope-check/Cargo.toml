[package]
license.workspace = true
name = "response-envelope-check"
version = "0.1.0"
edition = "2021"
publish = false
description = "Fail-closed validation of ntm robot-send response envelopes"

[lints.rust]
unsafe_code = "forbid"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
