# OMP-SURFACE-MAP.toml — the declaration input for the crate census.
#
# THE MISSION (CLAUDE.md, AGENTS.md fifth rule): 100% OMP surface coverage. Every crate declares
# which OMP surface it maps to. A crate absent from this file is UNDECLARED and gets flagged
# exactly as a crate with no caller gets flagged — silence is not an answer, and "none" is.
#
# ═══════════════════════════════════════════════════════════════════════════════════════════════
# REVISION 2, 2026-08-31 20:29Z — MY ORIGINAL CLASSIFICATION WAS REFUTED BY MEASUREMENT.
#
# I published revision 1 with four crates classified (b) reimplemented-by-scraping, on the premise
# that OMP's --mode=rpc offered an alternative we were ignoring. SilverWolf probed the installed
# binary at runtime and answered the deciding question:
#
#   --mode=rpc IS A SINGLE-SESSION TRANSPORT. IT CANNOT ADDRESS A THIRD-PARTY PANE.
#
# Three consequences, each of which INVERTS a row I wrote:
#   * CROSS-PANE DISPATCH IS (a), NOT (b). tmux send-keys and ntm robot-send are the ONLY
#     mechanisms that reach a pane we do not own. Our scraping is CORRECT, not lazy.
#   * OMP PROVIDES NO DELIVERY RECEIPT. My `omp_alternative = "a delivery response on the send"`
#     named something that DOES NOT EXIST. The receiver-verification gap is real and CANNOT be
#     closed by adopting an OMP surface.
#   * THE mux* LAYER IS TUI-ONLY and not reachable from --mode=rpc, so muxConnect/muxPing are not
#     the supervisor's "natural transport" as I claimed.
#
# What SURVIVED as genuine (b): single-session state via get_state, and session continuity via
# --continue/--resume/--session-dir, which we reimplement with pane-id tracking.
#
# THE LESSON, recorded because it cost a revision: I asked the worker to TEST my classification
# rather than confirm it, and the test refuted it. A classification is a CLAIM BY THE AUTHOR. Mine
# was wrong on the largest row, and the correction makes the architecture better news than the
# original finding — most of our scraping is architecturally forced.
# ═══════════════════════════════════════════════════════════════════════════════════════════════
#
# WHY A DECLARATION AND NOT AN INFERENCE: a census that greps crate sources for OMP surface names
# matches its own table. That is the self-referential checker — SIX instances in one session.
#
# CLASSIFICATION, the only three legal values plus the failure state:
#   "a" — genuinely not OMP's concern, OR no OMP alternative exists. A LEGITIMATE TERMINAL STATE.
#   "b" — we reimplemented an OMP surface that EXISTS AND IS REACHABLE. MUST name `omp_alternative`.
#   "c" — an OMP capability we do not use and should.
#   absent — UNDECLARED. The failure state the gate exists to catch.
#
# DENOMINATOR, measured against installed v18.0.11. Re-derive; never inherit:
#   find dist/types -maxdepth 1 -mindepth 1 -type d | wc -l   -> 57
#   omp --help, COMMANDS block                                -> 39
#
# NO-CLAIM: a declaration is a CLAIM BY THE AUTHOR, not a measurement — revision 1 is the proof.
# Adjudication belongs to the inventory map, omp-coverage-mission-ipg.12.
# NO-CLAIM 2 (SilverWolf's, carried): the --mode=rpc probes ran against a no-session, no-tools,
# no-lsp, no-extensions instance. A production session with extensions and LSP is UNMEASURED.

[meta]
revision = 2
denominator_type_dirs = 57
denominator_subcommands = 39
measured_against = "@oh-my-pi/pi-coding-agent v18.0.11"
measured_on = "2026-08-31"
crates_declared = 50
revision_2_reason = "cross-pane dispatch and delivery receipt reclassified (b)->(a); --mode=rpc proven single-session"

# ── (b) REIMPLEMENTED BY SCRAPING — an OMP alternative EXISTS AND IS REACHABLE ───────────────────
# Only two survive revision 2. Both are SINGLE-SESSION concerns, which is exactly the scope where
# --mode=rpc is real.

[crates.dispatch-silence-watch]
classification = "b"
omp_surface = "session"
omp_alternative = "--continue / --resume / --session-dir give real session continuity"
instead_we = "track pane ids and poll for dispatched-then-silent beads"
negative_evidence = "an unclaimed dispatch is invisible to this watcher — measured on 5rh, which sat open with zero comments while a worker carried it. Session continuity would not fix that; the bead projection would still be missing."

[crates.loop-queue-filter]
classification = "b"
omp_surface = "session, task"
omp_alternative = "get_state over --mode=rpc returns structured session state for our OWN session"
instead_we = "filter br ready output and infer state from pane text"
negative_evidence = "if the queue authority is beads rather than OMP, this is (a) — the (b) applies only to the SESSION-state half, not to the queue itself"

# ── (a) NO OMP ALTERNATIVE EXISTS, or genuinely not OMP's concern ────────────────────────────────
# The four cross-pane crates moved here in revision 2. Their scraping is FORCED, not chosen.

[crates.receiver-receipt]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: OMP's --mode=rpc DOES NOT PROVIDE A DELIVERY RECEIPT. Revision 1 named an alternative that does not exist."
instead_we = "diff a timer reset plus a spinner-stripped content hash across two captures"
negative_evidence = "cp-z42vu returned success:[4] while the packet never arrived; the pending-dispatch marker records the inverse. The gap is REAL and cannot be closed by adopting an OMP surface — it can only be closed by a receipt protocol that does not exist today."

[crates.composer-typed]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: inspects a pane we do not own; --mode=rpc is single-session and cannot address it"
negative_evidence = "ntm refuses codex panes with 'cod composer not visible' (cp-nq2s9) while those panes demonstrably accept input — a terminal-inspection false positive that no OMP surface can arbitrate, because OMP cannot see that pane either"

[crates.fleet-composite]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: composes state across panes we do not own"
negative_evidence = "ntm's snapshot returns total_sessions: 0 with success: true when stale; tmux does not lie. A single-session RPC cannot enumerate a fleet."

[crates.tick-monitor]
classification = "a"
omp_surface = "none reachable for CROSS-PANE observation"
why = "REVISION 2: observes OTHER panes. get_state is (b) but only for our OWN session, which this crate does not observe."
instead_we = "classify() matches a braille spinner + elapsed timer on the LAST status line"
negative_evidence = "a stale spinner in scrollback reports a dead pane BUSY forever; two panes read <no marker> because a tool-call box border renders AFTER the status line. Both are scraping artifacts — and both are UNAVOIDABLE while the target pane is not ours."

[crates.omp-orchestrator]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: I claimed omp/muxConnect, muxPing and muxRestartServer were the supervisor's natural transport. The mux* layer is TUI-ONLY and NOT accessible from --mode=rpc. That claim is retired."
negative_evidence = "if a future OMP exposes mux over RPC this becomes (c) immediately — the classification is version-pinned to v18.0.11 and must be re-derived on upgrade"

[crates.ack-stage]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: an acknowledgement requires a receipt, and OMP provides none"
instead_we = "read back a bead comment with br comments list"
negative_evidence = "br comment (singular) prefix-matches, errors on stderr, and EXITS 0 — an agent believed two VERDICT comments landed when neither had. The read-back is load-bearing regardless of transport."

[crates.ack-spine]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 2: the three-authority split exists because no receipt protocol exists. Revision 1 predicted a real ack would collapse authorities 1 and 3 — measurement says there is no such ack to adopt."
negative_evidence = "if OMP ever ships a delivery receipt, authorities 1 and 3 collapse and this crate shrinks. Until then the split is forced."


[crates.no-shell-gate]
classification = "a"
omp_surface = "none"
why = "a repo gate over our own tracked files; OMP has no opinion about our .sh policy"
negative_evidence = "if it ever needed to inspect a running agent it would become (b) — it does not; it reads git ls-files"

[crates.pre-delete-citation-check]
classification = "a"
omp_surface = "none"
why = "guards bead-cited paths against deletion; the tracker is beads, not OMP"
negative_evidence = "caught a live specimen — a staged deletion of a path cited in a CLOSED bead's close_reason — with no OMP involvement at any step"

[crates.kernel-bypass-gate]
classification = "a"
omp_surface = "none"
why = "scans our source for handrolled equivalents of our own kernels"
negative_evidence = "it cannot see an operator handrolling in a shell, and all five measured handrolls were in a shell"

[crates.kernel-only-operator-hook]
classification = "a"
omp_surface = "none"
why = "a PreToolUse hook over OUR agent's tool calls, not over OMP's protocol"

[crates.commit-build-fence]
classification = "a"
omp_surface = "none"
why = "refuses a commit while a build is registered in flight; git and cargo, not OMP"

[crates.undrained-pipe-lint]
classification = "a"
omp_surface = "none"
why = "a source lint for the try_wait-without-draining deadlock; a Rust concurrency defect"

[crates.state-wildcard-lint]
classification = "a"
omp_surface = "none"
why = "refuses a wildcard match arm on a state enum so a new variant is a compile error"

[crates.path-literal-guard]
classification = "a"
omp_surface = "none"
why = "refuses hardcoded repo and home paths so the substrate installs anywhere"

[crates.subprocess-contract]
classification = "a"
omp_surface = "none"
why = "the drain-safe cancel-correct process runner; the LAYER BENEATH any client we build"
negative_evidence = "it has 3 callers, the most in the workspace — if an RPC client removed every subprocess spawn it would lose them, and revision 2 says that client cannot exist for cross-pane work"

[crates.finding]
classification = "a"
omp_surface = "none"
why = "the type that makes a named gap an obligation; a tracker concern"
negative_evidence = "it shipped with one manifest caller whose own caller count was zero — reachable only from dead code until the followup stage depended on it"

[crates.finding-dispatch]
classification = "a"
omp_surface = "none"
why = "renders a SupervisorDecision into a filed Finding"
negative_evidence = "ZERO callers, and it is omp-orchestrator's only manifest caller — the supervisor's sole inbound edge is itself dead code. WIRE or RETIRE; there is no third option."

[crates.omp-rpc-session]
classification = "b"
omp_surface = "jsonrpc, session"
omp_alternative = "OMP --mode=rpc with negotiate_protocol v2, get_state, get_session_stats, and get_messages"
instead_we = "drive one OMP child through an asupersync-owned typed stdin/stdout protocol"
negative_evidence = "live quick probe succeeded with negotiated=2, response_count=4, and unknown_frames=3; this does not reach third-party panes or provide a delivery receipt"

[crates.omp-inventory-map]
classification = "b"
omp_surface = "cli, type roots, jsonrpc, slash-commands"
omp_alternative = "direct OMP --help, installed type listings, and RPC startup metadata"
instead_we = "generate an evidence-carrying machine-readable inventory from direct probes and cargo metadata"
negative_evidence = "live map reports UNKNOWN when slash-command startup metadata is incomplete; it does not convert missing probe data into healthy coverage"

[crates.installer]
classification = "a"
omp_surface = "none"
why = "installs repository binaries and launchd configuration; OMP does not own this substrate installation boundary"

[crates.omp-types]
classification = "a"
omp_surface = "none"
why = "the canonical typed vocabulary derived from asupersync; it is not an OMP lifecycle surface"
negative_evidence = "the crate is a workspace type boundary and has no OMP process caller or OMP protocol dependency"

# ── (c) UNUSED CAPABILITY WE SHOULD ADOPT ────────────────────────────────────────────────────────
# Survivors of revision 2, from SilverWolf's per-surface pass on wave .4.

[crates.dispatch-claim-fence]
classification = "a"
omp_surface = "none reachable"
why = "REVISION 3: refuses a packet naming an unclaimed bead. Landed at 02b5f25 and was undeclared here until the surface gate caught it — the gate's first catch of a crate it did not already know about."
negative_evidence = "A DispatchPermit authorizes from a point-in-time `br show --json` projection and explicitly does NOT attest that transport occurred; the dispatch ledger remains the authority for that separate claim. This is the same claim/receipt split that ntm conflates — sender success is not receiver receipt — and the reason the crate exists is that this session dispatched packets naming unclaimed beads twice."

[crates.porting-gate]
classification = "a"
omp_surface = "none"
inputs = "candidate crate path, Cargo metadata, OMP-SURFACE-MAP.toml, ASUPERSYNC-CONFORMANCE.md, and repository HEAD"
outputs = "versioned JSON gate report with six clause results and a refusal code"
what_must_be_true = "all six porting clauses pass in one archived repository state before a crate arrival is accepted"
negative_evidence = "missing caller, declaration, conformance row, archive-green result, shell/python file, or inventory field refuses the candidate; zero candidates is an anti-vacuity error"
instead_we = "run one candidate-scoped gate instead of treating a copied crate or installed binary as an arrival"

[crates.convergence-stamp]
classification = "a"
omp_surface = "none"
why = "consolidates every grading round into ONE stamped document (docs/plan/ROUNDS.md + docs/plan/STAMP.toml) and provides the predicate that refuses a new round until the stamp covers them all. OMP has no opinion about how we account for our own grading rounds. Exists because the round record was split across twelve files with two irreconcilable halves - CONVERGENCE.jsonl held rounds 8-15 and 22 while rounds 16-21 lived ONLY in eleven per-agent round*.jsonl files and had never reached the canonical ledger, and round 11 is absent from both. Measured 2026-09-01: 559 declared findings across 14 rounds, 220 dispositioned, and nothing could answer \"is every round represented\" from a single artifact. Rust because the repo forbids .sh/.py; sha256 is hand-written because the workspace carries no digest dependency and is verified against the FIPS 180-4 vectors and against shasum -a 256."

[crates.plan-assemble]
classification = "a"
omp_surface = "none"
why = "regenerates docs/PLAN.md from its section files; OMP has no opinion about how we assemble our own documentation. Exists because the prior assembler was 3,297 bytes of Python in /tmp while assembly_freshness demanded the 651 KB artifact be current — a gate whose remedy did not exist. Ported to Rust because the repo forbids .py, and accepted on byte-identical output (sha 56881bf3c2cd3958)."

# ── Extraction status (2026-09-01) ───────────────────────────────────────────────────────────────
# These crates are cross-pane observers. OMP v18.0.11 exposes no reachable alternative for them.


[crates.oracle-pane-state-differential]
classification = "a"
omp_surface = "none reachable for CROSS-PANE oracle comparison"
why = "The differential compares tmux pane_state against ntm fleet state; OMP cannot address either external fleet projection through --mode=rpc."
instead_we = "compare the two independent projections and fail closed on disagreement or empty oracle data"
negative_evidence = "a successful but stale ntm snapshot is not evidence of an empty fleet; the independent tmux census is required."

# ── EXTRACTED FLEET CRATES (bead omp-orchestrator-815; classification recorded at extraction) ──

[crates.dispatcher-deadman]
classification = "a"
omp_surface = "none reachable for DISPATCH LIVENESS"
why = "watchdog: eligible work that received no packet - the invisible failure"
instead_we = "get_state cannot see whether a send was attempted"
negative_evidence = "sender success is not receiver receipt; the watchdog exists for the gap between them"

[crates.fast-dispatch]
classification = "a"
omp_surface = "none reachable for BOUNDED ADMISSION"
why = "admit on a fresh standing verdict, select free panes; fails closed on a stale verdict"
instead_we = "OMP dispatch admission is not exposed"
negative_evidence = "stale verdicts are the measured failure class; freshness is enforced here"

[crates.fleet-monitor]
classification = "a"
omp_surface = "none reachable for CROSS-PANE attention"
why = "OBSERVE lane: attention wait and idle/ready scan; blocking waits, not polling"
instead_we = "no OMP surface exposes a blocking fleet-state transition wait"
negative_evidence = "the anti-pattern is polling; the crate blocks on state transitions"

[crates.fleet-reconcile]
classification = "a"
omp_surface = "none reachable for CROSS-SOURCE reconciliation"
why = "diffs ntm projection against tmux reality and names the projection drift"
instead_we = "get_state is (b) only for our own session, which is exactly the source being checked"
negative_evidence = "stale projection is the failure mode being detected; same-source checking proves nothing"

[crates.fleet-truth]
classification = "a"
omp_surface = "none reachable for CROSS-PANE fleet census"
why = "one place answers 'what is the fleet doing' across ntm projection and tmux"
instead_we = "list-panes + ntm projection read, typed"
negative_evidence = "ntm snapshot returned total_sessions:0 with success:true when stale; tmux does not lie"

[crates.loop-coverage]
classification = "a"
omp_surface = "none reachable for CROSS-CRATE COVERAGE accounting"
why = "maps which production lanes have callers by scanning manifests and sources"
instead_we = "grep -rl over manifests and sources at census time"
negative_evidence = "a checker whose input includes text about the thing it checks is excluded by name"

[crates.loop-driver]
classification = "a"
omp_surface = "none reachable for SINGLE-INSTANCE DRIVING"
why = "single-instance, deadline-bounded driver; two ticks fighting over one pane is corruption"
instead_we = "no OMP primitive enforces single-instance orchestration"
negative_evidence = "the corruption class is cross-process; only a local fence sees it"


[crates.loop-switch]
classification = "a"
omp_surface = "none reachable for LOOP RUNTIME STATE"
why = "the loop-off switch file: a human stops the loop by creating one file"
instead_we = "OMP has no kill-switch surface for external orchestration loops"
negative_evidence = "the switch is a local file contract; reading it is filesystem, not session"

[crates.loop-tick]
classification = "a"
omp_surface = "none reachable for TICK EXECUTION"
why = "single-pane dispatch tick - the unit the driver repeats"
instead_we = "OMP orchestration is not exposed as a dispatchable tick primitive"
negative_evidence = "the tick composes pane-dispatch-fence + composer-typed; no OMP surface composes those"

[crates.ntm-fleet-monitor]
classification = "a"
omp_surface = "none reachable for CROSS-PANE classification"
why = "typed fleet actions and approval waves; classifies, does not send"
instead_we = "get_state is single-session and cannot classify a fleet"
negative_evidence = "classification from tmux/ntm truth, not from a cached label"

[crates.omp-idle-dispatch]
classification = "a"
omp_surface = "none reachable for IDLE-DISPATCH DECISIONS"
why = "dispatch decision from idle evidence: only a confirmed-idle pane gets a bounded packet"
instead_we = "get_state has no cross-session pane admission concept"
negative_evidence = "the wrong-pane dispatch class is what this exists to prevent"

[crates.oracle-compare]
classification = "a"
omp_surface = "none reachable for CLAIM-VS-ORACLE comparison"
why = "shared comparator: claim vs independent oracle; empty oracle is an ERROR, never agreement"
instead_we = "no OMP surface arbitrates a claim against an independent reading"
negative_evidence = "the comparator is logic, not session interaction"

[crates.pane-dispatch-fence]
classification = "a"
omp_surface = "none reachable for CROSS-PROCESS admission fencing"
why = "cross-process per-pane admission fence; two dispatchers landing during a clear vaporise the packet"
instead_we = "no OMP surface arbitrates concurrent senders to a pane"
negative_evidence = "fencing is a process-coordination concern, not a session API"

[crates.pane-dispatch-ready]
classification = "a"
omp_surface = "none reachable for CROSS-PANE admission"
why = "can this pane safely receive a dispatch; fail-closed admission from composer/state evidence"
instead_we = "get_state has no composer-occupancy signal"
negative_evidence = "safe_to_dispatch is not liveness; admission needs evidence OMP does not surface"

[crates.pane-oracle-diff]
classification = "a"
omp_surface = "none reachable for CROSS-SOURCE DIFFERENTIAL"
why = "tmux pane census vs ntm projection differential; catches projection drift before a dispatch rides it"
instead_we = "same-source checking cannot catch source drift"
negative_evidence = "two independent sources are the point; one OMP reading is one source"

[crates.pane-truth]
classification = "a"
omp_surface = "none reachable for CROSS-PANE capture classification"
why = "typed ground-truth pane state from capture-pane output, differential oracle"
instead_we = "get_state cannot see panes of other sessions; capture-pane can"
negative_evidence = "the shell differential remains the oracle; this is the typed reading"

[crates.reap-finished-panes]
classification = "a"
omp_surface = "none reachable for CROSS-PANE reaping"
why = "sweep finished panes before the next dispatch; an unreaped pane is capacity that silently disappears"
instead_we = "OMP exposes no per-pane completion ledger"
negative_evidence = "bead status is the completion authority, and it is ours, not OMP's"

[crates.refill-idle-panes]
classification = "a"
omp_surface = "none reachable for CROSS-PANE refill"
why = "refill every idle pane from the bv DAG; an idle worker beside a ready queue is the conductor failure"
instead_we = "OMP has no queue concept over external trackers"
negative_evidence = "bv is the queue authority; OMP cannot know it"

[crates.tick-dispatch]
classification = "a"
omp_surface = "none reachable for FENCED DISPATCH"
why = "ground-truth pane dispatch fence decided by tmux/ntm truth, not a cached label"
instead_we = "get_state cannot arbitrate pane-level dispatch admission"
negative_evidence = "cached labels are what failed (5rh); tmux truth is the fence"

[crates.verify-dispatch]
classification = "a"
omp_surface = "none reachable for DELIVERY VERIFICATION"
why = "verification from bead status only - ground truth, never a pane self-report"
instead_we = "get_state reports session state, not bead-side verdicts"
negative_evidence = "a pane can claim work while the tracker says otherwise; the tracker is the authority"

[crates.wired-but-inert-guard]
classification = "a"
omp_surface = "none reachable for WIRING PROOF"
why = "a gate walks declared lanes and fails on any that no caller invokes"
instead_we = "OMP cannot assert our internal wiring"
negative_evidence = "BUILT is not WIRED; the gate exists because nothing else proves a caller"

[crates.scratch-home]
classification = "a"
omp_surface = "none reachable for SCRATCH WORKSPACE"
why = "test-only scratch home used to isolate filesystem fixtures; it is not an OMP integration surface"
instead_we = "keep fixture state under an explicit temporary root"
negative_evidence = "the crate is an extraction workspace helper, not a consumer of the installed OMP surface"
