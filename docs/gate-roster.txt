# omp-orchestrator-fsu7 gate roster ledger.
#
# INTENDED DERIVED, and the regenerate path DOES NOT EXIST YET -- omp-orchestrator-gotdk.
# This line read "DERIVED, never hand-maintained: regenerate with `gate-runner --plan`" and that
# instruction is NOT EXECUTABLE. Measured 2026-09-12: crates/gate-runner/src/main.rs:50 declares
# LEDGER_PATH and :189 only READS it (`fs::read_to_string`); the crate's three writes are
# `write_aggregate` at :603 (the citation line) and two test fixtures. There is no roster writer,
# so following the old instruction produces no change and reads as success -- the same
# self-refuting-instruction shape as a doc comment whose own probe disproves it.
# UNTIL THE WRITER LANDS, ROWS ARE ADDED BY HAND against this derivation, which is the ONLY
# authority -- never a hand-kept list, never this file read back into itself:
#   cargo metadata --no-deps --format-version 1 --offline | jq -r '.packages[].name' | sort
# When the writer lands it MUST be an explicit flag and MUST NOT run inside `--plan` or in CI:
# a roster regenerated automatically can never detect the deletion this file exists to catch.
# Its ONLY job is to make DELETION detectable. A derived roster that simply loses a crate
# reports a smaller pass, which is exactly the silent halving gate.yml suffered when a job
# key went missing. A row here with no crate in the workspace is an ERROR naming the crate.
# A crate with no row here is also reported -- and is still RUN, so a new gate crate needs
# no registration to be executed.
ack-spine
ack-stage
admission-reason
agent-mail-native
asupersync-conformance
bead-availability
bead-holder
blocker-taxonomy
build-stamp
cargo-lane-budget
commit-build-fence
composer-typed
contabo-reclaim
convergence-stamp
crate-atom-gate
crate-soundness-verify
decision-ledger
dispatch-claim-fence
dispatch-saga
dispatch-silence-watch
dispatcher-deadman
doctrine-retirement-gate
extraction-roster
fast-dispatch
finding
finding-dispatch
fleet-composite
fleet-idle-monitor
fleet-monitor
fleet-reconcile
fleet-truth
fuzz-build-gate
gate-runner
grader-attribution-gate
inbox-monitor
input-manifest
installer
kernel-bypass-gate
kernel-only-gate
kernel-only-operator-hook
lifecycle-event
lifecycle-monitor
loop-coverage
loop-driver
loop-queue-filter
loop-switch
loop-tick
m2-grading-lane
named-test-filter-gate
no-shell-gate
ntm-fleet-monitor
ntm-kernel
omp-host-tool-guard
omp-inventory-map
omp-orchestrator
omp-rpc-session
omp-surface-consumption
omp-types
ompo-doctor
ompo-start
oracle-compare
oracle-pane-state-differential
orchestration-tick-gate
pane-dispatch-fence
pane-dispatch-ready
pane-oracle-diff
pane-truth
path-literal-guard
plan-assemble
porting-gate
pre-delete-citation-check
preregistration-gate
r1-breadth-gate
reap-finished-panes
receiver-receipt
refill-idle-panes
response-envelope-check
s1-coverage
s2-gate
salvage-taxonomy
scratch-home
sender-identity
silent-success-census
staged-build-gate
state-wildcard-lint
subprocess-contract
text-structure
tick-dispatch
tick-monitor
undrained-pipe-lint
verify-dispatch
wired-but-inert-guard
worker-oracle-gate
worker-tag-gate
