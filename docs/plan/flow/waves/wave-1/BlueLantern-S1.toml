[[disagreement]]
box = "S1"
field = "wave1_sections"
owner_says = "S1 is a draft human-start row with layered hooks and branches."
reviewer_says = "S1 has no [[box.measurement]], [[box.gap]], [box.agreement], or [box.diagram] section, so it is not reviewable under the Wave 1 addendum."
evidence = "2026-09-03T05:39:03Z; grep counts for docs/plan/flow/boxes/S1.toml were measurement=0, gap=0, agreement=0, diagram=0; addendum is this-repo:docs/plan/flow/CONTRACT.md:L54-L96."
severity = "blocker"
resolution = "accepted-and-landed: the blocker is cleared. Owner re-ran grep -cF on docs/plan/flow/boxes/S1.toml -> [[box.measurement]] 6, [[box.gap]] 8, [box.agreement] 1, [box.diagram] 1 (all four were 0 at the reviewer's 05:39:03Z reading). S1 is reviewable under the Wave 1 addendum; the diagram it points at re-validates today, frankenmermaid validate diagrams/S1.mmd --fail-on warning -> exit 0, Nodes 52 Edges 60."

[[disagreement]]
box = "S1"
field = "hook_schema"
owner_says = "Seven hook rows describe surface, hook, and status."
reviewer_says = "Those rows use the pre-addendum surface/hook/status shape and do not carry the required certified Rust fields id, event, matcher, class, fail_mode, binary, policy_file, source_commit, language, stage, certified, and harm_class."
evidence = "sed -n '74,106p' docs/plan/flow/boxes/S1.toml; sed -n '98,104p' docs/plan/flow/CONTRACT.md; the latter is the required control-plane/hooks_certified.toml schema."
severity = "major"
resolution = "accepted AND STILL OPEN, deliberately not half-answered. Owner re-measured: the eight [[box.hook]] rows at S1.toml:75-106 still carry surface/hook/status and none of the twelve certified fields (id, event, matcher, class, fail_mode, binary, policy_file, source_commit, language, stage, certified, harm_class) from CONTRACT.md:98-104. The resolving artifact is a 12-field row per hook naming its future hooks_certified.toml id and its /hook-certification stage. It is NOT written now on purpose: source_commit, certified and policy_file have no true values yet, and a 12-field row filled with placeholders would be a fabricated certificate -- worse than the missing row, because a reader stops looking. Under BUILD FREEZE no hook may be enabled either, so the honest state is: schema named, row unwritten, certification absent."

[[disagreement]]
box = "S1"
field = "crate"
owner_says = "crate = installer (...) + umbrella MISSING and crate_status = exists-no-caller."
reviewer_says = "The contract permits one crate directory or the literal MISSING; a semicolon/composite crate field cannot be resolved to one cargo package. The complete umbrella remains MISSING and must be separated from installer evidence."
evidence = "sed -n '13,21p' docs/plan/flow/CONTRACT.md; sed -n '10,18p' docs/plan/flow/boxes/S1.toml; ls crates/installer and no umbrella crate directory."
severity = "major"
resolution = "accepted-and-landed, same fix as AmberGate's third row: S1.toml:16 is now a single directory (crate = \"installer\"); the umbrella is separated into the bead list :18, the gap ledger :198-201 and no_claim :130, and remains MISSING."