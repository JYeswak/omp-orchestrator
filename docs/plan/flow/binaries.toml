# binaries.toml — verified design+test configuration of the 16 Dicklesworthstone binaries we depend on.
# Mirror root: /Volumes/ZestData/dicklesworthstone-mirror/ (read-only). Paths below are relative to <mirror>/<repo>/.
# Evidence rule: a field is stated only after BinRegistry ran `ls` (existence) or `sed -n` / line-grep (line claim)
# in the 2026-09-03 session. Scout assertions BinRegistry could not confirm carry the literal `UNVERIFIED:` prefix
# (whole field) or an inline `UNVERIFIED:` marker (partially confirmed field). `unverified_count` = number of the 21
# design/test fields in that row containing an UNVERIFIED marker. Scout inputs: BinTracker, BinGates, BinFleet, BinFoundations.
#
# Scout cites refused (path does not exist on the mirror, by ls, or the claim is contradicted by a read line):
#   BinTracker: beads_rust/src/output.rs and src/config.rs (both are directories), beads_viewer_rust/src/cli/mod.rs,
#              src/scoring.rs, src/triage.rs (real: src/cli.rs, src/analysis/{graph,triage}.rs), bv bin name `bv` (real: bvr),
#              mcp_agent_mail_rust/scripts/install.sh (real: root install.sh), crates/mcp-agent-mail-core/src/doctor.rs
#              (real: crates/mcp-agent-mail-cli/src/doctor/), README.md health-endpoint lines (no `health/liveness` in README by grep),
#              core/src/config.rs `~/.config/agent-mail/config.toml + AM_CONFIG_DIR` (grep found nothing).
#   BinGates:  remote_compilation_helper "absent" (present), destructive_command_guard/tests/{cli,evaluator,normalize,packs}.rs,
#              dcg install.sh functions install_unix/detect_shell/validate_config/install_packs/install_hook, dcg CI jobs
#              lint/test-release/windows-test/docker/e2e-phase-*, dcg EXIT_PARSE_ERROR=2 (it is 4), ubs lib/ and contract_conformance.py,
#              cass tests/{indexer,search,doctor,analytics}.rs, cass src/cli/, cass unsafe_code=forbid in Cargo.toml (real: lib.rs deny).
#   BinFleet:  process_triage/crates/pt-core/src/{scanner/scoring.rs,logger/,decision_record.rs,supervision/snapshot_persist.rs},
#              process_triage/crates/pt-core/tests/decision_plane_tests.rs, storage_ballast_helper/tests/decision_plane_tests.rs,
#              sbh fuzz/, ntm "no install.sh" and "no doctor" (both exist), frankenterm "no install.sh" (exists),
#              frankenterm .github/workflows (absent in mirror), pt/ft "property_fuzz NONE" (fuzz targets exist), pt exit lattice 0/1/2.
#   BinFoundations: asupersync/benchmarks.yml at root (lives in .github/workflows/), asupersync Cargo.toml:1 for lints (real: :939-943),
#              asupersync "6 bins" (7 [[bin]]), frankentui slo.yaml:1-234 (file is 293 lines), meta_skill bin "jsm" (bin is `ms`),
#              meta_skill LICENSE "Apache 2.0 + MIT" (LICENSE:1 = MIT with OpenAI/Anthropic Rider).

[[binary]]
repo = "beads_rust"
bin = "br"
cli_grammar = "clap `br <verb>`: `pub struct Cli` src/cli/mod.rs:694 with `#[command(subcommand)] pub command: Commands` (:695-696); `pub enum Commands` :748; 47 verb modules in src/cli/commands/ by ls (agents, audit, blocked, capabilities, capacity, changelog, close, comments, completions, config, coordination, count, create, defer, delete, dep, doctor, doctor_subsystems/, epic, gate, graph, history, info, init, label, lint, list, orphans, q, query, ready, reopen, robot_docs, scheduler, schema, search, show, stale, stats, sync, update, upgrade, vcs, version, where)"
robot_mode = "global `--json` (`#[arg(long, global = true)] pub json: bool` src/cli/mod.rs:707-708); doctor `--robot-triage` (:3338-3339); per-subcommand `--json` on doctor ls/undo/migrate-schema/explain (:3470-3567); robot_docs.rs command module (ls src/cli/commands/)"
exit_lattice = "src/error/structured.rs:233-276 `pub const fn exit_code`: 2 database/init (DatabaseNotFound..AlreadyInitialized), 3 issue/operational (IssueNotFound, AmbiguousId, NothingToDo, CloseIncomplete), 130 ShuttingDown, 4 validation (ValidationFailed, PolicyViolation, WorkflowCapacityExceeded), 5 dependency (CycleDetected, DependencyNotFound, SelfDependency, DuplicateDependency), 6 sync (JsonlParseError, PrefixMismatch, ConflictMarkers, PathTraversal), 7 config, 8 I/O (IoError, JsonError, YamlError), 1 InternalError; src/error/mod.rs:570-579 asserts IoError=8, InternalError=1; separate DoctorExitCode (ConcurrencyLost, FindingsPresent) in src/cli/commands/doctor_subsystems/exit_codes.rs (ls; used src/main.rs:197-219, 1932-1933)"
envelope = "error envelope `{error: {code, message, hint, retryable}}` src/error/structured.rs:590-595 (`pub struct StructuredError { code: ErrorCode, ... }` :290-292; test asserts json[error][code]==ISSUE_NOT_FOUND :1350-1353); in `--json` mode the structured error is routed to the JSON stream (src/main.rs:1894-1914); success output via `pub struct OutputContext { mode: OutputMode, ... }` src/output/context.rs:19-21 (no single success wrapper verified)"
config = "src/config/mod.rs:5-10 precedence: env vars, project `.beads/config.yaml`, user `~/.config/beads/config.yaml` (falls back to `~/.config/bd/config.yaml`), legacy `~/.beads/config.yaml`, DB config table, defaults; user_config_path :212-225; project_config_path :236-240; BEADS_DIR honoured :262; config key registry backs `br config schema` and doctor `config.unknown_keys` (:6990-6994)"
state = "`.beads/` (or `_beads` for monorepos, src/config/mod.rs:54-58) holding `beads.db` (SQLite) + tracked `issues.jsonl` + `metadata.json` + `config.yaml` + `routes.jsonl` + `redirect` (:320-324, 6732-6735); history snapshots `.beads/.br_history` (:7098); recovery quarantine `.beads/.br_recovery/` (:12085); storage impl src/storage/{sqlite,schema,events}.rs and sync locks src/sync/{db_inode_lock,history,path,witness}.rs (ls); linked git worktrees resolve to the primary checkout's `.beads` (:322-332, GitHub #429)"
lints = "Cargo.toml:231-238 `[lints.rust] unsafe_code = deny` with comment naming three sanctioned `#[allow(unsafe_code)]` carve-outs (sync::db_inode_lock, shutdown::restore_default_sigpipe_unix, shutdown::exit_process); :240-242 `[lints.clippy] pedantic + nursery = deny`; rust-toolchain.toml, tarpaulin.toml (ls)"
tests_unit = "in-crate `#[cfg(test)]`: src/error/mod.rs:569-579 exit-code asserts; src/close_policy.rs:2887-2888 gate-name asserts; src/cli/commands/sync.rs:5991-6391 SyncArgs mode-conflict tests; src/storage/sqlite.rs:21120-21175 transition_comment tests, :23709-23713 cycle-report test; src/config/mod.rs:7719-12087 config/discovery tests"
tests_integration = "tests/ by ls: e2e_*.rs family (e.g. e2e_sync_needs_flush_guard.rs, e2e_sync_failure_injection.rs, e2e_doctor_fixture_suite.rs, e2e_doctor_chokepoint.rs, e2e_broken_pipe.rs, e2e_concurrency.rs, e2e_git_safety_full_cli.rs, e2e_mcp_protocol.rs, e2e_installer.rs), storage_*.rs (crud, deps, history, invariants, ready, export_atomic), repro_*.rs regression files, workspace_failure_replay.rs, tests/e2e/ + tests/e2e_scripts/ dirs"
conformance_or_oracle = "the Go `bd` binary is the oracle: tests/conformance.rs + conformance_{edge_cases,labels_comments,schema,text_output,workflows}.rs (ls); .github/workflows/conformance.yml:29-30 job `conformance` named `br<->bd Conformance`, weekly cron (:14-15) with `strict_mode` input (:6-7)"
property_fuzz = "proptest: tests/proptest_{claim_exclusion,hash,id,jsonl_roundtrip,merge,model_roundtrip,parent_child,status_partition,sync_path,time,validation}.rs with committed .proptest-regressions (ls); cargo-fuzz: fuzz/fuzz_targets/{config_yaml,content_hash,jsonl_import,markdown_import,merge_issue,search_issues,sync_cycle}.rs + fuzz/corpus (ls); tests/e2e_sync_fuzz_edge_cases.rs, repro_sync_cycle_fuzz_crashes.rs (ls)"
golden = "insta snapshots tests/snapshots/*.snap (ls): golden_beads_init__{init_directory_listing,init_text_contents}, golden_rich_panels__{list,show,show_markdown,stats}_width_{80,120}, storage_golden_snapshot__create_update_close_sqlite_rows_and_jsonl; drivers tests/golden_beads_init.rs, golden_rich_panels.rs, storage_golden_snapshot.rs, snapshots.rs (ls); tests/snapshots/{cli_output,error_messages,json_output,jsonl_format,robot_output,schema_output,toon_output,history_diff_output}.rs (ls)"
gate_self_test = "`br doctor --selftest` (src/cli/mod.rs:3397-3400, conflicts_with repair/repair_indexes/robot_triage/quick) backed by src/cli/commands/doctor_subsystems/selftest.rs (ls); known-bad fixture corpus tests/doctor_fixtures/ (ls: config_yaml_malformed, jsonl_conflict_markers, jsonl_duplicate_ids, jsonl_malformed_utf8, db_missing_with_jsonl, empty_database_with_jsonl, blocked_cache_stale, gitignore_leaking_beads, doctor_mutates_without_fix, healthy_workspace_baseline, ... + COVERAGE.md) driven by tests/e2e_doctor_fixture_suite.rs; .github/workflows/doctor.yml:36-37 job `doctor-health`"
ci_jobs = "ci.yml:32-432 jobs: check, security (Security Audit), test (sharded matrix), reliability-gates, audit-gates (Audit Guardrails), build (matrix), bench, version-audit, e2e-quick; six-hourly cron (:12-15); conformance.yml: conformance; doctor.yml: doctor-health; other workflows by ls: audit.yml, e2e-full.yml, release.yml, update-package-manifests.yml, notify-acfs.yml"
doctor_selftest = "`br doctor` with subcommands ls / undo / migrate-schema (apply, undo) / explain each with `--json` (src/cli/mod.rs:3474-3568), flags `--robot-triage` (:3339), `--quick` (:3341-3343), `--selftest` (:3400); doctor_subsystems/{capabilities_doctor,engine,exit_codes,mutate,refuse_gates,run_dir,schema_migration,selftest,surface}.rs (ls); read-only doctor refuses on a live write lock with ConcurrencyLost (src/main.rs:1998-2022)"
install = "install.sh (ls) options :8-25 (--version, --dest, --system, --easy-mode, --verify, --artifact-url, --checksum, --checksum-url, --insecure-skip-checksum, --from-source, --skip-skills, --with-migration-skill, --uninstall); curl|bash self-protection re-exec from an on-disk copy (:31-40, issue #250); functions: detect_platform :586, resolve_version :694, acquire_lock :764, verify_archive_checksum :1296, archive_member_name_is_safe :1340, validate_tar_archive_members :1453, validate_zip_archive_members :1480, download_release :1497, install_binary_atomic :1179, build_from_source :1238, check_conflicts :1616, fix_alias_conflicts :866 (br aliased to bun run), do_uninstall :554, main :1727; packaging/ (ls)"
hooks_installed = "install_skills() install.sh:885 installs Claude Code / Codex skills (skills/bd-to-br-migration by ls; opt-in `--with-migration-skill` :23, `--skip-skills` :22, print_skills_summary :976); agent-facing AGENTS.md managed by `br agents` (src/cli/mod.rs:749-750); no PreToolUse-style hook checked"
versioning = "Cargo.toml:3 version = 0.5.7, rust-version 1.88 (:5); build.rs:1-30 vergen-gix (build timestamp, target triple, rustc semver) plus emit_git_metadata via `git rev-parse HEAD`; ci.yml:399-400 job `version-audit` verifies embedded version info; tests/e2e_version.rs (ls); UPGRADE_LOG.md, CHANGELOG.md (ls)"
fail_closed = "close policy: transition_comment is request-scoped and historical comments never satisfy it (src/close_policy.rs:214-217); missing => PolicyViolation gate `transition_comment_missing` (:1206-1219) => exit 4 (structured.rs:256-257); a comment on a non-transition is a validation error (sqlite.rs:6959-6962); write lock held by another process => ConcurrencyLost refusal instead of a generic error (src/main.rs:175-219); sync mode flags mutually exclusive via mode_count (sync.rs:913-917); JSONL outside `.beads` refused unless an explicit external DB family (config/mod.rs:5684-5687)"
design_invariant = "dual store: SQLite `.beads/beads.db` is the working database, git-tracked `issues.jsonl` is the sync authority; `br sync --flush-only` maps to SyncOperation::Flush (sync.rs:1478-1479); dependency cycles condensed with iterative Tarjan SCC in emission order (src/storage/sqlite.rs:607-610, 740) surfaced by detect_dependency_cycle_report (:18608, called dep.rs:1933); every exit funnels through shutdown::exit_process (src/main.rs:930-933, 1002-1008)"
what_we_should_copy = "(1) ErrorCode -> const fn exit_code category lattice (structured.rs:233-276); (2) doctor_fixtures/ known-bad corpus + `--selftest` + ConcurrencyLost refusal path; (3) installer archive-member validation and atomic binary swap (install.sh:1340-1480, 1179); (4) proptest + cargo-fuzz + insta golden trio in tests/; (5) Go-reference conformance workflow with strict_mode input"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 0

[[binary]]
repo = "beads_viewer_rust"
bin = "bvr"
cli_grammar = "clap `pub struct Cli` src/cli.rs:69; flag-based robot/query surface, subcommands reserved for hand-typed actions such as `bvr upgrade` (:70-72); binary and lib both named `bvr` (Cargo.toml:33-39; description :6 `CLI binary: bvr`) — scouts called it `bv`"
robot_mode = "`--robot-next` :101, `--robot-overview` (alias `--robot-orient`) :103-104, `--robot-triage` :107, `--robot-triage-by-track` :110, `--robot-triage-by-label` :113, `--robot-plan` :116, `--robot-insights` :119, `--robot-priority`, `--robot-alerts`, `--robot-help` (:597-606) in src/cli.rs; `--robot-schema` / `--robot-docs` need no issues loaded (src/main.rs:502-548); `--robot-diff` requires `--diff-since` (:1222-1223)"
exit_lattice = "src/main.rs: `ExitCode::from(2)` for usage / mutually-exclusive flags (:455, :494 background-mode conflict, :499 orphaned modifiers, :614, :724 `--watch-export` without `--export-pages`, :1223); `ExitCode::from(1)` for runtime errors (:520-534 etc); `ExitCode::SUCCESS`; drift commands propagate `result.exit_code` (:2028, :2057-2066); EarlyCommandOutcome carries exit_code (:2615-2617)"
envelope = "`pub struct RobotEnvelope { generated_at: String, data_hash: String, output_format: String, ... }` src/robot.rs:15-18; JSON-schema envelope requires [generated_at, data_hash] (:991) and includes output_format + version (:2097-2101); robot-docs shape `{generated_at, output_format: json, version, topic}` (:832-836); drift output carries has_drift + exit_code (:1743-1744); `version` = v{CARGO_PKG_VERSION} (:2182)"
config = "UNVERIFIED: BinTracker gave no config-path claim for bv and BinRegistry ran no config grep"
state = "reads br issues via src/loader.rs (ls); persists FeedbackStore (`FeedbackStore::open(&feedback_path)` src/main.rs:1666, `--feedback-accept/--feedback-ignore` :2195) and drift Baseline (`Baseline::load(&project_dir)` :1998, `--save-baseline` :1994); exporters src/export_{md,pages,sqlite}.rs (ls); analysis cache src/analysis/cache.rs (ls)"
lints = "Cargo.toml:85-86 `[lints.rust] unsafe_code = forbid`; :88-91 `[lints.clippy] pedantic + nursery = warn`, doc_markdown allow; rust-version 1.85 (:5)"
tests_unit = "in-crate: src/analysis/graph.rs:2038 pagerank_sums_near_one, :2478-2482 triage_only config asserts, :2510-2513 slow-phase skips; src/analysis/triage.rs:1360 triage_all_closed_produces_no_actionable, :1560 triage_includes_actionable_in_progress_items, :1677 blockers_to_clear chain, :1795 omits top commands when nothing actionable; TUI snapshot tests emit src/snapshots/bvr__tui__tests__*.snap (ls)"
tests_integration = "tests/ 18 entries (ls): admin_cli.rs, background_mode.rs, cli_model_validation.rs, conformance.rs, e2e_export_pages.rs, e2e_robot_matrix.rs, e2e_workspace_history.rs, export_md.rs, export_pages.rs, robot_alerts.rs, robot_burndown_scope.rs, robot_history_since.rs, schema_validation.rs, stress_fixtures.rs, test_parse_timestamp.rs, conformance/, test_utils/, testdata/; shell drivers scripts/e2e_robot_cli.sh, e2e_preview_pages.sh (ls)"
conformance_or_oracle = "the Go beads_viewer is the oracle: tests/conformance/go_reference/ + tests/conformance/fixtures/ (ls) consumed by tests/conformance.rs; ci.yml:40-41 job `conformance` (Conformance Tests); parity contracts cite Go issues in source (beads_viewer#158 at src/analysis/triage.rs:779, 1246-1247; issue #25 status gate :784)"
property_fuzz = "no fuzz/ directory at repo root (ls root listing); proptest usage not grepped"
golden = "insta TUI snapshots src/snapshots/ (ls): bvr__tui__tests__snap_{main,board,graph,history,insights,flow_matrix,label_dashboard,actionable}_{narrow,medium,wide}.snap, *_detail_focus, e2e_journey_*.snap; fixture corpus tests/testdata/*.jsonl (adversarial_parity, adversarial_stress, all_closed, boundary_conditions, ... + FIXTURES.md by ls)"
gate_self_test = "UNVERIFIED: BinTracker made no self-test / fires-on-known-bad claim for bv and BinRegistry ran no `--selftest` grep"
ci_jobs = "ci.yml:13-112 jobs: check (Check & Lint), unit (Unit Tests), conformance (Conformance Tests), e2e (E2E Tests), bench (Benchmarks smoke), build (Release Build, needs unit+conformance+e2e); single workflow file (ls .github/workflows)"
doctor_selftest = "UNVERIFIED: BinTracker made no doctor claim; `bvr upgrade` subcommand exists (src/cli.rs:70, main.rs:2791-2843) but no doctor grep was run"
install = "no install.sh at repo root (ls); the installer is shipped by dsr: doodlestein_self_releaser/installers/bv/install.sh (ls)"
hooks_installed = "none observed: no hooks/ or skills/ dir in root ls; integrates with br by reading its store (src/loader.rs) and with git via src/analysis/{git_history,correlation}.rs (ls)"
versioning = "Cargo.toml:3 version = 0.3.0; build.rs:1-12 vergen-gix (build timestamp, target triple, rustc semver); robot outputs embed version (src/robot.rs:2182); `bvr upgrade` compares current vs latest and reports `Up to date (v..)` (src/main.rs:2791-2843)"
fail_closed = "usage errors exit 2 before any data load (src/main.rs:455-499); `--robot-diff` without `--diff-since` exits 2 (:1222-1223); drift check returns non-zero result.exit_code on breach (:2028); `--robot-search` without `--search` exits 1 (:2074-2075)"
design_invariant = "recommendations are status-gated by `graph.actionable_ids()` (open/in_progress only) and top picks additionally must be claimable — status exactly open and not a parent with open children (src/analysis/triage.rs:655-666, 777-786); PageRank computed by `fn compute_pagerank` src/analysis/graph.rs:835 under AnalysisConfig with per-metric skip reasons and timeouts (:17-19, 52-55, 700-703)"
what_we_should_copy = "(1) flag-based robot surface with `--robot-schema` emitting JSON Schema per command (src/robot.rs:455-458, 971-991); (2) go_reference conformance fixtures dir + CI job; (3) TUI insta snapshots at three widths; (4) AnalysisConfig with explicit skipped_metrics reasons (graph.rs:700-703)"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 3

[[binary]]
repo = "mcp_agent_mail_rust"
bin = "am"
cli_grammar = "UNVERIFIED: BinTracker claimed a `typer app` with subcommands projects mark-identity/discovery-init/adopt at crates/mcp-agent-mail-cli/src/lib.rs:50-100 (typer is Python; crate is Rust); lib.rs exists (ls) but was not read"
robot_mode = "UNVERIFIED: BinTracker claimed `--robot --max-seconds`; only crates/mcp-agent-mail-cli/src/robot.rs and doctor/robot_docs.rs existence was confirmed by ls"
exit_lattice = "UNVERIFIED: no scout claim; grep for EXIT_ consts / fn exit_code in cli/src/lib.rs, cli/src/main.rs, core/src/error.rs returned nothing"
envelope = "UNVERIFIED: no scout claim and no grep run"
config = "install.sh:2647-2648 defaults `STORAGE_ROOT=$HOME/.mcp_agent_mail_git_mailbox_repo` and `RUST_DB_PATH=$RUST_STORAGE_ROOT/storage.sqlite3`; :6816-6818 default storage `$HOME/.mcp_agent_mail` with legacy `.mcp_agent_mail_git_mailbox_repo`; env-file config loaded by load_env_key :1875; XDG config dir via rust_config_env_path :1253 (comment: `am setup` writes under XDG); BinTracker's `~/.config/agent-mail/config.toml + AM_CONFIG_DIR` not found (grep of core/src/config.rs:1-200)"
state = "git-backed mailbox repository (STORAGE_ROOT) plus SQLite `storage.sqlite3` (install.sh:2647-2648); storage crate src/{boot_check,recovery,lib}.rs (ls); FileReservation model carries `path_pattern`, `exclusive: bool`, `reason`, `created_ts`, `expires_ts` (crates/mcp-agent-mail-core/src/models.rs:201-205); core modules mailbox_durability.rs, reservation_artifact.rs, git_lock.rs (ls)"
lints = "Cargo.toml:164-172 `[workspace.lints.rust] unsafe_code = deny` (comment: every crate root carries `#![forbid(unsafe_code)]`; deny so env-mutating integration tests can opt in, br-sox5q); :173-174 `cfg(fuzzing)` declared; :176-178 `[workspace.lints.clippy] pedantic + nursery = warn`; deny.toml (ls); supply-chain-audit.yml (ls)"
tests_unit = "in-crate tests in crates/mcp-agent-mail-server/src/lib.rs: readiness 200 vs 503 (:19736-19790), liveness 200 unauthenticated (:28200-28203), POST on health => 405 (:27790-27793, :28037-28040), CORS on health (:21106-21180), access-log shape (:27058-27061)"
tests_integration = "tests/docs_drift_ci.rs, tests/conformance/fixtures, tests/artifacts (ls); crates/mcp-agent-mail-conformance/tests/{conformance,conformance_debug,contact_enforcement_outage,doc_consistency,error_code_parity,protocol_compliance,resource_coverage_guard,resource_description_parity,tool_description_parity}.rs (ls); shell e2e suite scripts/e2e_{http,serve,cli,console,archive,dual_mode,mcp_api_parity,mode_matrix,search_v3,share,spill_determinism,tui_*}.sh (ls)"
conformance_or_oracle = "the Python sibling mcp_agent_mail is the oracle: Cargo.toml:136 `the sibling is a GATED CLONE at the v0.3.32 release pin`; crates/mcp-agent-mail-conformance (ls) run by ci.yml:385-386 job `conformance` (Semantic Conformance); error_code_parity.rs and tool/resource description parity tests (ls); Dockerfile:306-307 healthcheck timings match the Python sibling image"
property_fuzz = "crates/mcp-agent-mail-guard/fuzz/{Cargo.toml,corpus,fuzz_targets} and crates/mcp-agent-mail-storage/fuzz/{Cargo.toml,corpus} (ls); `cfg(fuzzing)` hooks in guard and storage (Cargo.toml:173-174); crates/mcp-agent-mail-core/src/proptest_generators.rs (ls); no root fuzz/ dir (ls)"
golden = "crates/mcp-agent-mail-cli/src/golden.rs (ls); ci.yml:972-973 job `snapshots` (Help Snapshots — golden contract drift detection); scripts/e2e_histogram_snapshot.sh (ls)"
gate_self_test = "doctor selftest `fn run_selftest_sequence_in_process(project_key)` crates/mcp-agent-mail-cli/src/doctor/selftest.rs:528-530 exercising identity/messaging/reservations tools in-process; ci.yml:1385-1386 `skip-guard` asserts non-zero test counts so silent skips fail; :889-890 incident-regression-gates; :1140-1141 durability-e2e (corruption + fault injection); :1269-1270 chaos"
ci_jobs = "ci.yml jobs: dashboard-wasm :17, test :77, mode-matrix :166, native-command-matrix :224 (OS matrix), conformance :385, perf-security :441, e2e-dual-mode :605, search-v3-e2e :675, incident-regression-gates :889, snapshots :972, durability-unit :1031, durability-e2e :1140, chaos :1269, skip-guard :1385; other workflows by ls: archive-fsync-matrix, archive-perf-gate, atc-perf-gate, conformance-fixture-regen, deploy-pages, dist, docker, notify-acfs, observability_drift, publish, search-v3-weekly, supply-chain-audit"
doctor_selftest = "crates/mcp-agent-mail-cli/src/doctor/{capabilities,manifest,mutate,platform,process_owner,robot_docs,runs,selftest,undo}.rs + doctor/fixers/ (ls); doctor_orphan_refs.rs (ls); server-side startup_checks.rs, integrity_guard.rs, disk_monitor.rs (ls crates/mcp-agent-mail-server/src)"
install = "install.sh at repo root (ls; BinTracker's scripts/install.sh does not exist): detect_platform :396, establish_release_contract :360, establish_release_trust_model :382 (minisign vs legacy Sigstore chosen by version), require_minisign :7345, verify_minisign_signed_checksum :7361, require_safe_cosign :7235, verify_sigstore_bundle :7281, verify_checksum :7108, verify_archive_members_exact :7444, verify_release_binaries_exact :7492, install_binary_pair_transactional :3742 (journaled two-binary transaction; rollback_binary_transaction_target :3432, recover_binary_pair_transaction :3580, handle_binary_transaction_signal :8167), Python->Rust migration detect_python :1518 / displace_python_alias :1665 / stop_python_server :1944 / migrate_env_config :2646, uninstall :6970 with purge_data_paths :6847; also install.ps1, install-local.sh (ls)"
hooks_installed = "installer writes MCP client configs: setup_mcp_configs :6168, setup_claude_code_mcp_via_cli :6016, sync_codex_http_configs :6319, setup_single_opencode_json_config :5683, cleanup_mcp_configs :6732 on uninstall; user service via write_launchd_service_plist :4783 / platform_supports_user_service_management :4540 (systemctl or launchctl); git pre-commit guard material: scripts/git-with-amlock.sh and crates/mcp-agent-mail-guard (ls) — guard install path not grepped"
versioning = "Cargo.toml:19-20 `[workspace.package] version = 0.3.32`; core/src/am_version.rs (ls); installer pins the exact release and checks `--version` output of both binaries (binary_version_matches_exact :7477, verify_release_binaries_exact :7492); UPGRADE_LOG.md, CHANGELOG.md (ls)"
fail_closed = "installer: minisign required for releases >= v0.3.31, otherwise abort (require_minisign :7345-7348; comment :7360 `Fail-closed at every step`); cosign only accepted at safe versions (require_safe_cosign :7235); checksum verification cannot be skipped without explicit `--no-verify` (:7107); readiness returns 503 when the DB check fails (server lib.rs:19787-19790); non-GET health requests => 405 (:11569-11570, :11579-11580)"
design_invariant = "no tokio anywhere: Cargo.toml:132-134 forbids frankensearch aggregate features because they pull hf-hub/reqwest/hyper/tokio, downstream crates opt into `no-Tokio subfeatures`; health surface fixed at `/health/liveness`, `/health/readiness`, `/health`, `/healthz` (server lib.rs:56-61; handlers :11568, :11578) with liveness unauthenticated (:28200-28203)"
what_we_should_copy = "(1) transactional two-binary install with journal, rollback and signal handling (install.sh:3742, 3432, 3580, 8167); (2) release trust model selecting the signature scheme by version (install.sh:382); (3) skip-guard CI job asserting non-zero test counts (ci.yml:1385); (4) liveness/readiness split with unauthenticated liveness and 405 on wrong method"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 4

[[binary]]
repo = "ntm"
bin = "ntm"
cli_grammar = "spf13/cobra: cmd/ntm/main.go (ls) -> internal/cli/root.go (ls) with `func ExitCode(err error) int` :616-625; doctor command `newDoctorCmd()` internal/cli/doctor.go:31-34 (`Validate the NTM ecosystem health`); Go module go.mod (ls); VERSION 1.31.0 (sed)"
robot_mode = "internal/robot/ (ls: 242 entries incl. robot.go, spawn.go, types.go) with `type RobotResponse struct { Success bool json:success ... }` internal/robot/types.go:233-236 (doc :232 mentions a `hint` field); e2e-tests.yml:211-212 job `e2e-robot-mode` (Robot Mode Commands); the `--robot-*` flag count claimed by BinFleet (65+) was not counted"
exit_lattice = "internal/cli/root.go:616-625: nil -> 0; errors implementing `ExitCode() int` -> `robot.NormalizeProcessExitCode(code)`; anything else -> 1; `func NormalizeProcessExitCode(code int) int` internal/robot/types.go:662-665 (comment :661: unknown codes collapse to a normal command error exit 1)"
envelope = "`RobotResponse{Success bool json:success, ...}` internal/robot/types.go:233-236 — Success is documented as the first field agents check (:234-235); remaining fields not read"
config = "internal/config/config.go:1819-1827: `$XDG_CONFIG_HOME/ntm/config.toml` when XDG_CONFIG_HOME set, else `~/.config/ntm/config.toml`; `NTM_PROJECTS_BASE` env overrides DefaultProjectsBase (:1831-1835); ensemble config validated in config.toml (:851-852)"
state = "UNVERIFIED: BinFleet claimed `.beads/issues.jsonl + tmux session state via tmux list-panes`; no grep was run for the persisted-state layout"
lints = ".golangci.yml at repo root (ls); ci.yml:140-141 job `lint`; Go has no unsafe-forbid lint"
tests_unit = "internal/robot/*_test.go = 136 files (ls | wc); internal/cli/doctor_test.go, spawn_test.go, assign_test.go (ls); internal/robot/robot_test.go (ls)"
tests_integration = "e2e/ (ls: activity_metrics_test.go, atomic_assignment_cli_e2e_test.go, canonical_pane_contract_test.go, checkpoint_test.go, cleanup_test.go, codex_goal_send_test.go, composer_delivery_e2e_test.go, config_migration_test.go, conflict_deadlock_cli_e2e_test.go, context_rotation_e2e_test.go, ...); tests/, fakeshell/, testdata/ (ls root); ci.yml:525-526 job `integration`; e2e-tests.yml:80-81 `e2e-mock` (Hermetic E2E Tests)"
conformance_or_oracle = "real sibling binaries as oracle: e2e-tests.yml:274-275 job `e2e-macos-bv` (macOS Real br+bv E2E) on macos-15; ci.yml:289-290 `parity-harness` (Hermetic Serve Parity); :183-184 `openapi-drift`; internal/agentmail/ and internal/bv/ adapters (ls)"
property_fuzz = "UNVERIFIED: BinFleet reported NONE identified; BinRegistry ran no fuzz/proptest grep"
golden = "UNVERIFIED: BinFleet reported NONE identified; testdata/ exists (ls root) but its contents were not classified"
gate_self_test = "ci.yml:31-32 `reality-guards`, :52-53 `reality-guards-go`; e2e-tests.yml:80 `e2e-mock` with `mock_mode` dispatch input (:37-38), :176-177 `e2e-infra` (E2E Infrastructure Check), :211 `e2e-robot-mode`; nightly cron (:63-64)"
ci_jobs = "ci.yml jobs: reality-guards :31, reality-guards-go :52, release-preflight :87, vscode-extension :115, lint :140, openapi-drift :183, test :223 (OS matrix), parity-harness :289, coverage :313 (Coverage Thresholds), build :367 (goos/goarch matrix), security :429, goreleaser-check :468, dependency-review :504, integration :525, upgrade-check :580 (OS matrix), ci-success :614; e2e-tests.yml jobs: e2e-mock, e2e-infra, e2e-robot-mode, e2e-macos-bv, e2e-success; other workflows by ls: release.yml, web-audit.yml, notify-acfs.yml (BinFleet listed workflow file names as jobs)"
doctor_selftest = "`ntm doctor` internal/cli/doctor.go:31-34 (newDoctorCmd) and runDoctor :323-325 (30 s context timeout); doctor_test.go (ls); BinFleet's `NONE identified` refuted"
install = "install.sh at repo root (ls; BinFleet's `no install.sh` refuted): detect_platform :102; other functions not enumerated; release_artifacts.toml, Makefile, Dockerfile, package.json (ls); dsr ships installers/ntm/ (ls doodlestein_self_releaser/installers)"
hooks_installed = "runtime integrations internal/agentmail/ and internal/bv/ (ls); vscode/ extension + ci.yml:115-116 vscode-extension job; SKILL.md at root (ls); Claude/Codex hook installation not grepped"
versioning = "VERSION file = 1.31.0 (sed); ci.yml:468-469 goreleaser-check, :87-88 release-preflight (dry-run), :580 upgrade-check matrix; release.yml (ls); CHANGELOG.md, UPGRADE_LOG.md (ls)"
fail_closed = "any error without an ExitCoder maps to exit 1 (root.go:624); NormalizeProcessExitCode collapses unknown codes to the documented set (types.go:661-665); doctor runs under a hard 30 s timeout (doctor.go:324)"
design_invariant = "UNVERIFIED: BinFleet claimed `single-source-of-truth tmux session state accessed atomically via tmux commands`; not grepped (tmux adapter file not located)"
what_we_should_copy = "(1) ExitCoder interface + NormalizeProcessExitCode funnel (root.go:616-625, types.go:662); (2) reality-guards CI jobs that run before anything else; (3) e2e job driving real br+bv binaries on macOS; (4) openapi-drift and upgrade-check matrix jobs"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 4

[[binary]]
repo = "frankenterm"
bin = "ft"
cli_grammar = "clap; `[[bin]] name = ft, path = src/main.rs` crates/frankenterm/Cargo.toml:14-16; src/main.rs is 5,572,581 bytes (ls -la); `Doctor {` variants at main.rs:1100 (`--circuits`) and :8175-8178 (session-persistence doctor, `--format auto|plain|json|toon`); crate `[[test]]` declared :18; subcommand total not counted (grep window covers the first 4 MB only)"
robot_mode = "robot/MCP surface crates: frankenterm-core-mcp, frankenterm-core-resource-types, frankenterm-core-policy-types, frankenterm-core-telemetry-types (ls crates/); doctor `--format` accepts json/toon (main.rs:8176-8178); `--robot-*` flag enumeration not performed"
exit_lattice = "EPIPE -> quiet `exit(141)` = 128 + SIGPIPE via a panic hook installed at the start of main (crates/frankenterm-sigpipe/src/lib.rs:3-14, GH#75); other exit codes not enumerated"
envelope = "UNVERIFIED: BinFleet claimed `{status, data, error:{code,message}}`; no grep was run"
config = "UNVERIFIED: BinFleet self-labelled its config path as inferred; only fuzz/fuzz_targets/config_toml_parser.rs and crates/frankenterm-core-config-types exist by ls — the path was not grepped"
state = "crates/frankenterm-flight-recorder, frankenterm-core-replay, frankenterm-core-replay-types (ls); golden/replay fixtures tests/goldens, tests/renderer_golden, tests/tui_render_parity (ls); frankenterm-core-tantivy for the search index (ls)"
lints = "Cargo.toml:129-133 `[workspace.lints.rust] unsafe_code = forbid`, unreachable_code allow; crate inherits `[lints] workspace = true` (crates/frankenterm/Cargo.toml:22-23); ast-grep rules/no-adhoc-regex.yml + sgconfig.yml and lints/{cache_gpu_handle,cx_propagation} (ls); deny.toml (ls)"
tests_unit = "UNVERIFIED: not sampled by BinRegistry (BinFleet cited only [dev-dependencies] assert_cmd/predicates)"
tests_integration = "tests/ (ls): e2e_workflow_trigger.rs, input_to_photon_bound.rs, test_symexec_redirect.rs, e2e/, bats suites clean_stale_tests.bats, clippy_owned_filter_tests.bats, operator_lock_tests.bats, swarm_tick_tests.bats, robot_work_atomicity_model/, attestation_verify_self_test/, fixtures/, scripts/; crates/frankenterm/Cargo.toml:18 `[[test]]`"
conformance_or_oracle = "real terminal emulators as parity oracles: evidence/{ghostty,rio,zellij} (ls) plus per-bead evidence dirs; tests/tui_render_parity and tests/renderer_golden (ls); external gate in frankentui ci.yml:2189-2190 job `frankenterm-conformance-gates` (FrankenTerm Conformance/Differential/WS Gates)"
property_fuzz = "fuzz/fuzz_targets (ls): bounded_varbincode, byte_compression_frames, config_toml_parser, escape_parser_raw, extension_manifest, ft_argv, fts_query, ftvi_index_from_bytes, ftx_package, ipc_auth_envelope, ...; BinFleet's `NONE identified` refuted"
golden = "tests/golden, tests/goldens, tests/renderer_golden (ls)"
gate_self_test = "tests/attestation_verify_self_test/ (ls); crates/ft-perf-gate (ls); .github/ in the mirror holds only dependabot.yml (ls) so no in-repo CI gate wiring is verifiable here"
ci_jobs = ".github/workflows/ absent in the mirror (ls .github => dependabot.yml only) — BinFleet's `.github/workflows present` refuted for this mirror; downstream gating exists in frankentui ci.yml:2189 frankenterm-conformance-gates"
doctor_selftest = "`ft doctor` main.rs:1100-1103 (`--circuits` shows circuit-breaker status); session-persistence `doctor` main.rs:8175-8178"
install = "install.sh at repo root (ls; BinFleet's `no install.sh` refuted); release/ dir (ls); installer functions not enumerated"
hooks_installed = "UNVERIFIED: BinFleet claimed none; no grep for hook installation was run"
versioning = "Cargo.toml:121-124 workspace version 0.15.2, rust-version 1.95; crates/frankenterm-build-identity used as build-dependency and dependency (crates/frankenterm/Cargo.toml:25-29); CHANGELOG.md (ls)"
fail_closed = "broken pipe becomes a quiet exit 141 instead of a panic/EPIPE trace (sigpipe lib.rs:11-14); the same crate owns the recoverable-panic boundary (:16-17); frankenterm-pty-guardian crate (ls)"
design_invariant = "workspace-wide zero unsafe (Cargo.toml:133) forces the SIGPIPE fix to be a panic hook rather than libc::signal (sigpipe lib.rs:9-11); wire/contract types split into dedicated *-types crates (audit, cass, caut, config, connector, error, policy, replay, resource, telemetry, x11-resize — ls crates/)"
what_we_should_copy = "(1) frankenterm-sigpipe panic-hook pattern for piped CLIs; (2) *-types crate split for stable contracts; (3) a fuzz target per parser (argv, config, escape sequences, index bytes, ipc envelope); (4) evidence/ per-emulator parity directories"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 4

[[binary]]
repo = "process_triage"
bin = "pt-core"
cli_grammar = "clap in crates/pt-core/src/cli.rs (ls); `[[bin]] name = pt-core, path = src/main.rs` crates/pt-core/Cargo.toml:15-17 with `[lib] name = pt_core` (:11-13); repo-root `pt` wrapper script (ls) fronts it; verb modules in src/ by ls: action, agent_init, audit, calibrate, capabilities, collect, daemon, decision, events, fleet, inbox.rs, install, learn, plan, replay, session, supervision, tui, verify.rs; VERSION 2.1.0 (sed)"
robot_mode = "crates/pt-core/src/output/ (ls) and tests cli_formats.rs, cli_help.rs, e2e_agent_{diff_query,explain_sessions,fleet_report,fleet_transfer,priors_inbox,tail_watch}.rs (ls); UNVERIFIED: BinFleet's `--json on every verb` was not grepped"
exit_lattice = "crates/pt-core/src/exit_codes.rs:21-77 enum ExitCode: Clean=0, PlanReady=1, ActionsOk=2, PartialFail=3, PolicyBlocked=4, GoalUnreachable=5, Interrupted=6; user/environment 10-19: ArgsError=10, CapabilityError=11, PermissionError=12, VersionError=13, LockError=14, SessionError=15, IdentityError=16; internal 20-29: InternalError=20, IoError=21, TimeoutError=22; BinFleet's `0 ok / 1 error / 2 usage` refuted"
envelope = "UNVERIFIED: BinFleet claimed `{success, data, error:{code,message}}` in output/mod.rs; the file exists (ls) but was not read"
config = "UNVERIFIED: BinFleet claimed ~/.config/pt/config.toml with SBH_CONFIG override; grep of crates/pt-core/src/config/mod.rs:1-150 for config.toml / SBH_CONFIG / PT_CONFIG matched nothing"
state = "UNVERIFIED: BinFleet cited logger/jsonl.rs, logger/sqlite.rs and supervision/pattern_persistence.rs — crates/pt-core/src/logger does not exist (ls; src/logging and src/supervision do)"
lints = "UNVERIFIED: BinFleet claimed unsafe_code = forbid in pt-core Cargo.toml — no `unsafe_code` in crates/pt-core/Cargo.toml, workspace Cargo.toml, or lib.rs/main.rs:1-40 (grep)"
tests_unit = "crates/pt-core/src/supervision/supervision_tests.rs, src/test_utils.rs, src/test_log.rs, src/mock_process.rs (ls); BinFleet's src/action/prechecks.rs test claim not checked"
tests_integration = "crates/pt-core/tests/ (ls): action_real.rs, advanced_inference_regression.rs, agent_apply_{constraints_blocked,dry_run_ok,nothing_to_do,policy_blocked,precheck_blocked,requires_yes}.rs, agent_plan_policy.rs, alien_integration.rs, automation_mode.rs, baseline_tests.rs, blast_radius_{provenance,tests}.rs, cgroup_throttle_real.rs, cli_e2e_real.rs, cli_errors.rs, cli_formats.rs, cli_help.rs, collect_{net,proc,sys}_real.rs, container_gpu_detection.rs, decision_{nomock,real}.rs, e2e_{action_tray,agent_*,capabilities,cli_scenarios,config_presets,daemon_dormant,degraded_env,metrics}.rs; BinFleet's tests/decision_plane_tests.rs does not exist (ls)"
conformance_or_oracle = "the live OS is the oracle: *_real.rs tests against procfs/sysctl/cgroups (collect_proc_real.rs, collect_sys_real.rs, cgroup_throttle_real.rs, action_real.rs — ls) enforced by ci.yml:109-110 job `no_mock_policy` (No-Mock Policy); musl-test.yml (ls)"
property_fuzz = "fuzz/fuzz_targets (ls): fuzz_bundle_reader, fuzz_config_policy, fuzz_config_priors, fuzz_network_{tcp,udp,unix}, fuzz_proc_{cgroup,environ,io,sched}; tests/decision_properties.rs + decision_properties.proptest-regressions (ls); BinFleet's `NONE identified` refuted"
golden = "UNVERIFIED: BinFleet cited supervision/snapshot_persist.rs and diff.rs as golden stores — snapshot_persist.rs does not exist (ls); no golden dir was located"
gate_self_test = "UNVERIFIED: BinFleet's `fires on: process spawn not detected, signal delivery fails, ledger corruption` cites supervision_tests.rs (exists) but its assertions were not read"
ci_jobs = "ci.yml:16-265 jobs: version-check (Version Consistency), shellcheck, bash_syntax, no_mock_policy, manpages, rust_lint (OS matrix), cargo_test (OS matrix), tests (OS matrix), install-test (OS matrix); other workflows by ls: coverage.yml, musl-test.yml, perf.yml, release.yml, test.yml, update-packages.yml, notify-acfs.yml"
doctor_selftest = "crates/pt-core/src/verify.rs and src/capabilities/ (ls); tests/e2e_capabilities.rs (ls); no `Doctor` variant matched in cli.rs by grep"
install = "install.sh at repo root (ls; BinFleet's `no install.sh` refuted): detect_platform :502-504, main :1233-1236 (with `--emit-skill-payload` path); pt-install-tools/, completions/, Dockerfile, justfile (ls); workspace members crates/pt-{bundle,common,config,core,math,redact,report,telemetry} (ls crates/; Cargo.toml:2-5)"
hooks_installed = "UNVERIFIED: BinFleet claimed none; no grep for hook or service installation was run (src/install/ and src/daemon/ exist by ls)"
versioning = "VERSION 2.1.0 (sed) and crates/pt-core/Cargo.toml:3 version 2.1.0; ci.yml:16-17 version-check job; exit_codes.rs:54-55 VersionError=13 for wrapper/core mismatch; update-packages.yml (ls)"
fail_closed = "exit_codes.rs:33-34 PolicyBlocked=4 (safety gates), :57-58 LockError=14 (another pt instance), :63-64 IdentityError=16 (PID reused since plan), :48-49 CapabilityError=11 (e.g. lsof missing); refusal tests agent_apply_requires_yes.rs, agent_apply_policy_blocked.rs, agent_apply_precheck_blocked.rs, agent_apply_constraints_blocked.rs (ls)"
design_invariant = "UNVERIFIED: BinFleet cited decision_record.rs / decision/mod.rs as the immutable action log — decision_record.rs does not exist (ls; src/decision/ does) and no file was read"
what_we_should_copy = "(1) banded exit lattice: outcomes 0-6, user/env 10-19, internal 20-29 (exit_codes.rs:21-77); (2) no_mock_policy CI job plus *_real.rs oracle tests; (3) agent_apply_* refusal tests for every gate; (4) install-test CI matrix on the shipped installer"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 9

[[binary]]
repo = "storage_ballast_helper"
bin = "sbh"
cli_grammar = "clap: src/main.rs + src/cli_app.rs (ls); `[[bin]] name = sbh, required-features = [cli]` Cargo.toml:13-16; subcommand modules src/cli/{install,uninstall}.rs (ls); verbs `sbh status`, `sbh check` referenced in src/daemon/self_monitor.rs:36"
robot_mode = "UNVERIFIED: BinFleet claimed `--json` with `{success, data, error, errors[]}` in src/cli_app.rs; not grepped"
exit_lattice = "UNVERIFIED: tests/cli_exit_codes.rs exists (ls) but BinFleet's `CliError::exit_code()` was not found in src/cli_app.rs:1-400 (grep) and the code set was not enumerated"
envelope = "UNVERIFIED: same claim as robot_mode; not grepped"
config = "src/core/config.rs: xdg_config_file_for_home :1164-1165 `~/.config/sbh/config.toml`; XDG_CONFIG_HOME override :1177-1180; macos_config_file_for_home :1191-1192; system_config_file :1241-1244 (`/Library` on macOS); documented precedence :1541-1545 (--config, SBH_CONFIG, platform default); root prefers the system config so `sudo sbh` and the service agree (:1274-1276, :1561-1564); unit test of SBH_CONFIG precedence :2912-2914"
state = "src/daemon/self_monitor.rs:4-7 `state.json` is the CLI-to-daemon channel, written atomically (write .tmp then rename) every DAEMON_STATE_WRITE_INTERVAL_SECS = 30 (:30-31); staleness floor >= 2x interval (:33-37); `daemon.lock` advisory exclusive lock released by the kernel on any exit incl. SIGKILL (:71-79, DAEMON_LOCK_FILE_NAME; daemon_lock_path :90-93); PolicyStateRecord observe/canary/enforce/fallback_safe (:40-43); src/logger/ (ls)"
lints = "Cargo.toml:104-107 `[lints.rust] unsafe_code = forbid`, missing_docs = warn, unused = warn; `[lints.clippy]` :108; `[features]` :22-25 (tui deliberately not default; CI builds --no-default-features --features cli,daemon,sqlite)"
tests_unit = "in-crate tests e.g. src/core/config.rs:2912-2914 (SBH_CONFIG precedence via resolve_config_path_from); ci.yml:156-157 `unit`, :180-181 `unit-as-root`, :215-216 `unit-tui` (OS matrix)"
tests_integration = "tests/ (ls): integration_tests.rs, daemon_e2e.rs, fallback_verification.rs, cli_exit_codes.rs, explain_ledger.rs; ci.yml:258-259 `integration`, :343-344 `daemon-e2e` (injected pressure + loop-mounted ext4), :385-386 `decision-plane`, :434-435 `e2e`; BinFleet's tests/decision_plane_tests.rs does not exist (ls)"
conformance_or_oracle = "real filesystems as oracle: daemon-e2e runs on a loop-mounted ext4 with injected pressure (ci.yml:344); unit-as-root (:180-181); linux-arm64 on ubuntu-24.04-arm (:278-280); macos-platform runner matrix (:462-464); macos-benchmarks performance budgets (:1050-1051)"
property_fuzz = "no fuzz/ directory (ls; BinFleet agreed); proptest usage not grepped"
golden = "UNVERIFIED: BinFleet cited tests/decision_plane_tests.rs (nonexistent) and explain_ledger.rs golden outputs (file exists by ls, content not read)"
gate_self_test = "UNVERIFIED: BinFleet's `fires on: config validation fails, daemon lock detection, scoring veto, deletion preflight` was not grepped"
ci_jobs = "ci.yml:34-1259 jobs: check (Format + Lint), homebrew-formula, homebrew-tap-deploy-key-preflight, unit, unit-as-root, unit-tui, integration, linux-arm64, daemon-e2e, decision-plane, dashboard, e2e, macos-platform, macos-coverage, macos-benchmarks (macOS Performance Budgets), stress, artifact-contract, provenance; workflow_call trigger (:12); other workflows by ls: cert-expiration.yml, release.yml"
doctor_selftest = "UNVERIFIED: BinFleet claimed `sbh doctor` (run_doctor in cli_app.rs / src/cli/bootstrap.rs); not grepped"
install = "`sbh install` src/cli/install.rs and `sbh uninstall` src/cli/uninstall.rs (ls); service files from src/daemon/service.rs (ls); install.sh and packaging/ at root (ls); homebrew-formula CI validation (ci.yml:66-68)"
hooks_installed = "launchd / systemd service management (src/daemon/service.rs, ls); no agent-hook directory at root (ls); Homebrew tap deploy (ci.yml:119-122)"
versioning = "Cargo.toml:3 version = 0.5.1; ci.yml:1256-1258 `provenance` (Provenance Metadata, non-tag runs), :1226-1227 artifact-contract; cert-expiration.yml (ls)"
fail_closed = "stale-state floor prevents reporting a dead daemon as live (self_monitor.rs:33-37); kernel-held daemon.lock cannot outlive the process, so SIGKILL leaves no false lock (:73-75); protection rules module src/scanner/protection.rs (ls); scoring engine src/scanner/scoring.rs (ls, ArtifactCertainty/posterior floor per BinFleet not read)"
design_invariant = "CLI and daemon communicate only through an atomically-replaced state.json plus a kernel-held advisory lock (self_monitor.rs:4-7, 71-79); root and the service read the same system config so status and enforcement agree (config.rs:1274-1276, 1561-1564)"
what_we_should_copy = "(1) atomic state.json + kernel advisory lock liveness protocol; (2) XDG/macOS/system config resolution with the root-prefers-system rule; (3) daemon-e2e on a loop-mounted ext4 and unit-as-root CI jobs; (4) macOS performance-budget CI job"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 6

[[binary]]
repo = "destructive_command_guard"
bin = "dcg"
cli_grammar = "clap src/cli.rs: `Doctor { fix }` :202-206, `Hook(HookCommand)` :224-225, `Allowlist { action }` :228-232, `Install { force }` :311-315, `Scan(ScanCommand)` :565-566, `Explain { command }` :587-590, `Check { json }` :1473-1477; `[[bin]] name = dcg, path = src/main.rs` Cargo.toml:32-34; src/{agent,allowlist,ast_matcher,evaluator,heredoc,normalize,packs/,sarif,scan,simulate,suggest,trace,update}.rs (ls)"
robot_mode = "`dcg check --json` (src/cli.rs:1475-1477); src/output/ (ls); SARIF via src/sarif.rs (ls); tests/agent_json_format.rs and tests/golden/robot (ls)"
exit_lattice = "src/exit_codes.rs:40-80: EXIT_SUCCESS=0, EXIT_DENIED=1, EXIT_WARNING=2 (with --fail-on warn), EXIT_CONFIG_ERROR=3, EXIT_PARSE_ERROR=4, EXIT_IO_ERROR=5; BinGates' `PARSE_ERROR=2` refuted; hook path: Codex denials use stdout JSON + exit 0 to avoid the legacy exit-2 route (src/hook.rs:399-403); Posit Assistant reads permissionDecision on exit 0, exit 2 blocks with stderr (:389-391)"
envelope = "hook stdout `{hookSpecificOutput: {hookEventName: PreToolUse, permissionDecision: allow|deny|ask, permissionDecisionReason, allowOnceCode, ruleId, packId, severity, remediation}}` (src/hook.rs:154-175 HookOutput/HookSpecificOutput; asserted :4024-4031, :5057-5061); Copilot flat `{permissionDecision, permissionDecisionReason}` (:216-222, :4044); Gemini `{decision, reason}` (:3607-3608); HookInput has `event` and Codex `turn_id` alias turnId (:22-24, :62-66)"
config = "src/config.rs (ls); config.schema.json at repo root (ls); tests/config_schema_drift.rs, config_toggles_e2e.rs, config_error_repro.rs (ls); config path not grepped"
state = "allow-once pending exceptions src/pending_exceptions.rs (ls; codes surfaced in the reason text hook.rs:1731-1735); history src/history/ with tests history_full_flow.rs, history_integration.rs, history_legacy_compat.rs and history-e2e.yml (ls); src/session.rs, src/stats.rs (ls)"
lints = "Cargo.toml:189-190 `[lints.rust]` with `# unsafe_code = forbid` commented out and moved to src/lib.rs and src/main.rs to allow unsafe in tests; :192-193 `[lints.clippy] all = warn`; rust-toolchain.toml pins nightly / edition 2024 (ci.yml:171)"
tests_unit = "src/hook.rs in-file tests: #339 reason-size cap (:3884-3887), #332 allow-once code named in reason (:3927-3930), ask decision shape (:4023-4046), protocol misclassification regression (:4880-4884), per-protocol verdict extraction (:5319-5331, :5376-5388, :5443-5455)"
tests_integration = "tests/ 109 entries (ls): agent_hook_output.rs, agent_exit_codes.rs, agent_json_format.rs, agent_profile_comprehensive.rs, codex_hook_protocol.rs, cli_e2e.rs, allowlist_command_tests.rs, config_schema_drift.rs, containers_pack_comprehensive.rs, cross_pack_corpus.rs, e2e_real_service.rs, git_bypass.rs, graduated_response.rs, heredoc_pack_gap.rs, history_*.rs, tests/install/{install_test,uninstall_test,agent_config_test}.bats, tests/e2e/, tests/corpus/, tests/fixtures/; BinGates' tests/{cli,evaluator,normalize,packs}.rs do not exist (ls)"
conformance_or_oracle = "real agent harnesses are the oracle: ci.yml:927-930 job `codex-e2e` (networked Codex API, push-to-main only); tests/codex_hook_protocol.rs and tests/e2e_real_service.rs (ls); protocol variants ClaudeCompatible / Codex / Copilot / Gemini / Hermes with wire-shape assertions (src/hook.rs:382-403, 5319-5331); ci.yml:1513 published-release-matrix"
property_fuzz = "fuzz/fuzz_targets (ls); ci.yml:409-411 `fuzz-smoke` on every run and :1449-1452 `fuzz` deep suite on schedule/manual"
golden = "tests/golden/{artifacts,hook,robot} (ls); tests/golden_artifacts.rs, golden_isomorphism.rs, golden_json_tests.rs (ls); ci.yml:1066-1067 `scan-regression` fixtures keep scan output stable"
gate_self_test = "tests/agent_exit_codes.rs + agent_hook_output.rs (ls) assert verdicts per harness; ci.yml:1142-1144 `perf-regression` against committed baseline JSON, :604 `memory-tests`, :720-723 `benchmarks` against src/perf.rs budgets; BinGates' job names test-omp-robot-boundary / executable-bridge / phase-3-e2e-codex are not in ci.yml"
ci_jobs = "ci.yml:15-1516 jobs: check, check-windows, fuzz-smoke, coverage, memory-tests, benchmarks, e2e, bats, codex-e2e, scan-regression, perf-regression, fuzz, published-release-matrix; other workflows by ls: acfs-checksums-dispatch, bench, cli-version-audit, dcg-scan, dist, history-e2e, notify-acfs, release-automation, scan; BinGates' lint / test-release / windows-test / docker / e2e-phase-* not present"
doctor_selftest = "`dcg doctor --fix` src/cli.rs:202-206; `dcg check --json` :1473-1477; codecov.yml, agent_baseline/ (ls)"
install = "install.sh (ls): detect_platform :805-808, verify_checksum :1265-1268; install.ps1, uninstall.sh, uninstall.ps1 (ls); bats suite tests/install/ (ls) run by ci.yml:869-871 `bats`; BinGates' install_unix / detect_shell / validate_config / install_packs / install_hook not found (grep)"
hooks_installed = "`dcg install --force` (src/cli.rs:311-315) and `dcg hook` (:224-225) wire the PreToolUse hook; harness detection via HookProtocol (src/hook.rs:382-403) incl. PA_PROJECT_DIR env (:392-393, :915-919); tests/install/agent_config_test.bats (ls); action/ dir for GitHub Action usage (ls)"
versioning = "Cargo.toml:3 version = 0.14.0; cli-version-audit.yml (ls); src/update.rs (ls); release-automation.yml + dist.yml (ls)"
fail_closed = "Codex denials are emitted as exit-0 stdout JSON because Codex 0.144.x classified exit-2 as a failed hook and failed OPEN (src/hook.rs:399-403); Posit Assistant tool-name gate keeps deny payloads readable (:915-919); CLI `check` returns EXIT_DENIED=1 on any block (exit_codes.rs:42-48) and EXIT_WARNING=2 under --fail-on warn (:50-56)"
design_invariant = "permissionDecisionReason is the only field every harness surfaces to the model, so verdict, rule id and allow-once code are carried in the reason text and its size is capped (src/hook.rs:1649-1655 #339, 1731-1735 #332)"
what_we_should_copy = "(1) per-harness protocol enum with wire-shape tests (hook.rs:382, 5319); (2) golden hook/robot artifacts plus golden_isomorphism tests; (3) fuzz-smoke on every run plus scheduled deep fuzz; (4) bats install/uninstall/agent-config tests; (5) banded exit codes with a warning tier (exit_codes.rs:40-80)"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 0

[[binary]]
repo = "remote_compilation_helper"
bin = "rch"
cli_grammar = "clap: rch/src/main.rs (`#![forbid(unsafe_code)]` :7; `pub mod agent` :9); `[[bin]] name = rch, path = src/main.rs` rch/Cargo.toml:8-10; command modules rch/src/commands/ (ls): agents, config, config_doctor, config_init, daemon, helpers, hook, init, queue, rabs_gc, shim, speedscore, status, sync, types, why, workers, workers_deploy, workers_init, workers_setup; rch/src/{completions,doctor,doctor_webhooks,status_display,status_types,toolchain,transfer}.rs (ls)"
robot_mode = "JSON output shapes pinned in rch/tests/goldens/speedscore_list_status_shapes.json and top_level_daemon_parse_error_api_error.json (ls); rch-common/src/exec_response.rs and errors/ (ls); `--json` flag spelling not grepped"
exit_lattice = "rch/src/hook.rs:102-132: EXIT_SUCCESS=0, EXIT_BUILD_ERROR=1, EXIT_TEST_FAILURES=101 (cargo test ran, some failed), EXIT_SIGNAL_BASE=128 (128 + signal, e.g. 137 SIGKILL), EXIT_ARTIFACT_TRANSFER_FAILED=102 (remote compile succeeded, artifacts not returned; pairs with RCH-E309 BuildArtifactMissing), EXIT_REMOTE_REQUIRED_REFUSED=103 (retryable fail-closed refusal: no worker assignable / daemon down)"
envelope = "top-level daemon parse-error / api-error JSON golden rch/tests/goldens/top_level_daemon_parse_error_api_error.json (ls); rch-common/src/exec_response.rs (ls); field shape not read"
config = "rch/src/config.rs:17 `RCH_CONFIG_DIR` env; resolution :35-46 (bd-9fgeu): RCH_CONFIG_DIR, then docs-canonical `$XDG_CONFIG_HOME/rch` else `~/.config/rch` when it exists (macOS ProjectDirs previously orphaned `~/.config/rch/config.toml` :39-42), then an existing platform ProjectDirs dir; project-local `.rch/config.toml` (:108); config cache invalidated by source metadata, schema bump, `RCH_DISABLE_CONFIG_CACHE=1`, or corrupt cache (:107-111)"
state = "rch/src/state/, cache.rs, cache_gc.rs (ls); daemon crate rchd + rch/src/hook/daemon_ipc.rs (ls); rch/src/hook/timing_history.rs, rabs_recorder.rs, source_fidelity.rs (ls); rabs-* crates for the RABS artifact system (ls root: rabs-action, rabs-asupersync, rabs-cas, rabs-key, rabs-protocol, rabs-replay, rabs-sandbox, rabs-scheduler, rabs-wkr, rabs-wrap, rabsd); receipts/ (ls)"
lints = "rch/src/main.rs:7 `#![forbid(unsafe_code)]`; workspace Cargo.toml has no [workspace.lints] unsafe_code line (grep of Cargo.toml); rust-toolchain.toml (ls); ci.yml:96-99 `clippy` job with strict warnings, :84 `fmt`"
tests_unit = "rch/src/hook/tests.rs (ls) alongside the hook submodules artifact_patterns, auto_start, cargo_target_dir, command_parsing, dependency_closure, fallback_selection, remote_result, selection_response, transfer_orchestration (ls rch/src/hook/)"
tests_integration = "rch/tests/ (ls): audit_e2e.rs, compile_context.rs, e2e_fleet.rs, e2e_hook.rs, hook_fail_open.rs, hook_integration.rs, integration_suite.rs, rustc_wrapper_fingerprint_fixture.rs, stream_isolation.rs, ui_integration.rs, verbose_flag_integration.rs, common/, e2e/, integration/, support/; root tests/ (ls): e2e/, goldens/, true_e2e/, manifest_sync_test.sh; feature-gated `[[test]] true_e2e` and `true_e2e_ssh` (rch/Cargo.toml:102-110, required-features true-e2e)"
conformance_or_oracle = "real remote workers over SSH are the oracle: ci.yml:190-193 job `e2e` runs `--features true-e2e`; tests/true_e2e/ssh_tests.rs (rch/Cargo.toml:107-110); wrapper-contract.yml (ls); rustc_wrapper_fingerprint_fixture.rs (ls); ci.yml:241-244 `benchmark` verifies latency budgets from AGENTS.md"
property_fuzz = "no fuzz/ directory at repo root (ls); proptest usage not grepped"
golden = "rch/tests/goldens/ (2 JSON goldens, ls) and root tests/goldens/ (ls)"
gate_self_test = "rch/tests/hook_fail_open.rs (ls) pins the fail-open/fail-closed boundary of the cargo hook; ci.yml:46-49 `manifest_sync` validates the ACFS manifest, :73 `workflow_lint`, :241 `benchmark` latency budgets, :400-403 `ci-success` requires check+fmt+clippy+security+test+e2e+build-release"
ci_jobs = "ci.yml:32-403 jobs: check, manifest_sync, workflow_lint, fmt, clippy, security (weekly cron :7-8), docs, test (matrix), e2e, benchmark, build-release (matrix), coverage, ci-success; other workflows by ls: e2e.yml, rabs-binaries.yml, release-automation.yml, release.yml, test-release.yml, wrapper-contract.yml, dependabot-automerge.yml, notify-acfs.yml"
doctor_selftest = "rch/src/doctor.rs, doctor_webhooks.rs, commands/config_doctor.rs, self_healing_overrides.rs (ls); rch-common/src/capability_probe.rs, fleet_smoke_profile.rs, fleet_status.rs, disk_pressure_report.rs (ls)"
install = "install.sh (ls) functions: setup_ui :76, spin :149, confirm :161, draw_box :183, show_header :286, systemd_user_available :325, system_rchd_service_active :338, detect_service_manager :346, ensure_command :368, acquire_lock :378, cleanup_lock :392, detect_platform :400; workspace members rch, rchd, rch-wkr (Cargo.toml:3-6) plus rch-common, rch-telemetry, rabs-* (ls); packaging/, manifest_index.sh (ls)"
hooks_installed = "cargo wrapper hook rch/src/hook.rs (bypass env RCH_CARGO_WRAPPER_BYPASS :134, RCH_REQUIRE_REMOTE :135); rch/src/commands/{hook,shim}.rs (ls); rch-common/src/hooks.rs (ls); user service via systemd/launchd (install.sh detect_service_manager :346); skills/{building-glamorous-tuis,crafting-readme-files} (ls); rch-telemetry crate (ls)"
versioning = "Cargo.toml:27-28 workspace version = 1.0.62 (rch/Cargo.toml:3 version.workspace); rch/src/update/ (ls); rch-common/src/fleet_provenance.rs, binary_hash.rs, classifier_drift.rs (ls); release.yml, release-automation.yml, test-release.yml (ls)"
fail_closed = "with RCH_REQUIRE_REMOTE set and no worker assignable the build is refused with EXIT_REMOTE_REQUIRED_REFUSED=103 rather than silently running locally (rch/src/hook.rs:125-132); admission gate in rch-common/src/{admission_rejection,admission_recommendation,admit_preflight}.rs (ls; `admission_rejection` does not occur under rch/src by grep); exec policy rch-common/src/{exec_policy,exec_misuse}.rs (ls); boundary test rch/tests/hook_fail_open.rs (ls)"
design_invariant = "the hook separates infrastructure state from compile state: retryable refusal (103) vs build failure (1) vs artifact transfer failure after remote success (102) so callers never mistake a fleet problem for a compiler error (rch/src/hook.rs:116-132)"
what_we_should_copy = "(1) three-way exit split infra/compile/transfer (hook.rs:102-132); (2) config-dir resolution honouring the docs-canonical XDG path before platform dirs (config.rs:35-46); (3) true-e2e feature-gated tests against real SSH workers; (4) hook_fail_open.rs boundary test plus committed JSON shape goldens"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 0

[[binary]]
repo = "ultimate_bug_scanner"
bin = "ubs"
cli_grammar = "Bash meta-runner script `ubs` at repo root (ls); AGENTS.md:301-305 usage: `ubs --only=rust,toml src/`, `ubs --ci --fail-on-warning .`, `ubs .` (respects .ubsignore), `ubs -v .`, `ubs doctor --fix`; language modules modules/ubs-{cpp,csharp,elixir,golang,java,js,python,ruby,rust,swift}.sh + modules/lib/ubs-common.sh + modules/helpers/ (ls)"
robot_mode = "output formats text, JSON, JSONL, SARIF, TOON (AGENTS.md:189); `--ci` strict mode (:302); flag parsing in the ubs script not grepped"
exit_lattice = "UNVERIFIED: BinGates claimed 0 clean / 2 error-partial / 3 no-scan; grep of ubs:1-200 and header-comment patterns across the script found no exit-code documentation"
envelope = "UNVERIFIED: BinGates cited AGENTS.md:189-280 for the finding schema (severity, confidence, category ids, ubs:ignore suppression); only line 189 was read"
config = "UNVERIFIED: no scout claim; only `.ubsignore` is mentioned (AGENTS.md:303) and no config grep was run"
state = "cached language modules verified/repaired by `ubs doctor --fix` (AGENTS.md:305); UNVERIFIED: BinGates' cache path `~/.local/share/ubs/modules` not grepped"
lints = "ci.yml:320-321 job `lint` (contents not read); Bash project — no Rust lint table; pyproject.toml + uv.lock for Python helpers (ls)"
tests_unit = "fixture corpus test-suite/ (ls): buggy/, clean/, edge-cases/, frameworks/, quality/, realistic/, per-language dirs cpp, csharp, elixir, golang, java, js, kotlin, python, ruby, goldens/, manifest.json, run_all.sh, banner_tester.sh, install/"
tests_integration = "ci.yml:116-119 `manifest` job runs the 447-case manifest sharded four ways (run_manifest.py --shard I/N) to keep the PR gate under ten minutes; test-suite.yml:77-78 `tests` full nightly run (cron :10); ci.yml:168 `docker-smoke`, :188-191 `macos-smoke` (bash 3.2 + BSD userland)"
conformance_or_oracle = "test-suite/manifest.json expected findings over the buggy/clean/realistic corpus is the oracle (ls); ci.yml:259-262 `goldens-bot` regenerates rule-coverage and SARIF-evidence goldens when rules or fixtures change; test-suite.yml:22-26 `bench` compares against benchmarks/baseline.json via scripts/bench_cusum.py (CUSUM)"
property_fuzz = "no fuzz directory at repo root (ls); none claimed by scouts"
golden = "test-suite/goldens/ (ls); ci.yml:259 goldens-bot (rule-coverage + SARIF-evidence goldens)"
gate_self_test = "test-suite.yml:22-26 CUSUM benchmark regression vs benchmarks/baseline.json; scripts/check_docs_claims.py, check-version-tag-drift.sh, verify_sha256sums.sh, verify_checksums.sh (ls); checksum-health.yml + checksum-sync.yml (ls)"
ci_jobs = "ci.yml:20-323 jobs: build, test, manifest, docker-smoke, macos-smoke, goldens-bot, lint; test-suite.yml: bench, tests; manifest.yml: noop (dispatch-only); other workflows by ls: acfs-checksums-dispatch, checksum-health, checksum-sync, nix, notify-acfs, oci, release-artifacts, release"
doctor_selftest = "`ubs doctor --fix` verifies/repairs cached modules (AGENTS.md:305); UNVERIFIED: BinGates' installer `doctor self-test` feature not grepped"
install = "install.sh (ls): detect_platform :826-829, main :3930 (saves original args for re-exec during update); SHA256SUMS at root + scripts/{update_sha256sums,verify_sha256sums,verify_checksums}.sh (ls); flake.nix + nix.yml (ls); Dockerfile + oci.yml (ls); UNVERIFIED: BinGates' cosign verify / --easy-mode / --from-source flags not grepped"
hooks_installed = "SKILL.md at root (ls); consumed as a pre-merge gate by other repos (coding_agent_session_search ci.yml:203-204 `ubs-changed-files` UBS Pre-Merge Gate; ci_workflow_validates_ubs_gate.rs there); no hook installer grepped"
versioning = "VERSION 5.3.13 (sed); scripts/check-version-tag-drift.sh, cut-release.sh (ls); release.yml, release-artifacts.yml (ls); CHANGELOG.md (ls)"
fail_closed = "`--ci --fail-on-warning` strict mode (AGENTS.md:302); checksum verification scripts and checksum-health workflow (ls); UNVERIFIED: BinGates' `exit 3 = no-scan` (empty scan set as error) not grepped"
design_invariant = "one Bash meta-runner dispatches per-language modules concurrently and merges findings into a common contract with shared lib (AGENTS.md:189; modules/lib/ubs-common.sh by ls); UBS cannot yet scan Bash, the language it is written in (AGENTS.md:189, bead ultimate_bug_scanner-7mga.1)"
what_we_should_copy = "(1) manifest.json expected-findings oracle sharded in CI; (2) goldens-bot auto-regeneration of derived goldens; (3) CUSUM benchmark regression against a committed baseline; (4) macos-smoke job pinning bash 3.2 + BSD userland"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 7

[[binary]]
repo = "coding_agent_session_search"
bin = "cass"
cli_grammar = "clap in src/main.rs (ls); `[[bin]] name = cass, path = src/main.rs, test = false` Cargo.toml:226-229 plus helper bins cass-pages-perf-bundle (:231-233) and e2e-run-bundle (:236-238); package `coding-agent-search` (:2); tests cli_dispatch_coverage.rs, cli_flag_precedence.rs, cli_index.rs, cli_search_semantic_flags.rs, cli_status.rs, cli_stats_source_filter.rs (ls); BinGates' src/main.rs:300-400 verb list not read"
robot_mode = "src/robot_budget_envelope.rs, recipes_robot_docs.rs, doctor_robot_docs.rs (ls); tests cli_robot.rs, cli_robot_log_hygiene.rs (ls); BinGates' `robot-budget-envelope` / `query-cost-planner` are source modules (src/query_cost_planner.rs by ls), not Cargo features (grep of Cargo.toml)"
exit_lattice = "UNVERIFIED: no scout claim; grep for EXIT_ consts / ExitCode enum in src/main.rs found nothing and src/robot.rs does not exist"
envelope = "UNVERIFIED: BinGates cited src/lib.rs:50-150 for `{version, status, data{results, metadata}, error{code,message}}`; lib.rs:1-12 is attributes + `pub mod` list and the range was not read"
config = "UNVERIFIED: no scout claim and no grep run"
state = "frankensqlite store: Cargo.toml:73 `frankensqlite = =0.3.14 (package fsqlite, features fts5, async-api)`, :74-76 fsqlite-types for the storage-integrity probe, :216-218 rusqlite retained only for C-SQLite interop fixtures; search via frankensearch =0.4.2 with native embedder/reranker (:115-126); src/storage/, src/indexer/, src/search/ (ls); tests atomic_swap_publish_crash_window.rs (ls)"
lints = "src/lib.rs:5-7 `#![cfg_attr(not(test), deny(unsafe_code))]` with sanctioned `#[allow(unsafe_code)]` + SAFETY sites (startup env writes, FFI); no `unsafe_code` in Cargo.toml (grep) — BinGates' `unsafe_code=forbid` refuted; deny.toml, audit.toml (ls); ci.yml:164-165 `lint`"
tests_unit = "UNVERIFIED: in-crate unit tests not sampled (tests/ has 274 entries by ls | wc)"
tests_integration = "tests/ 274 entries (ls): cli_*.rs, connector_*.rs (aider, amp, antigravity, chatgpt, ...), bakeoff_harness.rs, concurrent_search.rs, agent_detection_completeness.rs, cass_256_baseline_feature_gate.rs, cass_257_*.rs, ci_workflow_validates_ubs_gate.rs, atomic_swap_publish_crash_window.rs, accessibility/; BinGates' tests/{indexer,search,doctor,analytics}.rs do not exist (ls)"
conformance_or_oracle = "real agent session corpora per connector (tests/connector_*.rs by ls; franken-agent-detection =0.2.2 with connector features Cargo.toml:127); ci.yml:446-447 `ssh-sync-docker` (SSH Sync Docker Tests), :653-654 `crypto-vectors` (Crypto Test Vectors), :505-506 `e2e-orchestrated`; tests/canonicalize_equivalence.rs (ls)"
property_fuzz = "fuzz/fuzz_targets (ls): chunked, config, decrypt, fuzz_cli_argv, fuzz_connectors, ...; fuzz.yml (ls); proptest: tests/perf_proptest.rs, spec_crypto_roundtrip_proptest.rs, canonicalize_equivalence.proptest-regressions (ls)"
golden = "tests/golden/ (ls); src/proof_artifact.rs, evidence_bundle.rs, perf_evidence.rs (ls)"
gate_self_test = "ci.yml:34-35 `no-mock-audit` (No-Mock Policy Audit) gates every job; :203-204 `ubs-changed-files` UBS pre-merge gate; tests/ci_workflow_validates_ubs_gate.rs (ls); src/doctor_chokepoint.rs, metric_integrity.rs (ls)"
ci_jobs = "ci.yml:34-766 jobs: no-mock-audit, lint, ubs-changed-files, test-rust (OS matrix), ssh-sync-docker, e2e-orchestrated, e2e-tui-matrix, crypto-vectors, security, build (target matrix), e2e-log-summary; other workflows by ls: bench, browser-tests, coverage, fresh-clone-build, fuzz, install-test, lighthouse, perf, release, acfs-checksums-dispatch, notify-acfs, ubs-version.txt"
doctor_selftest = "src/doctor.rs, doctor_chokepoint.rs, doctor_recover.rs, doctor_robot_docs.rs, doctor_runs.rs, doctor_undo.rs, source_doctor_health.rs, fleet_doctor_schema.rs (ls); tests cli_doctor.rs, cli_doctor_budget.rs (ls)"
install = "install.sh + install.ps1 at root (ls); install-test.yml (ls); packaging/ (ls); dsr ships installers/cass/ (ls doodlestein_self_releaser/installers); installer functions not enumerated"
hooks_installed = "SKILL.md at root (ls); ubs-version.txt pin in .github/workflows (ls); src/franken_sync.rs, src/daemon/ (ls); no agent-hook installer grepped"
versioning = "Cargo.toml:3 version = 0.7.1; exact pins frankensqlite =0.3.14, frankensearch =0.4.2, franken-agent-detection =0.2.2 aligned to the asupersync 0.4.x line (:283-285); src/update_check.rs, release_verify.rs, fleet_version_skew.rs, dependency_drift.rs (ls); build.rs (ls)"
fail_closed = "no-mock-audit job is a `needs:` prerequisite of lint/ubs/ssh-sync jobs (ci.yml:166, 205, 448); ubs pre-merge gate fails on regressions vs base versions (:202-204); src/atomic swap crash-window test (tests/atomic_swap_publish_crash_window.rs by ls)"
design_invariant = "pure-Rust stack pinned by exact version: production storage and salvage use frankensqlite, never rusqlite (Cargo.toml:216-218); semantic search is always-on pure Rust with no AVX2 static-init hazard (:182-188, cass#256/#308)"
what_we_should_copy = "(1) no-mock-audit as a `needs:` gate for every job; (2) consuming ubs as a changed-files pre-merge gate with a version pin file; (3) `cfg_attr(not(test), deny(unsafe_code))` with SAFETY-commented allows; (4) exact `=x.y.z` pins across the franken* constellation"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 4

[[binary]]
repo = "asupersync"
bin = "asupersync"
cli_grammar = "Cargo.toml:344-376 seven `[[bin]]` entries: asupersync (src/bin/asupersync.rs, feature cli), offline_tuner (cli + simd-intrinsics), dependency_marginal_ledger (dependency-ledger), generate_rfc6330_vectors (scripts/), atpd (atpd-daemon), atp (atp-cli), channel_mpsc_select_e2e; BinFoundations' `6 binaries` undercounts; library-first crate (description :26 `Spec-first, cancel-correct, capability-secure async runtime`)"
robot_mode = "UNVERIFIED: no scout claim and no grep run for --json/robot flags on the asupersync/atp CLIs"
exit_lattice = "UNVERIFIED: no scout claim and no grep run"
envelope = "core result type `pub enum Outcome<T, E> {` src/types/outcome.rs:218 (Ok | Err | Cancelled per BinFoundations; variants not read); capability context `pub struct Cx<Caps = cap::All> {` src/cx/cx.rs:235"
config = "UNVERIFIED: no scout claim and no grep run"
state = "UNVERIFIED: runtime, no persisted state claimed; not grepped"
lints = "Cargo.toml:939-943 `[lints.rust] unsafe_code = deny` (comment: default deny; the epoll reactor's polling FFI uses `#[allow(unsafe_code)]`), unexpected_cfgs declares cfg(loom) / cfg(fuzzing) (:942); :952-955 `[lints.clippy] pedantic + nursery = warn`; BinFoundations' `Cargo.toml:1` cite refuted"
tests_unit = "UNVERIFIED: in-crate unit tests not sampled"
tests_integration = "tests/ 1463 entries (ls | wc) incl. declared `[[test]]` kafka_real_broker (tests/integration/kafka_real_broker.rs, Cargo.toml:630-631), lock_contention_atlas_contract (:634-635), timer_wheel_advance_invariants (tests/metamorphic/, :638-639), atp_quic_bounded_memory (:642-644, features tls + test-internals), atp_autotune_safety_regression (:897-898), offline_tuner_env_logger_parity (:905-907)"
conformance_or_oracle = "conformance/ (ls): quic/ (RFC 9000), raptorq_rfc6330/ with REQUIREMENTS_MATRIX.json (ls), http2_h2spec.md, src/, tests/, artifacts/; .github/workflows/conformance.yml, tokio_parity_dashboard_drift.yml, nightly-differential-stress.yml (ls); ATP_DOD_CHECKLIST.md (ls)"
property_fuzz = "fuzz/fuzz_targets (ls); .github/workflows/fuzz.yml and property-tests.yml (ls); tests/metamorphic/ (Cargo.toml:639); cfg(fuzzing) + cfg(loom) declared (:942)"
golden = "bench `golden_output` (Cargo.toml:745-747, harness = false); conformance/artifacts/ (ls)"
gate_self_test = ".github/workflows/methodology-gates.yml and perf-pr-check.yml (ls); e2e hardening passes e2e_hardening_{7,8,9,10,11,12,13,14,15}_analysis.md + e2e_hardening_summary.md at root (ls); atp-proof-lanes.yml formal lane (ls)"
ci_jobs = ".github/workflows/ 12 files (ls): atp-proof-lanes, benchmarks, ci, conformance, fuzz, methodology-gates, nightly-differential-stress, pages, perf-pr-check, property-tests, publish, tokio_parity_dashboard_drift; per-file job ids not enumerated"
doctor_selftest = "UNVERIFIED: no scout claim and no grep run"
install = "library crate consumed via Cargo (mcp_agent_mail_rust, coding_agent_session_search, remote_compilation_helper rabs-asupersync by ls); publish.yml (ls); no install.sh checked"
hooks_installed = "none applicable (library + benches); atpd daemon binary exists (Cargo.toml:363-366)"
versioning = "Cargo.toml:22-23 version = 0.4.9, edition 2024; consumers pin the 0.4.x line (coding_agent_session_search Cargo.toml:283-285); publish.yml (ls)"
fail_closed = "UNVERIFIED: cancel-correctness claims (Outcome::Cancelled, region/cancellation publication) come from BinFoundations and the type bodies were not read"
design_invariant = "capability-secure context `Cx<Caps = cap::All>` (src/cx/cx.rs:235) and three-way `Outcome<T, E>` (src/types/outcome.rs:218) are the two core types; unsafe is deny-by-default with FFI-only allows (Cargo.toml:939-943)"
what_we_should_copy = "(1) conformance/ directory with a machine-readable REQUIREMENTS_MATRIX.json per RFC; (2) methodology-gates + perf-pr-check workflows; (3) numbered e2e_hardening_N passes against the shipped artifact; (4) tokio parity drift dashboard as an external oracle"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 7

[[binary]]
repo = "frankentui"
bin = "doctor_frankentui"
cli_grammar = "library workspace (crates/ftui facade) with one operational binary crate crates/doctor_frankentui (ls; verified by ci.yml:1154-1155 job `doctor-frankentui-verification` and doctor_frankentui_extended.yml by ls); 20 workspace crates by ls: doctor_frankentui, ftui, ftui-a11y, ftui-backend, ftui-core, ftui-demo-showcase, ftui-extras, ftui-harness, ftui-i18n, ftui-layout, ftui-pty, ftui-render, ftui-runtime, ftui-showcase-wasm, ftui-simd, ftui-style, ftui-text, ftui-tty, ftui-web, ftui-widgets (BinFoundations listed ftui-wasm; the crate is ftui-showcase-wasm); Cargo.toml:3-6 members"
robot_mode = "UNVERIFIED: no scout claim and no grep run"
exit_lattice = "UNVERIFIED: no scout claim and no grep run"
envelope = "UNVERIFIED: no scout claim and no grep run"
config = "UNVERIFIED: no scout claim; demo.yaml at root (ls) not read"
state = "UNVERIFIED: library; no persisted-state claim and no grep run"
lints = "crates/ftui/src/lib.rs:1 `#![forbid(unsafe_code)]`; crates/ftui-core/src/lib.rs:2-3 `#![cfg_attr(not(test), forbid(unsafe_code))]` + `#![cfg_attr(test, deny(unsafe_code))]`; no unsafe_code line in workspace Cargo.toml (grep); ci.yml:2278-2279 `msrv`, :28-29 `toolchain-pin` (rust-toolchain.toml sync)"
tests_unit = "UNVERIFIED: not sampled (BinFoundations cited crates/ftui-*/tests/ generically)"
tests_integration = "tests/ at root (ls); ci.yml:414-415 `e2e-widget-api` (Widget API E2E), :566-567 `e2e-pty` (PTY E2E, OS matrix), :476-477 `demo-showcase`"
conformance_or_oracle = "terminal emulators as oracle: .github/workflows/emulator_compat_matrix.yml (ls); ci.yml:2189-2190 `frankenterm-conformance-gates` (FrankenTerm Conformance/Differential/WS Gates); :752-753 `golden-trace-gates`"
property_fuzz = "fuzz/Cargo.toml + fuzz/fuzz_targets (ls); ci.yml:2125-2126 `fuzz` (Fuzz Build Check)"
golden = "ci.yml:752-753 `golden-trace-gates` (Golden Trace Gates); :2343-2344 `pane-perf-artifacts` (Pane Perf Replay Artifacts)"
gate_self_test = "slo.yaml (293 lines by wc): `safe_mode_trigger: true = breach triggers safe-mode immediately` (:15), global `regression_threshold: 0.10`, `noise_tolerance: 0.05`, `safe_mode_breach_count: 3`, `safe_mode_error_rate: 0.10` (:18-21), safe_mode_trigger on render_frame_p99/p999 (:45, :51), allocations/heap (:153), error rates (:168, :183), runtime_first_frame_p99_us (:189), :223; ci.yml:2522-2523 `perf-rollout-gates` (tail monitors / drills / scorecard); :1154 doctor-frankentui-verification"
ci_jobs = "ci.yml jobs: toolchain-pin :28, check :48 (OS matrix), docs :124, features :157, coverage :199, benchmarks :332 (main only), docs-build :377, e2e-widget-api :414, demo-showcase :476, e2e-pty :566, golden-trace-gates :752, doctor-frankentui-verification :1154, fuzz :2125, frankenterm-conformance-gates :2189, msrv :2278, wasm :2298, pane-perf-artifacts :2343, perf-rollout-gates :2522; other workflows by ls: doctor_frankentui_extended.yml, emulator_compat_matrix.yml, release.yml; BinFoundations' `4/5 jobs` undercounts"
doctor_selftest = "crates/doctor_frankentui (ls) verified in CI by ci.yml:1154-1155 and doctor_frankentui_extended.yml (ls)"
install = "no install.sh at root (ls); library consumed via Cargo (storage_ballast_helper Cargo.toml:23-25 references the frankentui TUI); build-wasm.sh, Makefile (ls)"
hooks_installed = "none applicable (library); scripts/ dir exists (ls) but scripts/check.sh does not (ls)"
versioning = "Cargo.toml:27-29 workspace version = 0.6.0, edition 2024; ci.yml:28-29 toolchain-pin job; release.yml (ls)"
fail_closed = "SLO breach flips the running product into safe mode, not just CI red: slo.yaml:15 safe_mode_trigger semantics, :20 safe_mode_breach_count = 3, :21 safe_mode_error_rate = 0.10"
design_invariant = "performance budgets are data (slo.yaml p50/p95/p99/p999 latency, memory, error-rate rows with max_ratio vs baseline and safe_mode_trigger — :14-15, :44-51) enforced by perf-rollout-gates and golden-trace-gates in CI (ci.yml:752, 2522)"
what_we_should_copy = "(1) slo.yaml as machine-readable budgets with safe_mode_trigger; (2) golden-trace-gates + pane-perf replay artifacts; (3) emulator compatibility matrix as an external oracle; (4) a dedicated doctor_* crate verified by its own CI job; (5) toolchain-pin + msrv jobs"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 6

[[binary]]
repo = "frankenmermaid"
bin = "frankenmermaid"
cli_grammar = "crates/fm-cli/Cargo.toml:24-31 `[[bin]] frankenmermaid` and alias `fm-cli` (both src/main.rs; comment :28 keeps fm-cli for backwards compatibility) plus `[[bin]] evidence` (src/bin/evidence.rs, :33-35); `fn main() -> !` crates/fm-cli/src/main.rs:3347; `fn cmd_validate(input, ValidateCommandOptions)` :8720; `fn print_validate_text(result, fail_on: FailOnSeverity)` :9279; crates by ls: fm-cli, fm-core, fm-layout, fm-parser, fm-regression-harness, fm-render-canvas, fm-render-svg, fm-render-term, fm-wasm"
robot_mode = "UNVERIFIED: no scout claim beyond `validate --fail-on`; no --json grep run"
exit_lattice = "UNVERIFIED: FailOnSeverity governs validate's failure threshold (main.rs:9279) but the numeric exit codes were not grepped"
envelope = "UNVERIFIED: no scout claim and no grep run"
config = "UNVERIFIED: no scout claim and no grep run"
state = "evidence/ ledger dir with capability_matrix.json (ls); `evidence` binary (fm-cli Cargo.toml:33-35); ci.yml:225-226 `ledger-integrity-guard`, :621-622 `evidence-ledger-guard`"
lints = "crates/fm-cli/src/main.rs:1 `#![forbid(unsafe_code)]` (comment :24-25: a `#[global_allocator]` static coexists with it, mimalloc native-only); no unsafe_code line in workspace Cargo.toml (grep); rust-version 1.95 (Cargo.toml:20)"
tests_unit = "UNVERIFIED: not sampled"
tests_integration = "crates/fm-cli/tests/ (ls): golden_svg_test.rs, golden_deck_test.rs, golden_layout_test.rs, goldens_are_valid_mermaid.rs, golden/ dir; root tests/ (ls); crates/fm-regression-harness/src/main.rs (ls)"
conformance_or_oracle = "upstream Mermaid renderers are the oracle via crates/fm-regression-harness (ls); goldens_are_valid_mermaid.rs (ls); ci.yml:393-459 cross-platform-determinism-{native,wasm,compare} (byte-identical output across native targets and WASI); :372-373 `determinism-guard`"
property_fuzz = "fuzz/fuzz_targets (ls); ci.yml:320-323 `property-test-guard` (gated by quality-gate-config output property_enabled)"
golden = "crates/fm-cli/tests/golden/ + golden_{svg,deck,layout}_test.rs (ls); ci.yml:299-302 `golden-checksum-guard`"
gate_self_test = "ci.yml:11-14 `quality-gate-config` emits per-guard enable outputs consumed by golden-checksum-guard (:299), property-test-guard (:320), invariant-proof-guard (:340), determinism-guard (:372), performance-regression-guard (:514), degradation-guard (:590), evidence-ledger-guard (:621), decision-contract-guard (:774), release-gate-override-guard (:789), demo-evidence-guard (:818), quality-summary (:941); :134-137 `installer-canary` in a container; :197-200 optional-feature-smoke (serve/watch features that once failed to compile for four days)"
ci_jobs = "ci.yml:11-1168 jobs: quality-gate-config, core-check (fnx_mode matrix), installer-canary, png-smoke, optional-feature-smoke, ledger-integrity-guard, golden-checksum-guard, property-test-guard, invariant-proof-guard, determinism-guard, cross-platform-determinism-native, cross-platform-determinism-wasm, cross-platform-determinism-compare, performance-regression-guard, degradation-guard, evidence-ledger-guard, decision-contract-guard, release-gate-override-guard, demo-evidence-guard, quality-summary, wasm-build, coverage, npm-publish; pages.yml (ls)"
doctor_selftest = "UNVERIFIED: no scout claim; no Doctor variant matched in main.rs:1-400 (grep)"
install = "install.sh at repo root (ls) exercised by ci.yml:134-137 installer-canary; build-wasm.sh, pkg/, web/, web_react/, ios/, extensions/, wrangler.jsonc (ls)"
hooks_installed = "none observed at root (ls); npm-publish job (ci.yml:1165-1168) and pages.yml deploy (ls)"
versioning = "Cargo.toml:16-17 workspace version = 0.2.0; ci.yml:4-6 tags v* trigger; release-gate-override-guard (:789-790)"
fail_closed = "`validate --fail-on <severity>` (FailOnSeverity, main.rs:9279); release-gate-override-guard (ci.yml:789-792) polices any gate override; cross-platform-determinism-compare fails on byte drift (:457-460)"
design_invariant = "output determinism is a CI-enforced contract across native and WASI targets (ci.yml:372-460) and every quality guard is individually toggleable from one config job (:11-14)"
what_we_should_copy = "(1) quality-gate-config fan-out with per-guard enable flags; (2) cross-platform determinism compare (native vs WASI byte-identical); (3) evidence/capability_matrix.json ledger + evidence-ledger-guard; (4) installer-canary in a clean container; (5) optional-feature-smoke for features CI otherwise never builds"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 6

[[binary]]
repo = "doodlestein_self_releaser"
bin = "dsr"
cli_grammar = "single Bash orchestrator `dsr` at root, 417,063 bytes (ls -la); dispatch `case` at dsr:10999-11002 (`doctor) cmd_doctor \"$@\"`, `check)`); library src/*.sh (ls): act_runner, artifact_naming, build_state, canary, checksum_sync, config, dispatch, docker, git_ops, github, guardrails, host_health, host_selector, install_gen, logging, notify, packaging, quality_gates, release_formulas, sbom, secrets, signing, slsa, toolchain_detect, upgrade_verify, version"
robot_mode = "JSON output contract defined by schemas/ (ls): envelope.json plus per-command details schemas build, check, config, doctor, fallback, prune, release, report, repos, status, manifest.json; flag spelling not grepped"
exit_lattice = "UNVERIFIED: grep for EXIT_ constants in dsr and src/logging.sh found nothing; codes not enumerated"
envelope = "schemas/envelope.json (ls) with *-details.json per command (build, check, config, doctor, fallback, prune, release, report, repos, status — ls schemas/); field shape not read"
config = "config/repos.d/ and config/actrc.example (ls); src/config.sh (ls); paths not grepped"
state = "src/build_state.sh, src/host_health.sh, src/host_selector.sh (ls); no persisted-state path grepped"
lints = "Bash project; tests use bats (tests/bats.config.bash, ls); no shellcheck config checked"
tests_unit = "tests/unit/*.bats (ls): test_checksum_sync, test_core_functions, test_dispatch, test_docker, test_local_host_artifact_download, test_quality_gates, test_sbom, test_slsa, test_test_logging, test_throttling, test_version_detect; tests/test_harness.bats, tests/helpers/ (ls)"
tests_integration = "tests/integration/test_commands.bats, tests/e2e/test_installers_docker.sh, tests/canary/ (ls)"
conformance_or_oracle = "generated installers are exercised in Docker (tests/e2e/test_installers_docker.sh by ls) for installers/{bv,cass,ntm}/install.sh (ls); src/canary.sh + tests/canary (ls); src/upgrade_verify.sh (ls)"
property_fuzz = "none observed (no fuzz dir at root by ls; none claimed)"
golden = "UNVERIFIED: no scout claim and no golden dir located"
gate_self_test = "src/quality_gates.sh + tests/unit/test_quality_gates.bats (ls); src/guardrails.sh (ls); `dsr doctor` (dsr:10999-11000); schemas/doctor-details.json (ls)"
ci_jobs = ".github/workflows/ absent in the mirror (ls) — dsr is the fallback for when GitHub Actions is unavailable; scripts/ dir (ls) not enumerated"
doctor_selftest = "`dsr doctor` -> cmd_doctor (dsr:10999-11000) with schemas/doctor-details.json (ls)"
install = "install.sh at root (ls); src/install_gen.sh generates cross-platform installers into installers/{bv,cass,ntm}/install.sh (ls); src/packaging.sh, src/release_formulas.sh (ls)"
hooks_installed = "SKILL.md at root (ls); src/notify.sh (ls); no hook installer grepped"
versioning = "src/version.sh + tests/unit/test_version_detect.bats (ls); src/artifact_naming.sh (ls); CHANGELOG.md (ls)"
fail_closed = "src/signing.sh: `_signing_sha256()` :447 (sha256sum | shasum -a 256 per BinFoundations) and signing skips checksum/signature/sbom sidecars (`*SHA256*|*sha256*|*checksums*) continue` :667); src/slsa.sh, src/sbom.sh, src/secrets.sh, src/guardrails.sh (ls); minisign/cosign handling per BinFoundations not read beyond :447/:667"
design_invariant = "one shell orchestrator sources src/*.sh libraries and emits schema-validated JSON per command (schemas/envelope.json + *-details.json by ls); release artifacts carry checksums, minisign signatures, SBOM and SLSA provenance (src/signing.sh:447-667, src/sbom.sh, src/slsa.sh by ls)"
what_we_should_copy = "(1) schemas/envelope.json + per-command details schemas for a shell CLI; (2) installer generator + Docker e2e for the generated installers; (3) signing sidecar skip list (signing.sh:667); (4) bats unit suite per library file"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 2

[[binary]]
repo = "meta_skill"
bin = "ms"
cli_grammar = "clap; `[[bin]] name = ms, path = src/main.rs` Cargo.toml:23-25, package `ms` version 0.2.2 (:2-3); src/cli/{colors,formatters,commands,mod,output,progress} (ls); modules by ls src/: agent_detection, agent_mail, antipatterns, auth, beads, bundler, cass, cm, context, core, dedup, graph, import, lint, meta_skills, quality, search, security, skill_md, storage, suggestions, sync, testing, tui, updater; BinFoundations' bin name `jsm` refuted (jsm is a separate tool)"
robot_mode = "UNVERIFIED: no scout claim; src/cli/output.rs and formatters/ exist (ls) but --json was not grepped"
exit_lattice = "UNVERIFIED: no scout claim; grep for EXIT_ / ExitCode in src/main.rs and src/app.rs found nothing (src/error.rs does not exist)"
envelope = "UNVERIFIED: no scout claim and no grep run"
config = "src/config.rs (ls); path not grepped"
state = "dual persistence Tantivy + SQLite under src/storage/ (ls); global writer lock `LOCK_FILENAME = ms.lock` and sidecar `HOLDER_FILENAME = ms.lock.holder` (src/storage/tx.rs:128-138; sidecar exists because Windows LockFileEx makes the locked file unreadable, #150); migrations/ 15 entries by ls: 001_initial_schema .. 014_add_skill_origins with two 008_* files (008_add_skill_experiment_events.sql, 008_add_skill_feedback.sql) — BinFoundations' `14 migrations` is the top number, not the file count"
lints = "Cargo.toml:177-179 `[lints.rust] unsafe_code = deny` (changed from forbid so Rust-2024 env::set_var tests can `#[allow(unsafe_code)]`); :181 `[lints.clippy]` pedantic + nursery enabled; deny.toml (ls); insta.yaml (ls)"
tests_unit = "tests/unit/ (ls); tests/snapshot_tests.rs + tests/snapshots/ (ls); tests/fixtures/, tests/common/ (ls)"
tests_integration = "tests/integration/, tests/cli.rs, tests/e2e/ (ls: auto_load_workflow, backup_workflow, bundle_workflow, cass_workflow, cross_project_workflow, dedup_workflow, doctor_workflow, experiment_workflow, fixture, fresh_install, graph_workflow, ...); e2e.yml (ls)"
conformance_or_oracle = "UNVERIFIED: no external oracle claimed; ms lint/validate over the skill corpus is self-referential and no oracle harness was located"
property_fuzz = "tests/properties/ (ls); no fuzz/ dir (ls)"
golden = "insta snapshots tests/snapshots/ + tests/snapshot_tests.rs + insta.yaml (ls)"
gate_self_test = "tests/e2e/doctor_workflow.rs and fresh_install.rs (ls); ci.yml:75-78 `toolchain_contract` (Pinned Nightly Toolchain); :139-142 `security` runs regardless of other jobs"
ci_jobs = "ci.yml:35-174 jobs: check (Quick Check), lint, toolchain_contract, test (OS matrix), coverage, security, ci-success (needs lint, toolchain_contract, test, coverage, security); other workflows by ls: bench.yml, e2e.yml, release.yml, notify-acfs.yml"
doctor_selftest = "`ms doctor` verified by tests/e2e/doctor_workflow.rs (ls); src/updater/ (ls)"
install = "no install.sh at root (ls); homebrew-tap/ and scoop-bucket/ (ls); release.yml (ls); skills/ dir bundled (ls)"
hooks_installed = "src/agent_mail/, src/beads/, src/cass/, src/cm/, src/agent_detection/ integrations (ls); MCP surface used as global `ms` server per CLAUDE.md; hook installer not grepped"
versioning = "Cargo.toml:3 version = 0.2.2; migrations numbered 001-014 (ls); src/updater/ (ls); update_deps.py, update_deps_stable.py (ls)"
fail_closed = "exclusive advisory global lock `ms.lock` serialises Tantivy writers (src/storage/tx.rs:128-138); LICENSE:1 `MIT License (with OpenAI/Anthropic Rider)` — code vendoring stays blocked per house rule, tool use accepted"
design_invariant = "one global lock + holder sidecar guards the dual Tantivy/SQLite store so status stays readable while a writer holds the lock on every platform (src/storage/tx.rs:128-138, #150)"
what_we_should_copy = "(1) lock + readable holder-sidecar pattern for cross-platform advisory locks; (2) numbered SQL migrations dir; (3) toolchain_contract CI job for a pinned nightly; (4) e2e workflow tests per user journey (fresh_install, doctor, backup)"
verified_by = "ls/sed by BinRegistry 2026-09-03"
unverified_count = 4
