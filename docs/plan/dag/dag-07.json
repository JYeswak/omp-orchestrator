{
  "section": "07-installability",
  "source": "docs/plan/07-installability.md",
  "item_count": 41,
  "items": [
    {
      "id": "07.1",
      "title": "Generate the canonical release manifest from the Cargo target graph",
      "lines": "L45-L53, L574-L576",
      "what": "One machine ledger with one row per cargo-metadata bin target carrying target/owner/distribution(INSTALL|FOREIGN|UNRELEASED)/adapter; installer BINARIES, runtime adapter list, and post-install expected_set are projections of it, never a second-hand count.",
      "acceptance_seed": "run the NUMBERS.toml built_binaries command and the manifest generator on the same sha; expect identical row count (48 today) and installer/expected_set derived from rows with distribution=INSTALL, with a test that a hand-edited BINARIES const fails.",
      "priority": 0,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "partial", "evidence": "The other 47 bin targets are each either (a) folded into that binary as a subcommand, (b) demoted to a lib with no [[bin]], or (c) deleted — with the disposition recorded per target in a machine ledger, not prose."},
        {"id": "omp-orchestrator-8nuh", "status": "open", "match": "partial", "evidence": "reap-finished-panes is added to the installer's identity set, or the bead states why it must not be — an uncovered binary whose interface can drift is how this became latent."}
      ],
      "coverage": "PARTIAL",
      "gap": "xuu7 asks for a per-target disposition ledger but not the manifest schema (owner/distribution/adapter fields) nor that installer BINARIES (crates/installer/src/main.rs:13, 4 hardcoded tuples today) be generated from it.",
      "depends_on": ["external:omp-orchestrator-untracked-crates-break-clone-jlb (cargo metadata must succeed in a fresh clone)", "02.surface-census-target-denominator"],
      "blocks": ["07.24", "07.32", "07.7"],
      "terminal": false,
      "notes": "search: 'release manifest|canonical manifest|distribution=INSTALL' across issues.jsonl -> 0 hits."
    },
    {
      "id": "07.2",
      "title": "Emit an artifact-success receipt from cargo build --message-format=json",
      "lines": "L35-L38",
      "what": "Filter compiler-artifact messages with target kind bin and record target names, artifact paths, and the command exit code; a target in metadata without an artifact is reported missing and cannot satisfy install acceptance.",
      "acceptance_seed": "run cargo build --workspace --message-format=json | filter bin artifacts; expect a receipt whose set equals the manifest INSTALL rows, and a deliberately broken crate produces a named missing row, not exit 0.",
      "priority": 1,
      "kind": "measurement",
      "existing_beads": [
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "G1. --install builds the WHOLE WORKSPACE, so any one broken crate blocks installing any binary."}
      ],
      "coverage": "PARTIAL",
      "gap": "6hrv fixes per-target builds; nothing records the compiler-artifact receipt as the artifact denominator.",
      "depends_on": ["07.1"],
      "blocks": ["07.32"],
      "terminal": false,
      "notes": "search: 'compiler-artifact|message-format' -> 0 hits."
    },
    {
      "id": "07.3",
      "title": "Wire the four-way identity proof as a standing always-on check",
      "lines": "L266-L280, L100-L102",
      "what": "HEAD == build_id == --version == running process must agree; disagreement is reported per pair; the check runs continuously (supervisor tick / robot command), not only at install time.",
      "acceptance_seed": "run installer --check (or the umbrella doctor identity probe) against the live launchd binary; expect per-target rows naming which legs ran and exit 1 on any DRIFTED pair; mutation: stale the installed binary by one commit and the check goes RED within one tick.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "THE DRIFT CHECK IS A STANDING SURFACE, not a one-shot: one robot command answers \"is what is running the same as what is committed?\" and exits nonzero when it is not."},
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "--check names which identity legs actually ran, per target. No \"four-way\" claim on two legs."},
        {"id": "omp-orchestrator-2tny", "status": "in_progress", "match": "partial", "evidence": "Add a liveness check that stats the executable behind a running pid. launchctl reporting a live pid is not evidence the binary exists."}
      ],
      "coverage": "PARTIAL",
      "gap": "installer --check exists (crates/installer/src/lib.rs:5-8 states the four-way contract) but is a one-shot; no bead wires it into the supervisor tick or a standing doctor probe. ipg.13 closed with clause 8 unevidenced in tree.",
      "depends_on": ["07.22", "07.23"],
      "blocks": ["07.32", "external:omp-orchestrator-31lo acceptance 6", "external:omp-orchestrator-5tn acceptance 2"],
      "terminal": false,
      "notes": "6hrv G2 (restart + postcondition) is the (3)->(4) leg of this proof."
    },
    {
      "id": "07.4",
      "title": "Ratify: a command that did not perform its work MUST NOT exit 0 (R11 #1)",
      "lines": "L93-L98, L516-L518",
      "what": "Refusal gets exit 75 plus status:REFUSED; CI treats REFUSED as blocking; a no-op that exits 0 is the false-green class.",
      "acceptance_seed": "run installer with the build fence engaged; expect exit 75 and status REFUSED; a CI aggregator fixture with one REFUSED envelope must fail the job.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-1p0u", "status": "in_progress", "match": "partial", "evidence": "census and fix the SILENT-SUCCESS class -- every surface in this system that reports success while doing nothing."},
        {"id": "omp-orchestrator-exit-code-registry-rub", "status": "closed", "match": "partial", "evidence": "Register every exit code this workspace emits (12 distinct) ... what it does NOT mean"}
      ],
      "coverage": "PARTIAL",
      "gap": "No bead makes REFUSED blocking in a CI aggregator, and no lint refuses a new exit-0 no-op path.",
      "depends_on": ["07.19"],
      "blocks": [],
      "terminal": false,
      "notes": "installer main.rs:165 already returns 75 on the fence (MEASURED row in §3)."
    },
    {
      "id": "07.5",
      "title": "Decide the aggregator shape: one umbrella binary with adapter subcommands",
      "lines": "L113-L116, L576",
      "what": "7.9 leaves open whether the product is one omp-orchestrator aggregator, separate installed binaries, or something else; §2 assumes the umbrella.",
      "acceptance_seed": "a decision row in the ledger naming the shape and the 48-target disposition rule; the plan section no longer says 'remains open'.",
      "priority": 0,
      "kind": "decision",
      "existing_beads": [
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "exact", "evidence": "Josh, 2026-09-02: \"we can make nothing depend on control-plane - this needs to be a single installable binary following - fully contained.\" ... ONE binary target is the product."}
      ],
      "coverage": "COVERED",
      "gap": "",
      "depends_on": [],
      "blocks": ["07.6", "07.1"],
      "terminal": false,
      "notes": "7.9 NO-CLAIM (L576) predates xuu7; the doc should cite xuu7 as the resolution."
    },
    {
      "id": "07.6",
      "title": "Build the umbrella omp-orchestrator CLI scoping every target as an adapter",
      "lines": "L113-L116, L217-L223",
      "what": "One installed CLI whose doctor/health/repair take an adapter name; every one of the 48 targets is reachable as omp-orchestrator doctor <adapter> and help <adapter> names the command.",
      "acceptance_seed": "run omp-orchestrator help <adapter> for every manifest row; expect a usage line for each and exit 2 for an unknown adapter.",
      "priority": 0,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "partial", "evidence": "The other 47 bin targets are each either (a) folded into that binary as a subcommand ..."},
        {"id": "omp-orchestrator-jplf.7", "status": "open", "match": "partial", "evidence": "Close doctor, health, repair, clean-machine installation, and external-repository first-tick behavior."}
      ],
      "coverage": "PARTIAL",
      "gap": "No leaf bead builds the adapter dispatch surface; xuu7 is governing and jplf.7 is an epic with no children.",
      "depends_on": ["07.5", "07.1"],
      "blocks": ["07.8", "07.9", "07.10", "07.11", "07.12", "07.13", "07.14", "07.15"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.7",
      "title": "Enforce the response envelope and namespaced probe ids at construction",
      "lines": "L118-L124",
      "what": "Every command emits {schema_version,command,status(OK|DEGRADED|DOWN|UNKNOWN|REFUSED),data}; probe ids match ^omp(\\.[a-z][a-z0-9_-]*){2,}$ and bare segments are rejected at construction; details are structured =N values.",
      "acceptance_seed": "unit test: constructing probe id 'omp.identity' (one segment) panics/returns Err; a golden envelope round-trips; a prose detail string is rejected.",
      "priority": 1,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "doctor/health/repair, versioned JSON envelope, namespaced probe ids."},
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.12", "status": "closed", "match": "partial", "evidence": "envelope shape MEASURED at crates/omp-inventory-map/src/lib.rs (doc L110-L111)"}
      ],
      "coverage": "PARTIAL",
      "gap": "Envelope exists in omp-inventory-map only; no shared crate/type enforces the probe-id regex or the =N detail rule.",
      "depends_on": ["07.6"],
      "blocks": ["07.9", "07.10"],
      "terminal": false,
      "notes": "search 'probe id|namespaced' -> only ipg.13 clause 8."
    },
    {
      "id": "07.8",
      "title": "Ship omp-orchestrator doctor [<adapter>] with exit 0/1/2 and DOWN-never-panic",
      "lines": "L126-L135",
      "what": "Diagnose every subsystem or one adapter; data.probes[] rows {id,status,detail,upstream_owner?,repair_target?}; a dead adapter is DOWN with exit 1 (3 for critical), never a panic; names the adapter, not adapters_ok=false.",
      "acceptance_seed": "run doctor with tick-monitor removed from PATH; expect a probe row naming tick-monitor with status DOWN, exit 1, no panic.",
      "priority": 0,
      "kind": "command",
      "existing_beads": [
        {"id": "omp-orchestrator-jplf.7", "status": "open", "match": "partial", "evidence": "Doctor/health/repair are idempotent and reversible"}
      ],
      "coverage": "PARTIAL",
      "gap": "Epic only; no leaf specifies the probe envelope, exit codes, or the dead-adapter negative pattern. Per-crate 'doctor' strings exist in fast-dispatch/fleet-monitor/loop-driver dispatch_cli_contract.rs, not an umbrella.",
      "depends_on": ["07.6", "07.7"],
      "blocks": ["07.9", "07.17", "07.32"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.9",
      "title": "Ship omp-orchestrator health as a strict rollup of doctor sharing one classifier",
      "lines": "L137-L146",
      "what": "Single-shot rollup, exit 0/1/3, --watch; health and doctor cannot disagree (one classifier) pinned by a convergence test; health performs no mutating or unbounded I/O.",
      "acceptance_seed": "convergence test: for every fixture probe set, health rollup == doctor rollup; a mutation that forks the classifier goes RED; health under a hung substrate returns within its deadline.",
      "priority": 1,
      "kind": "command",
      "existing_beads": [
        {"id": "omp-orchestrator-jplf.7", "status": "open", "match": "partial", "evidence": "Close doctor, health, repair"}
      ],
      "coverage": "PARTIAL",
      "gap": "No leaf bead; the shared-classifier convergence test and no-blocking-I/O negative pattern are unrepresented.",
      "depends_on": ["07.8"],
      "blocks": ["07.32"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.10",
      "title": "Ship omp-orchestrator repair with a single mutate() chokepoint and byte-exact undo",
      "lines": "L148-L161, L476-L487",
      "what": "Dry-run default; --apply requires --confirm; --dry-run --apply rejected; exit 0/1/5; every write goes through one mutate() that backs up into .omp/runs/<run-id>/backups/ with before/after hashes and appends an ActionRecord; repair undo <run-id> restores byte-for-byte.",
      "acceptance_seed": "apply a repair, run undo, sha256 the touched paths before/after; expect identical; a mutation-auditor test fails the build on any write path that bypasses mutate().",
      "priority": 1,
      "kind": "command",
      "existing_beads": [
        {"id": "omp-orchestrator-jplf.7", "status": "open", "match": "partial", "evidence": "Doctor/health/repair are idempotent and reversible"}
      ],
      "coverage": "PARTIAL",
      "gap": "No leaf bead; chokepoint, backup layout, undo, exit 5 lock, and the mutate-auditor test are unrepresented. Prior art named: coding_agent_session_search doctor_chokepoint.rs.",
      "depends_on": ["07.8", "07.12"],
      "blocks": [],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.11",
      "title": "Ship omp-orchestrator validate <thing> as a pure read with exit 74 on reject",
      "lines": "L165-L176",
      "what": "Validate a config, dispatch packet, plist, or install target before anything acts; reject envelope {status:REJECT,reason,expected,observed,observed_length}; no filesystem writes; not satisfiable by a mutation.",
      "acceptance_seed": "run validate on a malformed plist; expect exit 74 with observed field populated; strace/fs-watch shows zero writes.",
      "priority": 2,
      "kind": "command",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.6", "07.19"],
      "blocks": ["07.10"],
      "terminal": false,
      "notes": "search 'validate' -> 29 hits, all validate_receipt/validation of packets, none an umbrella validate subcommand."
    },
    {
      "id": "07.12",
      "title": "Ship omp-orchestrator audit: append-only schema-versioned mutation ledger",
      "lines": "L178-L186",
      "what": "Rows {ts,actor,verb,idempotency_key,touched_paths[],receipt_path,post_check,result}; a repair that mutates without an audit row fails the mutate-auditor test; an ambiguous audit read refuses the mutation.",
      "acceptance_seed": "apply one repair then run audit --since; expect exactly one row with the touched paths; corrupt the ledger and audit exits 1.",
      "priority": 2,
      "kind": "command",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.6"],
      "blocks": ["07.10"],
      "terminal": false,
      "notes": "search 'append-only' -> ack-spine StepLedger (dispatch steps), not an install/repair mutation ledger."
    },
    {
      "id": "07.13",
      "title": "Ship omp-orchestrator why <id> with gaps as explicit UNKNOWN nodes",
      "lines": "L188-L195",
      "what": "Provenance chain data.chain[] from origin to current state for a binary, probe verdict, bead, or dispatch; a missing hop is a node {hop,status:UNKNOWN,reason}, never synthesized.",
      "acceptance_seed": "run why on a binary lacking build_id; expect a chain containing a hop build_id with status UNKNOWN and reason 'binary exposes no --version'; unknown id exits 1.",
      "priority": 2,
      "kind": "command",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.6", "07.12"],
      "blocks": [],
      "terminal": false,
      "notes": "search 'why <id>|provenance trace' -> 0."
    },
    {
      "id": "07.14",
      "title": "Ship --info, examples, quickstart, help <topic>, completion <shell>",
      "lines": "L197-L201",
      "what": "Self-documentation verbs are mandatory and completion bash | bash -n - must exit 0.",
      "acceptance_seed": "run omp-orchestrator completion bash | bash -n -; expect exit 0; each verb prints non-empty output with exit 0.",
      "priority": 2,
      "kind": "command",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.6"],
      "blocks": ["07.28"],
      "terminal": false,
      "notes": "search 'completion' hits are all 'completion signal'/'completion verification', none a shell-completion verb."
    },
    {
      "id": "07.15",
      "title": "Record the ADDRESSABLE replacement receipt for omp-inventory-map",
      "lines": "L203-L215",
      "what": "Replace the un-reproducible '13 tests / 544 KB' figures with the two exact commands, saved artifacts, exit codes, and commit; a changed command/scope/path is a new measurement.",
      "acceptance_seed": "run the two commands at L211-L212 on a pinned sha; expect artifacts/inventory-map-tests.list and artifacts/inventory-map-doctor.json present and the receipt naming both exit codes.",
      "priority": 2,
      "kind": "measurement",
      "existing_beads": [
        {"id": "omp-orchestrator-kxe.3", "status": "in_progress", "match": "partial", "evidence": "Reconcile all live plan denominators and provenance claims with NUMBERS.toml and current repository/tool outputs across sections 00–03, 06–08, and 11."}
      ],
      "coverage": "PARTIAL",
      "gap": "kxe.3 reconciles figures generally; it does not name these two commands or the artifact paths.",
      "depends_on": [],
      "blocks": [],
      "terminal": false,
      "notes": "omp-inventory-map --help returns CONFIG_ERROR unknown argument --help (L204-L205) — same class as 07.29."
    },
    {
      "id": "07.16",
      "title": "Check in a doctor capabilities golden and fail CI on probe-list drift",
      "lines": "L219-L223",
      "what": "omp-orchestrator doctor capabilities --json enumerates every probe id; a checked-in golden snapshot is diffed in CI; drift between declared and implemented probes fails.",
      "acceptance_seed": "add one probe without updating the golden; expect the CI leg RED; update golden, GREEN.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.8", "07.7"],
      "blocks": [],
      "terminal": false,
      "notes": "search 'capabilities' -> 0; 'golden' -> 0."
    },
    {
      "id": "07.17",
      "title": "Emit class upstream_substrate_issue and ship omp-orchestrator upstream-report",
      "lines": "L225-L231",
      "what": "When an adapter probe fails on the substrate side (omp/ntm/br/bv/git/cargo/tmux), the envelope carries class and upstream_owner and upstream-report <adapter> drafts the issue.",
      "acceptance_seed": "fixture: ntm returns total_sessions:0 with success:true; expect the probe row with upstream_owner=ntm and upstream-report producing a draft naming the version and reproduction.",
      "priority": 2,
      "kind": "command",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.8"],
      "blocks": [],
      "terminal": false,
      "notes": "search 'upstream-report|upstream_owner|upstream_substrate' -> 0."
    },
    {
      "id": "07.18",
      "title": "Ship the exit-code dictionary uniformly across all 48 targets",
      "lines": "L236-L262",
      "what": "Codes 0/1/2/3/5/70/71/74/75/103 with fixed names; MEASURED today only in installer (1/2/3/75) and omp-inventory-map (2); the other targets are unaudited against the dictionary.",
      "acceptance_seed": "a registry test asserts every bin target's declared exit constants map to dictionary rows; a target emitting 1 for a refusal goes RED.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-exit-code-registry-rub", "status": "closed", "match": "partial", "evidence": "Register every exit code this workspace emits (12 distinct) and every foreign code it receives, one row per code ... Change no code"},
        {"id": "omp-orchestrator-exit-1-overloaded-x1o", "status": "open", "match": "partial", "evidence": "Exit 1 carries three unrelated meanings across 32 crates and 89 literal sites ... Split them"},
        {"id": "omp-orchestrator-exit-const-collision-cas", "status": "open", "match": "partial", "evidence": "EXIT_CONFIG bound to two values; 78 bound to two names; 75 to five meanings"},
        {"id": "omp-orchestrator-exit-reserved-range-b7f", "status": "open", "match": "partial", "evidence": "EXIT_DEADLINE=124 collides with timeout(1); 77 deviates from EX_NOPERM"}
      ],
      "coverage": "PARTIAL",
      "gap": "Registry documents the current space; no bead adopts §3's dictionary (5/70/71/74/103 rows) as the target or enforces uniformity mechanically. Note cas: 75 already carries five meanings, conflicting with §3's single REFUSED.",
      "depends_on": ["external:omp-orchestrator-exit-const-collision-cas", "external:omp-orchestrator-exit-1-overloaded-x1o"],
      "blocks": ["07.4", "07.11", "07.19"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.19",
      "title": "Treat exit 103 with 0 passed/0 failed as REFUSED, blocking in CI",
      "lines": "L252-L260",
      "what": "Every refusal code carries status:REFUSED distinct from OK and DOWN; the CI aggregator treats REFUSED as blocking; the RCH mint-floor 103 is not a test result.",
      "acceptance_seed": "feed the aggregator a run whose only output is exit 103 / 0 passed; expect the job RED with reason REFUSED_UPSTREAM.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-tvu5", "status": "in_progress", "match": "partial", "evidence": "a cargo exit code is not a build verdict: the RCH shim exits 103 with zero diagnostic lines and every consumer blames the code"}
      ],
      "coverage": "PARTIAL",
      "gap": "tvu5 addresses consumers misattributing 103; the REFUSED-is-blocking aggregator rule is unrepresented.",
      "depends_on": ["07.18"],
      "blocks": [],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.20",
      "title": "Embed build_id via build.rs in every bin crate; installer refuses non-reporting artifacts (R11 #3)",
      "lines": "L284-L294, L489-L494, L521-L522",
      "what": "Every binary-producing crate ships a vergen-gix-shaped build.rs with an is-inside-work-tree guard and exposes build_id on --version; a binary that cannot report its build_id is not installable.",
      "acceptance_seed": "for every manifest INSTALL row run <bin> --version; expect build_id=<sha> equal to git rev-parse HEAD; strip the build.rs from one crate and installer --install refuses it by name.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "EVERY INSTALLED BINARY HAS --version EMITTING build_id. tick-monitor does not today ... Add it as part of this bead."},
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "G3. build_id=ABSENT on all three binaries, so the four-way proof silently degrades to a version-string compare."}
      ],
      "coverage": "PARTIAL",
      "gap": "build.rs exists in 5 crates only (installer, no-shell-gate, omp-orchestrator, pane-truth, tick-monitor; ls crates/*/build.rs) against 48 targets; tick-monitor now greps 3 for --version|OMP_BUILD_ID (doc L63 '0' is stale). No bead covers the other 43 or the install-time refusal of a non-reporting artifact.",
      "depends_on": ["07.1"],
      "blocks": ["07.3", "07.32"],
      "terminal": false,
      "notes": "House shape: hand-rolled git rev-parse in crates/installer/build.rs, not vergen-gix as the doc mandates — the integrator should decide whether to note the divergence."
    },
    {
      "id": "07.21",
      "title": "Compile in the owning-repo declaration; FOREIGN printed and excluded from drift",
      "lines": "L295-L299, L61",
      "what": "Each binary declares its owner alongside build_id; a PATH binary whose owner is not this repo is FOREIGN, named, and excluded from the drift denominator as a third outcome beside CONSISTENT and DRIFTED.",
      "acceptance_seed": "installer --check with pane-truth on PATH; expect a row 'pane-truth: FOREIGN (source in <repo>) — excluded from drift denominator' and a clean exit when the owned set is consistent.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "G4. pane-truth prints MISMATCH, not FOREIGN, though Foreign appears 4x in lib.rs. The class is implemented and not reached."},
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "partial", "evidence": "pane-truth is either ported into the binary or removed from the installer's set; the permanent-MISMATCH row is gone either way."}
      ],
      "coverage": "PARTIAL",
      "gap": "RepoOwnership::Foreign exists (crates/installer/src/lib.rs:160-169, :236-239) and is derived from source location, not a compiled-in owner declaration; crates/pane-truth now exists here (doc L61), so the owner rule and 6hrv G4 need re-measurement.",
      "depends_on": ["07.20"],
      "blocks": ["07.3", "07.25"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.22",
      "title": "Print the identity denominator as six named integers with derivation (R11 #2)",
      "lines": "L301-L303, L519-L520, L387",
      "what": "identity: consistent=N drifted=M foreign=K expected=E workspace_targets=48 probed=P — both terms of every ratio separately named and separately sourced; the OK and DRIFT lines print from the same variables.",
      "acceptance_seed": "run installer --check; expect the six-integer line with consistent+drifted+foreign==probed and expected derived from manifest INSTALL rows; a mutation decrementing one counter without the other goes RED.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "Anti-vacuity: an empty target set is an ERROR - a check that finds no binaries reports identically to one where everything matches."}
      ],
      "coverage": "PARTIAL",
      "gap": "crates/installer/src/main.rs:115-117 counts owned/foreign explicitly (regression note satisfied) but emits 'N/N owned binaries', not the six-term line with workspace_targets/expected/probed.",
      "depends_on": ["07.1"],
      "blocks": ["07.32"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.23",
      "title": "Complete the Rust installer crate per /installer-workmanship",
      "lines": "L311-L318, L341-L343, L355-L378, L385",
      "what": "Platform detection, preflight, atomic mkdir lock with stale-PID, SHA256 via sha256sum|shasum, Sigstore when cosign present, build-from-source fallback, install -m 0755, XDG completions, PATH setup, trap cleanup, per-component summary, uninstall instructions; each item present or NAMED ABSENT in --help.",
      "acceptance_seed": "installer --help lists every workmanship item with present/absent; a Rust test matrix exercises platform, proxy/offline, repeat-install, setup, migration; installer --install <t> --bin-dir <scratch> succeeds on a clean machine.",
      "priority": 0,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "THE INSTALLER IS A RUST CRATE. Platform detection, preflight, atomic lock, checksum, extract, install -m 0755, completions, agent config, summary, uninstall — all of it, in Rust"},
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "partial", "evidence": "Installability per /installer-workmanship, applied to the RUST installer ... Each present or NAMED ABSENT with a reason in installer --help."},
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "installer already IS the parity gate and is the one binary not installed: close its five gaps, do not rebuild it"},
        {"id": "omp-orchestrator-n4q", "status": "in_progress", "match": "partial", "evidence": "installer/src/lib.rs has 6 process::Command .output() sites ... with NO deadline"},
        {"id": "omp-orchestrator-installer-dep-uncommitted-txl", "status": "blocked", "match": "partial", "evidence": "crates/installer does not compile from a clean checkout of main. The dependency that makes it compile exists ONLY in somebody's working tree, uncommitted."}
      ],
      "coverage": "PARTIAL",
      "gap": "ipg.13 closed while the crate's usage is only [--check | --install TARGET | --version] [--bin-dir PATH] (main.rs:93): no platform detection, lock, checksum, completions, PATH, uninstall. 6hrv/n4q/txl each cover one slice; no bead owns the workmanship matrix.",
      "depends_on": ["external:omp-orchestrator-installer-dep-uncommitted-txl", "external:omp-orchestrator-6hrv", "external:omp-orchestrator-n4q", "07.1"],
      "blocks": ["07.24", "07.26", "07.27", "07.28", "07.29", "07.32", "07.36"],
      "terminal": false,
      "notes": "2tny acceptance 3 (every test path uses --bin-dir scratch) is a hard precondition for exercising this crate on the live machine."
    },
    {
      "id": "07.24",
      "title": "Generate the thin bootstrap install.sh as an untracked release artifact, golden-tested (R11 #4)",
      "lines": "L344-L354, L380-L383, L523-L526",
      "what": "A cargo xtask-style Rust command emits ~80-line install.sh into target/release-artifacts/ at release time; never git-added; its expected output lives in a .txt fixture and drift fails CI; the no-.sh gate keeps an empty exemption list.",
      "acceptance_seed": "run the generator; diff output against the .txt fixture (expect empty); git ls-files | grep -E '\\.(sh|py)$' -> 0; a one-byte change to the generator turns the golden leg RED.",
      "priority": 1,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "THE CURL ENTRY POINT IS A GENERATED RELEASE ARTIFACT, NOT A TRACKED FILE ... EMITTED by the installer crate into the release artifact and never committed"},
        {"id": "omp-orchestrator-4ak", "status": "closed", "match": "partial", "evidence": "A Rust gate that walks git ls-files and refuses any path matching *.sh or *.py ... the exemption list is empty."}
      ],
      "coverage": "PARTIAL",
      "gap": "4ak provides the gate; ipg.13 states the design; no generator, xtask, or .txt golden exists (grep xtask|release-artifacts in crates/*/src -> 0 files).",
      "depends_on": ["07.23", "07.25"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.25",
      "title": "Verify the bootstrap before execution: pinned digest or signature, no unsigned fallback",
      "lines": "L319-L326, L397-L401",
      "what": "The one-liner downloads install.sh to a private temp dir, verifies SHA256 against release metadata (or a detached signature against a trusted key), executes only after success; absent both, the curl path is unpublished and the fallback is exact-commit cargo install --git.",
      "acceptance_seed": "tamper one byte of the downloaded bootstrap; expect refusal naming expected and actual digests before any exec; with no pinned digest available the one-liner is not emitted.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "Hand it a corrupted tarball; the SHA256 check fails and names the expected and actual digests."}
      ],
      "coverage": "PARTIAL",
      "gap": "ipg.13 covers artifact checksum only; bootstrap-script verification before exec, release metadata pinning, signature path, and the no-unsigned-fallback rule are unrepresented.",
      "depends_on": ["07.24", "external:release channel / signing key decision (Josh)"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": "L397 NO-CLAIM: no signing key exists today — this is a human decision the integrator should surface."
    },
    {
      "id": "07.26",
      "title": "Honor HTTPS_PROXY/HTTP_PROXY/NO_PROXY, --proxy, and --offline on every fetch",
      "lines": "L361-L363",
      "what": "No fetch path bypasses the selected proxy; --offline refuses before any network access and consumes only a verified local cache.",
      "acceptance_seed": "run installer --offline with an empty cache; expect a typed refusal before any socket open (verified by a fake-network test); with HTTPS_PROXY set to a local recorder, every fetch is observed there.",
      "priority": 2,
      "kind": "gate",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.23"],
      "blocks": [],
      "terminal": false,
      "notes": "ipg.13 mentions 'proxy support on every fetch' in its ADOPT list but no acceptance leg; treated as MISSING."
    },
    {
      "id": "07.27",
      "title": "Version selection and idempotent repeat-install",
      "lines": "L364-L367",
      "what": "Prefer an explicit version; fall back to the version in the immutable artifact URL and report the fallback; a matching installed build exits 0 without rewriting; a different build plans a replacement and requires confirmation.",
      "acceptance_seed": "install twice at the same sha; expect second run exit 0 with zero file mtimes changed; install a different sha without --confirm; expect a PLANNED replacement and no write.",
      "priority": 2,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-omp-coverage-mission-ipg.13", "status": "closed", "match": "partial", "evidence": "already-installed short-circuit that STILL reconfigures"}
      ],
      "coverage": "PARTIAL",
      "gap": "Version fallback reporting and confirmation on a differing build are unrepresented; ipg.13's short-circuit was never acceptance-tested.",
      "depends_on": ["07.23", "07.3"],
      "blocks": [],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.28",
      "title": "Operator setup: PATH, completions, hook/skill registration, doctor/health entrypoints; SKIPPED never silent",
      "lines": "L368-L371",
      "what": "Installation configures each agent-facing surface and lists every resulting path in the final summary; missing optional integrations are reported SKIPPED.",
      "acceptance_seed": "install on a machine without a completion dir; expect the summary row 'completions: SKIPPED <reason>' and every configured path listed; a summary omitting a changed component fails the receipt test.",
      "priority": 2,
      "kind": "wiring",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.23", "07.14"],
      "blocks": [],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.29",
      "title": "Migration and removal: detect predecessor by owner/build_id, back up, stay reversible, print uninstall+rollback",
      "lines": "L372-L375, L328-L331",
      "what": "A predecessor install is detected by owner/build id, backed up, migrated or explicitly refused before replacement; the final summary prints exact uninstall and rollback commands and names every changed component; a destination never transitions to absent.",
      "acceptance_seed": "install over an existing binary; expect a backup path in the summary and a rollback command that, when run, sha256-matches the predecessor; a placement made a no-op turns the success report RED.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-2tny", "status": "in_progress", "match": "partial", "evidence": "refuse to UNLINK a destination without having a verified replacement already staged. A destination that exists must never transition to absent - the atomic form is write .staged.$$ -> mv -f over the target"}
      ],
      "coverage": "PARTIAL",
      "gap": "2tny covers atomic placement and stat-verified success; predecessor detection by owner/build_id, printed uninstall/rollback commands, and an uninstall verb are unrepresented.",
      "depends_on": ["07.23", "07.21"],
      "blocks": ["07.32"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.30",
      "title": "Reserved: installer restart + postcondition for launchd-managed targets",
      "lines": "L272, L274-L279",
      "what": "The running leg (4) of the identity proof requires the installer to restart launchd-managed targets and verify by postcondition (start time moved, identity agrees).",
      "acceptance_seed": "installer --install omp-orchestrator; expect launchctl pid changed and the heartbeat build_id equal to HEAD within one cycle.",
      "priority": 0,
      "kind": "wiring",
      "existing_beads": [
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "exact", "evidence": "--install performs the restart for launchd-managed targets and VERIFIES BY POSTCONDITION - the running process's start time moved AND the four-way identity now agrees."}
      ],
      "coverage": "COVERED",
      "gap": "",
      "depends_on": ["07.20"],
      "blocks": ["07.3"],
      "terminal": false,
      "notes": "Not stated as a separate mechanism in §07 prose but is the only way leg (4) can be satisfied; 6hrv G2 owns it."
    },
    {
      "id": "07.31",
      "title": "Post-install acceptance: doctor --json identity line with missing=0 and health exit 0",
      "lines": "L385-L395",
      "what": "The install is accepted only when doctor reports consistent=N drifted=0 foreign=K expected_set=N missing=0 probed=P with expected_set as the sorted manifest INSTALL names (or hash+names), every member probed, absent members named missing=<target>, nonzero exit on missing!=0 or any DRIFTED, and health returns 0; copying files and exiting 0 without the check is the false-green class.",
      "acceptance_seed": "install with one expected target removed from bin-dir; expect missing=<target>, nonzero exit, and no INSTALLED success line; full set present -> health exit 0.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-2tny", "status": "in_progress", "match": "partial", "evidence": "installer --install <t> cannot report success unless the destination exists afterward AND its reported build_id equals current HEAD."},
        {"id": "omp-orchestrator-6hrv", "status": "in_progress", "match": "partial", "evidence": "Fires-on-known-bad + known-good + a uniquely-anchored mutation ... an empty target set is an ERROR"}
      ],
      "coverage": "PARTIAL",
      "gap": "Both beads assert per-target success; neither derives expected_set from a manifest, probes every member, or gates on health exit 0.",
      "depends_on": ["07.1", "07.2", "07.3", "07.8", "07.9", "07.22", "07.29"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": "This is 07.32 in the earlier numbering of blocks fields; ids 07.31/07.32 refer to this item."
    },
    {
      "id": "07.32",
      "title": "Alias of 07.31 (post-install acceptance) — kept so block references resolve",
      "lines": "L385-L395",
      "what": "Same item as 07.31; earlier blocks fields cite 07.32.",
      "acceptance_seed": "see 07.31",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [],
      "coverage": "PARTIAL",
      "gap": "see 07.31",
      "depends_on": ["07.31"],
      "blocks": [],
      "terminal": false,
      "notes": "Integrator: merge into 07.31."
    },
    {
      "id": "07.33",
      "title": "Run the five-person/five-machine pilot and publish the scoreboard receipts",
      "lines": "L406-L422",
      "what": "One receipt per attempt; targets: first-attempt success >=4/5, median time-to-first-use <=10 min, <=1 intervention per install, >=4/5 complete one reversible adapter action with status=APPLIED; a release publishes counts and any miss is release-blocking until classified. Baseline pilot_attempts=0.",
      "acceptance_seed": "a pilot ledger with 5 receipt rows each carrying verified-bootstrap timestamp, missing=0, health exit, intervention count, and an APPLIED action; release notes cite the four metrics.",
      "priority": 1,
      "kind": "measurement",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.31", "07.36", "07.10", "external:Josh names five pilot users/machines"],
      "blocks": [],
      "terminal": true,
      "notes": "search 'pilot' -> 0 hits."
    },
    {
      "id": "07.34",
      "title": "Move machine literals to $XDG_CONFIG_HOME/omp-orchestrator/config.toml; unset HOME is a hard error",
      "lines": "L424-L431",
      "what": "The /Users/josh HOME fallback and the ~/.cargo/bin/cargo default become config with per-machine overrides; an unset HOME is refused, not guessed.",
      "acceptance_seed": "run the installer with HOME unset and no config; expect a typed refusal naming HOME/config; with config.toml present the CARGO path comes from it (assert via a fixture path).",
      "priority": 1,
      "kind": "crate",
      "existing_beads": [
        {"id": "omp-orchestrator-7ai", "status": "closed", "match": "partial", "evidence": "Home-relative paths (ledgers, state, tmux socket) come from the environment or a documented default, never a literal."},
        {"id": "omp-orchestrator-io3h", "status": "in_progress", "match": "partial", "evidence": "refuse a commit that lands a crate carrying hardcoded home-path literals, so extraction cannot re-open bead 7ai's closed invariant."},
        {"id": "omp-orchestrator-round22-drifted-figures-844", "status": "in_progress", "match": "partial", "evidence": "two sections asserting a /Users/josh fallback that no longer exists"}
      ],
      "coverage": "PARTIAL",
      "gap": "Literal removal is covered (7ai closed, io3h gate); the XDG config.toml file, per-machine overrides, and HOME-unset hard error are unrepresented (search 'XDG' -> 0). 844 says the /Users/josh fallback is already gone — L424-L425 is stale.",
      "depends_on": ["external:omp-orchestrator-io3h", "external:omp-orchestrator-k4sc"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": "installer main.rs:89 now errors 'INSTALL_BIN_DIR, --bin-dir, or HOME must be set' — partial evidence the HOME hard error landed for bin-dir."
    },
    {
      "id": "07.35",
      "title": "Installed binary discovers its repo by walking to .git and cross-checks the compiled-in owner",
      "lines": "L433-L437",
      "what": "Repo root from CARGO_MANIFEST_DIR is compile-time only; the installed binary walks up from cwd, and a mismatch between discovered repo and the compiled-in owning-repo declaration is a typed refusal.",
      "acceptance_seed": "run the installed binary from a checkout of a different repo; expect a typed refusal naming discovered vs declared owner; from its own checkout, exit 0.",
      "priority": 1,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-7ai", "status": "closed", "match": "partial", "evidence": "Repo root is DISCOVERED: walk up from cwd for a marker (.git, .beads, Cargo.toml workspace root), or take an explicit --repo flag ... run it from a directory with NO repo above it and assert it FAILS LOUDLY with a typed error"}
      ],
      "coverage": "PARTIAL",
      "gap": "Discovery and fail-loud are closed under 7ai; the owner cross-check against the compiled-in declaration (needs 07.21) is unrepresented.",
      "depends_on": ["07.21"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.36",
      "title": "M6 cold-start proof: clean-machine install receipt and external-repo first tick",
      "lines": "L3-L6, L460-L463, L385-L389",
      "what": "A second machine (macOS or Linux with the substrates) receives the install, the identity line, health exit 0, and one real tick in a repo that is not this checkout; the plan's cross-machine claim is untested until this runs somewhere else.",
      "acceptance_seed": "on a host that is not the Studio: one-command install, doctor identity line with missing=0, health 0, omp-orchestrator --once in a foreign repo reaching decide(); receipt carries hostname, sha, and both outputs.",
      "priority": 0,
      "kind": "milestone",
      "existing_beads": [
        {"id": "omp-orchestrator-jplf.7", "status": "open", "match": "partial", "evidence": "a clean-machine receipt proves installation, an external-repo first-tick receipt proves the real entry path, and an independent grader confirms both."},
        {"id": "omp-orchestrator-xuu7", "status": "open", "match": "partial", "evidence": "self-contained is not the same as portable — M6 cold-host proof (§09) is a separate milestone this bead unblocks rather than satisfies."},
        {"id": "omp-orchestrator-vyud", "status": "open", "match": "partial", "evidence": "§07 installability / M6 cold-host portability: the plan's proof for a second machine is \"clean install + first tick\". The test suite that certifies the tree cannot run on that machine."}
      ],
      "coverage": "PARTIAL",
      "gap": "Epic-level only; no leaf bead names the second host, the receipt schema, or the foreign-repo first tick.",
      "depends_on": ["07.31", "07.23", "07.24", "07.34", "07.35", "07.37", "external:omp-orchestrator-xuu7", "external:omp-orchestrator-vyud", "09.M6"],
      "blocks": ["07.33"],
      "terminal": true,
      "notes": "§09 owns the milestone sequencing; this row is the installability half."
    },
    {
      "id": "07.37",
      "title": "Typed missing-dependency refusal (omp-preflight/v1, exit 75) with binary per-adapter availability",
      "lines": "L439-L453, L500-L504",
      "what": "A machine without tmux/ntm/br fails closed with an envelope naming the dependency, the probed paths, and the install command; an adapter is fully available or DOWN with a named remediation — never a silent partial that reports ticks and moves no work.",
      "acceptance_seed": "PATH without ntm: expect {status:REFUSED, data.probe:omp.preflight.dependency.ntm.absent, probed:[...], install:..., exit:75} and zero dispatch; restore ntm, exit 0.",
      "priority": 0,
      "kind": "gate",
      "existing_beads": [
        {"id": "omp-orchestrator-h27o", "status": "closed", "match": "partial", "evidence": "K5 admission meet + refusal: typed nonzero naming pane and reason, Unknown never coerced, empty oracle is ERROR"}
      ],
      "coverage": "PARTIAL",
      "gap": "h27o types pane-admission refusals, not substrate-dependency preflight; no bead emits the omp-preflight envelope or probes tmux/ntm/br presence (search 'omp.preflight|missing dependenc' -> 0; §7 records no prior art).",
      "depends_on": ["07.7", "07.18"],
      "blocks": ["07.36"],
      "terminal": false,
      "notes": ""
    },
    {
      "id": "07.38",
      "title": "Dependency probe records version:UNPROBEABLE for substrates that reject --version (tmux)",
      "lines": "L455-L458",
      "what": "tmux --version returns 'unknown option'; the probe must not read a version-flag rejection as absence and records the rejection text as evidence.",
      "acceptance_seed": "probe tmux; expect present=true, version:UNPROBEABLE, evidence containing 'unknown option'; probe a missing binary; expect absent.",
      "priority": 2,
      "kind": "gate",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.37"],
      "blocks": [],
      "terminal": false,
      "notes": "search 'UNPROBEABLE|tmux --version' -> 0."
    },
    {
      "id": "07.39",
      "title": "Record the four R11 constraints as repo doctrine with a current receipt",
      "lines": "L510-L528",
      "what": "The four constraints (no-work-no-exit-0; ratios with named terms; no build_id = not installable; no-.sh governs tracked files only) are written as doctrine, each asserted enforced only when a receipt names revision, artifact, and command.",
      "acceptance_seed": "a doctrine file/registry rows for R11-1..4, each pointing at the gate item (07.4, 07.22, 07.20, 07.24) and a receipt sha; a constraint without a receipt reads PROPOSED, not ENFORCED.",
      "priority": 2,
      "kind": "doc-fact",
      "existing_beads": [
        {"id": "omp-orchestrator-4ak", "status": "closed", "match": "partial", "evidence": "Gate FIRST: fail the build on any .sh or .py — Rust, planted known-bad, mutation"}
      ],
      "coverage": "PARTIAL",
      "gap": "Only R11-4's gate exists as a bead; the doctrine record and receipts for R11-1..3 are unrepresented.",
      "depends_on": ["07.4", "07.22", "07.20", "07.24"],
      "blocks": [],
      "terminal": false,
      "notes": "The assignment names R11-R13; the section carries only R11 (grep 'R1[0-9]' -> lines 510, 512). R12/R13 are not in this file."
    },
    {
      "id": "07.40",
      "title": "Re-run the 48-target --help matrix with provenance and a SHA-256 receipt",
      "lines": "L589-L592, L606-L607, L622",
      "what": "Record the metadata target list, exact probe command, every target's output and exit code, and a digest before any invocation-coverage ratio is claimed; the historical 23-target table (7 NOT-BUILT / 6 REAL-HELP / 3 REJECTS / 3 HELP-AS-PATH / 2 SILENT / 1 EXECUTES / 1 ERRORS) and '1 of 16 names a timeout' are not current.",
      "acceptance_seed": "run the NUMBERS.toml help_discoverable_binaries command on a pinned sha; expect a receipt artifact listing 48 rows with classification, exit code, and sha256, and NUMBERS expect updated from that run.",
      "priority": 1,
      "kind": "measurement",
      "existing_beads": [
        {"id": "omp-orchestrator-kxe.3", "status": "in_progress", "match": "partial", "evidence": "Every [figures.*] command in NUMBERS.toml runs from the repository and its expect matches the command output"},
        {"id": "omp-orchestrator-opsa", "status": "in_progress", "match": "partial", "evidence": "Add the missing two [hash, level] by editing 34 existing rows."}
      ],
      "coverage": "PARTIAL",
      "gap": "NUMBERS.toml:231 help_discoverable_binaries (expect 10) is a first-line classifier only; no bead produces the full per-target output/exit/sha receipt, and the pre-commit-gate EXECUTES / silent-binary / help-as-path defects (L609-L618) have no fix beads.",
      "depends_on": ["07.1"],
      "blocks": ["07.41"],
      "terminal": false,
      "notes": "Fix targets named in doc: no-shell-gate, state-wildcard-lint, undrained-pipe-lint (help-as-path); pre-commit-gate (executes); loop-queue-filter, pre-delete-citation-check (silent)."
    },
    {
      "id": "07.41",
      "title": "Map actions A1-A11 to binary and subcommand; every help names its guards and timeouts",
      "lines": "L581-L582, L624-L628",
      "what": "Josh's objective — every journey surface mapped to a specific command with typed guards and timeouts — is not closed by the --help census; actions are behaviours inside omp-orchestrator with no entry point, and only 1 of 16 binaries named a timeout.",
      "acceptance_seed": "a ledger with one row per action A1-A11 naming binary+subcommand; each named command's --help contains its deadline; a row without a command reads NOT-BUILT.",
      "priority": 1,
      "kind": "measurement",
      "existing_beads": [],
      "coverage": "MISSING",
      "gap": "",
      "depends_on": ["07.40", "07.6", "05.actions-A1-A11"],
      "blocks": [],
      "terminal": false,
      "notes": "Action definitions live in §05; this row is the installability-side mapping only."
    }
  ],
  "section_deps": [
    "02: the 48-target denominator (cargo metadata) and the surface census version table (L227) are inputs to 07.1 and 07.17",
    "05: actions A1-A11 must be specified before 07.41 can map them to commands",
    "06: the probe list that doctor/health enumerate is owned by the gates section (L233-L234); 07.8/07.16 consume it",
    "09: milestone sequencing — every PROJECTED item is contingent on the actuate/complete layers landing (L77-L83) and M6 is a §09 milestone",
    "03: umbrella subcommand folding (07.6) needs the crate specs' orchestration semantics"
  ],
  "search_log": [
    "br list -a --json | jq '.issues[]' -> 284 rows",
    "br show omp-orchestrator-{2tny,31lo,8nuh,7ai,installer-dep-uncommitted-txl,6hrv,omp-coverage-mission-ipg.13,jplf.7,xuu7} --json -> 9 rows read in full",
    "br show omp-orchestrator-{exit-code-registry-rub,exit-const-collision-cas,exit-1-overloaded-x1o,exit-reserved-range-b7f,n4q,4ak,io3h,npq,k4sc,round22-drifted-figures-844,1p0u,untracked-crates-break-clone-jlb,t00,xv30,opsa,kxe.3,omp-coverage-mission-ipg.12,6nhj,d1n,vyud,5tn} --json -> read (first ~900 chars each)",
    "grep -ciE over .beads/issues.jsonl title+description+acceptance: umbrella 0; pilot 0; XDG 0; 'tmux --version|UNPROBEABLE' 0; 'upstream-report|upstream_owner|upstream_substrate' 0; capabilities 0; 'release manifest|canonical manifest|distribution=INSTALL' 0; golden 0; 'why <id>|provenance trace' 0; 'omp.preflight|missing dependenc|xtask|release-artifacts' 0; M6 21 (all from vyud/xuu7/jplf.7 text); 'cold-start|clean-machine' 4; doctor 22; adapter 6 (none umbrella); config.toml 2 (qfa, xdx — cargo config, unrelated); install.sh 1 (ipg.13); append-only 6 (ack-spine ledger); build.rs 3; help_discoverable 2; 'second machine|foreign host|another machine' 4",
    "grep -rln 'omp.preflight|\"doctor\"|xtask|release-artifacts|capabilities' crates/*/src -> 10 files, all per-crate dispatch_cli_contract doctors; no umbrella, no xtask",
    "ls crates/*/build.rs -> 5 (installer, no-shell-gate, omp-orchestrator, pane-truth, tick-monitor)",
    "grep -c -- '--version|OMP_BUILD_ID' crates/tick-monitor/src/main.rs -> 3 (doc L63 '0' is stale)",
    "crates/installer/src/main.rs:13-18 BINARIES = 4 tuples (omp-orchestrator, tick-monitor, pane-truth, installer) — doc L41 says 3; :93 usage [--check | --install TARGET | --version] [--bin-dir PATH]; :165 exit 75; :115-117 explicit owned/foreign counting",
    "crates/installer/src/lib.rs:5-8 four-way contract; :160-169 RepoOwnership::Foreign; :236-239 'excluded from drift denominator'",
    "NUMBERS.toml:29 figures.built_binaries expect=LIVE; :231 figures.help_discoverable_binaries expect=10",
    "grep -n 'R1[0-9]' docs/plan/07-installability.md -> only R11 at L510, L512"
  ],
  "no_claim": "This manifest maps §07 to beads by title/description text read through br; it did not run installer --check, cargo metadata, or any NUMBERS.toml command, so current figures (48 targets, 3-vs-4 installer entries, tick-monitor --version) are cited from tree greps at this moment, not re-measured receipts. R12 and R13 are named in the assignment but do not appear in this section; their coverage is not established here. Cross-section ids (02.*, 05.*, 09.M6, 03.*) are guesses for the integrator to resolve. Coverage of a closed bead (7ai, ipg.13, 4ak, rub) is taken from its text, not from re-verifying that its acceptance still holds on HEAD — ipg.13 in particular is closed while most of its clauses are absent from the tree, which the integrator should treat as a reopen candidate."
}
