{"section":"00-brief","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":1,"severity":{"blocker":0,"major":1,"minor":0},"verdict":"MAJOR_OPEN","false_positives_verified":1,"round_sequence_note":"briefed as round 16; rounds 16-21 already exist in docs/plan/round1[6-9]*.jsonl and round2[01]*.jsonl, so the graded plan state is post-round-21 and this row is recorded as round 22 to keep the consecutive-zero accounting honest. Filename kept as briefed.","note":"RAN all 22 NUMBERS.toml commands via an independent mirror of crates/no-shell-gate/tests/numbers.rs figures()+unescape() (/tmp/r22/numgate.py): RAN=22 DRIFTED=0 LIVE_OK=2. The registry itself is clean. MAJOR: NUMBERS.toml is NOT VALID TOML - duplicate `note` key in [figures.test_functions] at NUMBERS.toml:75 and NUMBERS.toml:79. Two readers: `awk '/^\\[figures\\./{t=$0} /^note[ ]*=/{c[t]++} END{for(k in c) if(c[k]>1) print k,c[k]}' NUMBERS.toml` -> `[figures.test_functions] 2`; `bun -e 'Bun.TOML.parse(await Bun.file(\"NUMBERS.toml\").text())'` -> `TOML Parse error: Cannot redefine key 'note'`. Consequence for reproducibility: any conforming TOML consumer (taplo, toml::from_str, a CI lint) gets ZERO figures, not 22. It is invisible only because numbers.rs reads the registry with a hand-rolled line scanner (numbers.rs:50-73) that ignores `note` entirely - the same lenient-parser blindness as the gate.yml defect filed under 06-gates. 00-brief:300 makes this registry the enforcement mechanism for its own rule that every number carries the command that derives it. FALSE POSITIVE VERIFIED: 00-brief:849's board total of 95 (61+21+10+2+1) now reads 96 live (61 closed/22 in_progress/10 open/2 blocked/1 grading, `for s in closed in_progress open blocked grading; do br list --status $s --json | jq '.issues|length'; done`) - NOT a finding, because 00-brief:851 states it is a dated integration snapshot and board_total is registered LIVE. NO-CLAIM: I did not read all 865 lines of 00-brief; I ran its 39 extracted commands and audited its registry-backed and board figures."}
{"section":"01-idea","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":1,"severity":{"blocker":1,"major":0,"minor":0},"verdict":"BLOCKED","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"BLOCKER: 01-idea.md:71 states `| actuate | dispatch | **DOES NOT EXIST** - a human types into panes |`. That is false against the current tracked source. `crates/omp-orchestrator/src/main.rs:714-912` is a complete production `send_and_verify`: it stages a packet (:725-736), branches on receiver kind (:738), and for codex panes invokes `tmux send-keys -t <pane> -l <packet>` then `send-keys Enter` (:740-760), otherwise `ntm --robot-send=<session> --panes=<pane> --msg-file=<staged>` (:768-776), writes a transport receipt (:782) and loops on receiver-receipt verification against a deadline (:784-911). It is CALLED, not dead: `grep -rn 'send_and_verify' crates/omp-orchestrator/src` -> `:714` (definition) and `:1461` (call site inside the tick). The file is tracked: `git ls-files -- crates/omp-orchestrator/src/main.rs` -> present. The crate's own header already states the correct verdict at lib.rs:25-27: the subprocess wiring 'is designed but unverified at runtime'. The table's own vocabulary distinguishes existence from proof elsewhere in the same seven rows - UNVERIFIED, FENCED, 'AVAILABLE, NOT WIRED' - so the honest cell is AVAILABLE, NOT VERIFIED, matching the `complete` row. Why blocker and not wording: .flywheel/AUTONOMOUS-WAVE.md:44-49 elevates this exact line to the wave's ordering principle ('That is the product... pick the one that gets the loop closer to dispatching'), so four panes are prioritising against 'the code is missing' when the measured state is 'the code exists and is unproven'. Those are different tasks - one is writing an actuator, the other is verifying one. Positive control for the greps: `grep -rc 'fn ' crates/omp-orchestrator/src/lib.rs` -> 79, so the tree is greppable and the hits above are real."}
{"section":"02-surface-census","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":1,"severity":{"blocker":0,"major":0,"minor":1},"verdict":"ACTIONABLE","false_positives_verified":3,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"This section's checkable claims REPRODUCE, including the ones most likely to have rotted. §7.5: `jq -s '[.[]|select(.tool==\"ee\")]|length'` -> 111 and grouped by disposition -> {RETIRE:109, WIRE:2}, exactly as :571 states; ms -> 61; ntm -> 114. Map identity: 614 rows / 302002 bytes / sha256 5b3c3238c4ec9dd7f72a097bb3668e7de224e3b6f0eddc1132de2902a1d9d93c. engaged -> 152, 152/614 = 24.8%. MINOR (cross-section, filed here because §2.2 owns the tooling warning): /usr/bin/grep on this box is `pi-uu-grep 0.2.0`, a REGEX engine, and it produces false zeros on unescaped brace and paren metacharacters. Measured: `grep -c '^{' docs/plan/SURFACE-MAP.jsonl` -> 0, rc=1, while `cut -c1 docs/plan/SURFACE-MAP.jsonl | sort | uniq -c` -> `614 {` and `grep -c '^\\{' ...` -> 614. §2.2's existing warning covers `grep -r --include=` returning a false zero; it does not cover metacharacters, and the brace case is the first character of every line in this plan's own JSONL artifacts. It bit three readers in this round alone (see the refuted sibling claim below, and my own `grep -c '^    (\"'` returning 0 against a live 4). One line in §2.2 closes it. FALSE POSITIVES VERIFIED (3): (a) a sibling grader reported SURFACE-MAP.jsonl has 200 rows, not 614 - REFUTED by six readers, `grep -c .`/`wc -l`/`jq -s length`/`awk NF`/python -> 614 each, and its stated method `grep -c '^{'` answers 0 on the shell, so the 200 came from the harness grep tool's default match cap, not from the file; (b) the bolded '6.2 %' headline at :438 is hedged four lines above at :424-427 as 'HISTORICAL SNAPSHOT (NON-AUTHORITATIVE)'; (c) the 86.2% RETIRE rate at :637 is explicitly followed by 'Neither rate describes the current 614-row map'."}
{"section":"03-crates","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":2,"severity":{"blocker":1,"major":1,"minor":0},"verdict":"BLOCKED","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"BLOCKER - the numeric spine of this plan reproduces on ONE DISK and not in the REPOSITORY. 19 of 50 crates/* directories are untracked: `comm -13 <(git ls-files -- crates | cut -d/ -f2 | sort -u) <(ls -1 crates | sort)` names dispatcher-deadman fast-dispatch fleet-monitor fleet-reconcile loop-coverage loop-driver loop-tick ntm-fleet-monitor omp-idle-dispatch oracle-compare pane-dispatch-ready pane-oracle-diff pane-truth reap-finished-panes refill-idle-panes scratch-home tick-dispatch verify-dispatch wired-but-inert-guard. They are not ignored: `git check-ignore -v crates/pane-truth/src/main.rs` -> rc=1. `git ls-files -- crates/pane-truth` -> EMPTY (not even its Cargo.toml). Proof by fresh clone of HEAD 574877b (`git clone --no-hardlinks . /tmp/r22/clone-a`): workspace_crates 31 vs registry expect 50; crates_forbidding_unsafe 30 vs 50; test_files 58 vs 92; test_functions 574 vs 984; and the AGENTS.md-scanning `unextracted_crates` figure answers 18 where the registry pins 0 with zero_is_real=true. Worse, `cargo metadata --no-deps --offline` FAILS IN THE CLONE: 'failed to load manifest for workspace member crates/loop-switch referenced via crates/* by workspace' - crates/loop-switch has exactly one tracked file (src/lib.rs) and no tracked Cargo.toml, and Cargo.toml:6 declares `members = [\"crates/*\"]`, so membership is disk-derived. The workspace does not load at all from a clone, which makes built_binaries=48 and help_discoverable_binaries=10 unrunnable there. `oracle-compare` - one of the three crates AUTONOMOUS-WAVE.md:36-38 names as the blocker to the first autonomous dispatch - is on disk with ZERO tracked files. AGENTS.md's own rule applies verbatim: 'if we cant replicate a test in repo, we dont have a test.' MAJOR: 03-crates.md:341 publishes `grep -rhoE '^pub enum \\u2026' --include=*.rs crates/ | wc -l` -> 59 and 'the struct form' -> 91. The command contains a LITERAL U+2026 HORIZONTAL ELLIPSIS inside the quoted ERE, so run verbatim it answers 0; positive controls `^pub enum ` -> 127 and `^pub struct ` -> 187. Neither published figure matches any expansion, and unlike items 1 and 2 of the same subsection this one carries no HISTORICAL label while being the section's stated resolution of a disagreement with the brief."}
{"section":"04-diagrams","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":0,"severity":{"blocker":0,"major":0,"minor":0},"verdict":"ACTIONABLE","false_positives_verified":1,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"Ran all 38 commands extracted from this section (/tmp/r22/results.jsonl); no unhedged single-number command output disagreed with the figure stated beside it. FALSE POSITIVE VERIFIED: :258's `grep -rhc '#\\[test\\]'` -> 409 is 575 below the live 984 (`grep -RhoF '#[test]' crates --include='*.rs' | wc -l`), but the same sentence says 'this figure drifts with every landing test and is now tracked in NUMBERS.toml [figures.test_functions]' - hedged, and the registry pin is green. :92 labels the whole edge/degree/extraction block 'HISTORICAL GRAPH SNAPSHOT'. NO-CLAIM on coverage: my first reader for this section aborted mid-pass (it could not write its output file), so this zero rests on my command sweep plus the hedge audit, NOT on a full independent line-by-line read of all 440 lines. A later round should re-read it cold."}
{"section":"05-actions","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":0,"severity":{"blocker":0,"major":0,"minor":0},"verdict":"ACTIONABLE","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"Ran all 16 commands extracted from this section; none contradicted an adjacent figure. Claim-backing census over bolded numeric claims: 15 claims, 12 with a command or registry key within +/-2 lines, 2 hedged, 1 flagged bare and on inspection that one is the requirement label 'R10', not a figure. NO-CLAIM on coverage: same aborted first reader as 04-diagrams; this zero is a command-sweep and census result, not a cold full read of all 499 lines."}
{"section":"06-gates","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":3,"severity":{"blocker":1,"major":2,"minor":0},"verdict":"BLOCKED","false_positives_verified":3,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"BLOCKER: .github/workflows/gate.yml is INVALID YAML and therefore NONE of its nine gate jobs can run. Job `kernel-bypass-gate` declares `runs-on` twice (gate.yml:46 and :52) and `steps` twice (:47 and :53) because the `state-wildcard-lint:` job header is missing at :52 - that job's steps were appended into the previous job's body. Two parsers, two answers, which is the whole point: the strict parser refuses the file - `bun -e 'require(\"yaml\").parse(...)'` -> `Map keys must be unique at line 52, column 5` - while PyYAML safe_load ACCEPTS it and reports 9 healthy jobs (last-key-wins), so a lenient reader sees nothing wrong. GitHub Actions rejects duplicate keys, so the consequence is not 'state-wildcard-lint is missing', it is 'every gate job in CI is unreachable', and `state-wildcard-lint` additionally has no job at all. Third reader, an indent-aware python scan: `DUP key 'runs-on' inside job 'kernel-bypass-gate' at lines [46, 52]` and the same for 'steps' at [47, 53]. Same defect class as the duplicate `note` key filed under 00-brief. MAJOR: 34 of 92 integration test files are UNTRACKED - `comm -13 <(git ls-files -- crates | grep '/tests/.*\\.rs$' | sort) <(find crates -type f -path '*/tests/*.rs' | sort) | wc -l` -> 34 - including every differential.rs, mutation.rs and planted_known_bads.rs for pane-truth, tick-dispatch, pane-oracle-diff, fast-dispatch, dispatcher-deadman, fleet-reconcile, loop-driver, loop-tick, ntm-fleet-monitor, pane-dispatch-ready, reap-finished-panes, refill-idle-panes, verify-dispatch and wired-but-inert-guard, plus crates/undrained-pipe-lint/tests/test-w4j-undrained.rs. A gate whose test is untracked does not exist in the repository; the clone answers test_files=58. MAJOR: :216 and :329 both state UNWIRED_LANE_ALLOWANCE has 'five rows (20-41)' 'sharing one reason and one landing bead'. Live it is FOUR rows at wired_lanes.rs:30-35 with four DISTINCT reasons naming two different mechanisms - cron wiring at :18/:38/:58 and */5 for reap-finished-panes, refill-idle-panes and tick-dispatch, and a human-certification-blocked hook (cp-nq2s9) for wired-but-inert-guard. Two readers agree on 4 (a python regex over the const block, an awk marker scan); a third, `sed -n '31,34p' ... | grep -c '^    (\"'`, returned 0 and is the pi-uu-grep paren defect filed under 02-surface-census, not a count. :329 builds its structural argument about 'the strongest pattern in this section' on the shared-reason characterisation, which is false. FALSE POSITIVES VERIFIED (3): (a) a sibling reported 'NO GIT REMOTE, so .github workflows are permanently unreachable' - REFUTED, `git remote -v` -> `origin https://github.com/JYeswak/omp-orchestrator.git (fetch/push)`; AGENTS.md:65's claim that it returns empty is itself stale, and 06-gates makes no remote claim at all (`grep -n remote docs/plan/06-gates.md` -> no matches, positive control: the same grep for 'gate' matches 30+ lines); (b) a sibling reported SURFACE_ALLOWANCE and FORBID_ALLOWANCE as uncited - both exist and are empty exactly as :198 says, at wired_lanes.rs:625 and :665; (c) :118's `wired_lanes.rs:85` for STRIP_TEST_CODE is now :90 but the sentence pins it to snapshot 995a147, so it is labelled, not drifted."}
{"section":"07-installability","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":3,"severity":{"blocker":0,"major":3,"minor":0},"verdict":"MAJOR_OPEN","false_positives_verified":2,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"MAJOR: :31's three-instrument table is drifted in two of three rows, and it is the row the section built to replace a figure that had already drifted twice. `grep -rl 'fn main' --include=main.rs` is published as 18; run verbatim (implicit `.`) it answers 51, scoped to crates/ it answers 41. Three readers on the scoped form: that grep -> 41, `find crates -name main.rs -type f | wc -l` -> 41, `git ls-files | grep -c '/main\\.rs$'` -> 22 (tracked only - itself the untracked-crate defect). MAJOR: the same table publishes `grep -c '^\\[\\[bin\\]\\]' crates/*/Cargo.toml` as 16; it sums to 40 (`for f in crates/*/Cargo.toml; do grep -c '^\\[\\[bin\\]\\]' $f; done | awk '{s+=$1} END{print s}'` -> 40, and an independent python count of lines equal to '[[bin]]' -> 40). Only the third row (cargo metadata) is labelled a historical snapshot. MAJOR: :425-426 asserts `crates/installer/src/main.rs:25` 'falls back to PathBuf::from(\"/Users/josh\") when HOME is unset' and 'lines 118-119 default CARGO to ~/.cargo/bin/cargo'. Both are false against the tracked source. main.rs:25 is `.or_else(|| dirs_home().map(|home| home.join(\".local/bin\")))` and :27 prints `INSTALLER ERROR: INSTALL_BIN_DIR or HOME must be set` returning ExitCode 2 - i.e. the hard-error behaviour the section then PROPOSES as remediation already shipped. main.rs:118-119 is a `println!(\"INSTALLER IDENTITY OK: ...\")`. Two readers find zero `/Users/josh` anywhere in the crate: `grep -rn '/Users/josh' crates/installer/` -> exit 1 no output, and a python literal scan over every file under crates/installer -> hits=0; positive control `grep -rc 'INSTALL_BIN_DIR' crates/installer/src/main.rs` -> 2, so the tree is readable. The section therefore costs and schedules a config-file remediation for a defect that no longer exists. FALSE POSITIVES VERIFIED (2): (a) :328's `ls $M/*/install.sh | wc -l` -> 50 reproduces exactly once `M` is taken from :16, where the section explicitly defines it - my extractor lost the assignment, the section did not; (b) a sibling reported the BINARIES const has drifted from :12 to :11 - REFUTED, `grep -n BINARIES crates/installer/src/main.rs` -> `12:const BINARIES: &[&str] = &[\"omp-orchestrator\", \"tick-monitor\", \"pane-truth\"];`."}
{"section":"08-end-users","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":1,"severity":{"blocker":0,"major":1,"minor":0},"verdict":"MAJOR_OPEN","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"MAJOR: :69 carries the word `MEASURED` on a claim that is false today - 'crates/installer/src/main.rs:16 resolves the repo root from env!(\"CARGO_MANIFEST_DIR\")... `:25` falls back to a literal /Users/josh; :12 hardcodes three binary names.' Of those three cites, :16 and :12 reproduce; `:25` does not. Live main.rs:25 is `.or_else(|| dirs_home().map(|home| home.join(\".local/bin\")))`, and there is no `/Users/josh` literal anywhere in crates/installer (shell grep exit 1 with no output; python literal scan hits=0; positive control `grep -rc 'INSTALL_BIN_DIR' crates/installer/src/main.rs` -> 2). This is the same false claim as 07-installability:425 and must be fixed in both, because two sections independently asserting it is what makes it read as corroborated. CONFIRMED BACKED in the same paragraph: the 61 CARGO_MANIFEST_DIR sites reproduce - the registry's LIVE `cargo_manifest_dir_sites` command answers 61, matching the prose, and the prose correctly cites the command rather than freezing the count."}
{"section":"09-milestones","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":0,"severity":{"blocker":0,"major":0,"minor":0},"verdict":"ACTIONABLE","false_positives_verified":7,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"ZERO findings, and seven false positives refuted - the largest FP cluster of the round, all one mistake. A first reader filed seven BLOCKERs, one per milestone M1-M7, on the ground that every observable cites `crates/omp-orchestrator/tests/fixtures/` and that directory does not exist. The directory genuinely does not exist (`ls -d crates/omp-orchestrator/tests/fixtures` -> No such file or directory; `ls crates/omp-orchestrator/tests/` -> disk_pressure_observation.rs gate_wiring_wave2.rs gate_wiring_wave3.rs no_noop.rs target_directory.rs target_ownership.rs wrapper.sha). It is not a defect: every milestone states the absence itself as its own precondition - :54 'the pre-milestone tree fails because the test, fixture, dependency edge, and shared signature are absent', and the same construction at :79, :98, :122, :145 and :217. The fixture path is the milestone's ACCEPTANCE TARGET, not a claim of present existence. Grading a specification as a claim is the shape to watch for: it produces a maximum-severity finding from a correct document, and the same error appears to have been recorded once already in round 15. Claim-backing census: 24 bolded numeric claims, 21 with a command or registry key within +/-2 lines, 3 hedged, 0 bare."}
{"section":"10-prior-art","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":0,"severity":{"blocker":0,"major":0,"minor":0},"verdict":"ACTIONABLE","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"Zero findings. The reproducibility risk specific to this section is external citation rot, and it does not fire today: /Volumes/ZestData/dicklesworthstone-mirror is mounted and readable (`ls -d` succeeds), and the sampled mirror, skills and fh citations resolve at the paths given. `ls $M/*/install.sh | wc -l` -> 50 against the same mirror, consistent with 07-installability:328. Claim-backing census: 16 bolded numeric claims, 15 with an adjacent command, 1 flagged bare and on inspection that one is the requirement label 'R7'. NO-CLAIM, and it matters for this section more than the others: citation checking was SAMPLED, not exhaustive, and every claim here depends on an external volume that a different reader may not have mounted - a citation into /Volumes/ZestData is unreproducible for anyone without that disk, which is a portability limit on the section's evidence rather than an error in it."}
{"section":"11-lifecycle","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":0,"severity":{"blocker":0,"major":0,"minor":0},"verdict":"ACTIONABLE","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"Zero findings, and this section carries the best-backed claim in the plan. :249 publishes a three-part artifact identity - '614 rows, 302,002 bytes, SHA-256 5b3c3238c4ec9dd7f72a097bb3668e7de224e3b6f0eddc1132de2902a1d9d93c' - and all three reproduce exactly: `grep -c . docs/plan/SURFACE-MAP.jsonl` -> 614, `wc -c` -> 302002, `shasum -a 256` -> 5b3c3238c4ec9dd7f72a097bb3668e7de224e3b6f0eddc1132de2902a1d9d93c, with the superseded 591-row/f155a358 identity labelled historical in the same sentence. That is the shape every other section should copy: a content hash cannot drift silently. Every `DECLARED (NUMBERS.toml)` marker checked names a key that exists in the registry - e.g. :52's installer 3-of-48 resolves to installer_known_binaries and built_binaries, both green in the 22-command sweep."}
{"section":"12-journey","round":22,"lens":"reproducibility","role":"fresh_eyes","graded_by":"Opus","new_findings":1,"severity":{"blocker":0,"major":1,"minor":0},"verdict":"MAJOR_OPEN","false_positives_verified":0,"round_sequence_note":"recorded as round 22; see 00-brief row.","note":"MAJOR: :298's F-stage table row reads `| F5 numbers | numbers.rs + NUMBERS.toml, 13 figures | this session |`. The registry carries 22. Two readers: `grep -c '^\\[figures\\.' NUMBERS.toml` -> 22, and my independent parser mirroring numbers.rs figures() -> REGISTRY_FIGURES=22 with all 22 commands executed. Unhedged, and it is the section that defines the KNOWN/PROJECTED/UNKNOWN taxonomy at :261 ('KNOWN - measured, with the command') - so a stale count in the row describing the number-registry stage is the taxonomy failing on itself. Confirmed BACKED elsewhere in the section: :342 correctly declares ZERO figures for a stage that has not run, reasoning that 'declaring a number for a stage that has never executed is a figure with no derivation' - that is the right instinct and the reason :298 stands out. Sampled KNOWN claims that resolved: FINDING_THRESHOLD == 3 at :410 against crates/finding-dispatch/src/lib.rs:15, and the 3 HD rows at :472 against docs/decisions.jsonl. NO-CLAIM: KNOWN-claim verification across all 1440 lines was SAMPLED, not exhaustive; this section is the largest in the plan and deserves a dedicated cold read."}
